> **开发口径记录（收尾补充 · 由 AI 维护，随开发更新）**
> - **实现体例**：无人机＝通用 `drone` 模板 + `effects.summon`（一模块一文件、注册进 `js/data/modules.js`），参考 `js/data/modules/drone/laserDroneSpawn.js`；`target:{}`（召唤类无命中目标）、携带模组**等级＝召唤模块等级（不受槽限）**、`temp:true` + `lifespan_ticks`、`maxSummoned`。
> - **既有模块映射（不新建重复模块）**：硬化护盾＝`shield/hardShield`、再生护盾＝`shield/regenShield`、同盟护盾＝`shield/allianceShield`、火箭发射器＝`attack/rocketLauncher`、火炮＝`attack/cannon`、激光＝`attack/laser`、固若金汤＝`function/impregnable`。
> - **「维修光束」**：新建 `js/data/modules/function/repairBeam.js`，**功能类**、**`picker:false` 仅无人机专属**（玩家不可在编队安装）、单体**仅友方**（天然排除自身）、耗能、含冷却；回血走既有唯一口径 `hp_target` + **`exact_amount`**（词条原值、**不乘类别系数**）经 `applyHpTo` 落地（按 `hpMax` 截断、不受受伤减免），门控沿用 `canImpact`（能量不足／目标满血 ⇒ 不激活），结算沿用**步骤 4c**。
> - **数值策略**：本文件**已给定的硬数值逐字照填**；**未给出的项（含各召唤模块冷却/能耗/召唤上限、未给三围的无人机 hp/护盾/能量/存在时间、维修光束回血/能耗/冷却）一律由 AI 预填占位值**（逐级绝对表、注释标注【占位预填 · 待用户调校】），**待用户人工调参**。
> - **存在时间**：本文件给出者照填（两个炮塔 **2400t**）；**未给者暂沿用 `laserDroneSpawn` 的占位 1200t**，待调参。
> - **图标**：**两个炮塔＝实心正六边形 SVG**（已交付 `assets/img/激光炮塔.svg`、`assets/img/哨戒炮塔.svg`，主色＝无人机类棕 `--cat-drone: #b07a4a`、外框逐字符一致、内部纹样区分）；**维修/壁垒/火箭三个无人机暂不设 `icon`**（走名称首字降级），待美术补图。
> - **清单归属（已统一）**：设定里的 **壁垒无人机**／**维修无人机**／**火箭无人机**／**激光炮塔**／**哨戒炮塔**／**纳米无人机** 对应 §6.2 的 `C38`（原护盾无人机→**壁垒无人机**）／`C39 维修无人机`／`C40 火箭无人机`／`C41`（原激光无人机→**激光炮塔**）／`C42`（原火炮无人机→**哨戒炮塔**）／**新增 `C56 纳米无人机`**；「维修光束」另立 **`C55 维修光束`（功能类）**。
> - **收尾状态（无人机类暂时开发完毕）**：6 个召唤模块（维修/壁垒/火箭无人机、激光/哨戒炮塔、纳米无人机）＋ 1 个专属功能模块（维修光束）**全部实装并通过人工测试**；引擎与 UI/CSS **零改动**（全部走既有 `summon` 路径）；**待美术补图**：维修/壁垒/火箭/纳米四个无人机暂无图标（名称首字降级：维/壁/火/纳），两个炮塔已用实心正六边形图标。
> - **已验证的关键行为**：`maxSummoned` **按模块 id 分别计数**（不同召唤模块互不挤占）；**纳米无人机的父体与子体同 id ⇒ 共用同一上限**，故**链式召唤不会无限增长**（实测种群上限内回补）；`lifespan_ticks` 到期**自动死亡并移出场景**；`cool_first` ⇒ **部署即进入冷却、开场不白送**；新召单位**下 tick 才开始行动**（无同 tick 连锁）。
> - **已裁决项（无人机类）**：① 文档给定硬数值**各级保持不变**（逐级表只放大占位项）；② **维修无人机保持 `combat` 定位**（未改 `logistics`）；③ **维修光束暂不产生战报**；④ **无护盾单位（`shieldCap:0`）保持显示空盾条「0/1」**（与既有火箭/导弹召唤物一致，UI 未改）。

# 维修无人机
搭载专用的维修光束模块。新增特殊的维修光束模块，功能类模块，单体目标，仅对友方，激活一次恢复目标一定生命值，消耗能量。
# 壁垒无人机
搭载一个固若金汤模块，与一个硬化护盾，自生生命值较低50，能量恢复为50，能量上限2000
# 火箭无人机
搭载一个火箭发射器模块，能量恢复为50，能量上限1000
# 激光炮塔
搭载2个激光模块，与一个再生护盾，能量恢复为30，能量上限1000，存在时间2400
# 哨戒炮塔
搭载1个火炮模块，与一个同盟护盾，能量恢复为30，能量上限1000，存在时间2400
# 纳米无人机
搭载1个火炮模块，与自身的纳米无人机模块（可继续召唤），无护盾，血量50，能量恢复20，能量上限500，存活时间＝本模块冷却时间×2（预写数值在属性即可：占位冷却200 ⇒ `lifespan_ticks 400`，调整冷却时需同步改该值）。
----
两个炮塔图标需要使用一个实心的正六边形SVG作为图标。有其它说明可补充该文档。