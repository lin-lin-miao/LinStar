# 模块配置字段说明（data/modules/<id>.js）

> 供开发/填表参考：解释每个模块文件里的字段与 `effects` 词条含义。
> 命名铁律：**effects 词条与通用配置字段一律【下划线】命名**（如 `shield_cap_target`）；`target` 内 `kinds/countMode/maxCount` 目前仍为 camelCase（是否并入下划线未定）。
> 数值铁律：所有模块的**功能与数值由用户人工设定**，仅存于各模块数据文件。

## 1. 模块对象字段总表

| 字段 | 类型 | 含义 | 是否跨等级共享 |
|------|------|------|:---:|
| `id` | string | 模块唯一标识（引擎引用） | ✔ |
| `nameKey` | string | 名称 i18n 词条 key（如 `module.cannon`） | ✔ |
| `name` | string(可选) | 名称降级占位：i18n 表中无该 `nameKey` 词条时用此文本（仍未填则回退 `id`）。**目前各模块均无需使用** | ✔ |
| `desc` | string(可选) | 描述降级占位：i18n 表中无该模块描述词条时用此文本。**目前不使用** | ✔ |
| `icon` | string(可选) | SVG 图标路径（如 `assets/img/...svg`，相对引用）。**有则模块槽/详情用图标；无则降级显示“名称首字”**（取本地化名称第一个字符） | ✔ |
| `glyph` | string(废弃建议) | 旧“槽短图标字符”（如 `炮`）。已由 `icon`→名称首字 取代展示，保留字段仅为兼容、不再作为渲染依据 | ✔ |
| `category` | string | 分类：`attack / shield / function / transport / mining / drone` | ✔ |
| `target` | object | 目标词条，见 §3 | 基底=Lv1，可被 levels 覆盖 |
| `effects` | object | 效果词条表，见 §2；模块文件里的即为 Lv1 | 基底=Lv1，可被 levels 覆盖 |
| `maxLevel` | number | 该模块最高可用等级；缺省时由 levels 最高等级+1 推算（至少 1） | ✔ |
| `levels` | array | 逐级绝对表（高阶等级与上一级的**差异**），见 §4 | —— |

## 2. effects 效果词条（下划线风格，词条驱动执行器）

> 引擎依据**存在哪些词条**自动执行，不做按 `type` 的硬编码；`type` 只是特殊效果钩子的标记列表。

### 2.1 伤害类
| 词条 | 类型 | 含义 |
|------|------|------|
| `damage` | number | 对每个选定目标造成伤害（乘**该船对应分类的系数**） |
| `max_damage` | number | （可选）逐步伤害封顶：伤害持续同目标累计，封顶 `max_damage` |
| `ramp_per_hit` | number | 逐步伤害成长率：攻击目标不变时，每次成功激活 +`ramp_per_hit`；目标改变/失去、模块**能量不足**、或停用 → 成长清零（回到基础值） |
| `shield_gain` | number | **仅作用于自身**：正=自身回盾（乘护盾系数） |
| `shield_cap_bonus` | number | **仅作用于自身**：提升自身护盾上限（安装即生效；若同时有 `duration_ticks` 则改为只在持续期生效） |
| `shield_reflect` | number | **仅作用于自身**：反射护盾——本模块护盾叠层被攻击消耗时，把落在本层内的伤害按此系数返还给攻击者（乘盾系数后的实际层量；`1`=全反射）。返还对攻击者按护盾→血正常结算，且不再二次反射 |

### 2.2 指向目标的量值词条（触发型，每次激活结算一次）
| 词条 | 含义 |
|------|------|
| `shield_gain_target` | 指向选定目标：正=为其回盾，负=削减其**当前护盾**（乘对应系数） |
| `hp_target` | 指向选定目标：正=加血，负=扣**当前血量**（负值可致死，绕护盾） |
| `energy_target` | 指向选定目标：正=加能，负=扣**当前能量** |

### 2.3 指向目标的上限词条（单次一次性、仅对当前所选目标）
| 词条 | 含义 |
|------|------|
| `shield_cap_target` | 抬(+)/压(−)目标**护盾上限** |
| `hp_cap_target` | 抬(+)/压(−)目标**血量上限** |
| `energy_cap_target` | 抬(+)/压(−)目标**能量上限** |

> 上限类语义（★）：**单次一次性、仅作用于当前所选目标**——每次触发先把上一次施加在(旧)目标上的影响撤销（`dropSourceMods`），再对本次解析到的目标重新施加；不随多次触发累加；切换目标后于**下一次触发**时作用到新目标（旧目标复原）；携带者阵亡 / 模块停用 / 时长结束亦撤销。所有上限每次从该船基准（`baseShieldCap/baseHpMax/baseEnergyCap`）重算后再叠加，故非累加。

### 2.4 数值/节奏词条
| 词条 | 含义 |
|------|------|
| `cooldown_ticks` | 激活间隔（tick 数）；配置里没有则默认 1 tick（每 tick 判定一次） |
| `energy_cost` | 每次激活消耗能量；能量不足则不触发、不扣能、无部分执行 |
| `duration_ticks` | 效果持续 tick 数；时长型模块持续期内不可重复触发，到期自动进冷却；持续期外 `shield_cap_bonus` 不贡献上限 |

### 2.5 特殊效果钩子
| 词条 | 类型 | 含义 |
|------|------|------|
| `type` | array\<string\> | **列表参数**，仅为特殊效果钩子的标记（如 `projectile`）；一个模块可有多个；未来新特殊效果据此接入，勿按它硬编码伤害/回复 |

已定义/保留的 `type` 标记：
- `projectile` / `beam`：仅作表现类标识（当前不触发额外逻辑）。
- `summon`：**召唤类模块**——激活时按其 `effects.summon` 配置补召一个临时单位。`effects.summon` 字段：
  - `type`：被召唤单位模板 id（`data/ships.js` 中模板；无人机统一用通用 `drone` 模板）；
  - `modules`：召唤单位携带的模组数组（数量**不受该单位 slots 上限**约束；未给 level 的模组沿用召唤模块等级）；
  - `attrs`（可选）：按船型模板结构对 `type` 模板做**全条目覆写**（缺省的条目沿用模板），键与 `data/ships.js` 单船一致：
    - `nameKey`：可选，为召唤单位显式指定名称词条；模块**未指定**时才把召唤单位显示名覆写为所属召唤模块 `nameKey`；
    - `base.{hp, shieldCap, energyCap, energyRegen}`：覆写基础三围；
    - `coefficients.{attack,shield,function,transport,mining,drone,...}`：覆写类别系数；
  - 召唤单位三围等属性可在模块 `levels` 中按级调整（`levels` 里重写 `summon` 即可）；
  - `lifespan_ticks`：临时单位存在时间(tick)，到期自动死亡；
  - `maxSummoned`：该阵营同类型单位“存活最大数”，达到后不再补召（保持待命，有空位即补）；
  - `temp`：召唤单位性质。缺省 `true`=临时单位——受 `lifespan_ticks` 存在时间约束、到期自动死亡，且**阵亡/到期后直接移出场景**（不保留灰色卡片）；`false`=普通单位——**无存在时间限制**（忽略 `lifespan_ticks`），持续作战至死亡，阵亡后**保留灰色卡片**（与普通舰一致）。
  - 召唤单位**图标 = 所属召唤模块的 `icon`**；模块无 `icon` 时该单位降级显示 `▲`。
- `ramp_full`：**逐步伤害的满目标条件**——仅在“目标选择的所有槽位都有目标”（如 multi maxCount=2 时两目标都在）才逐击增伤；目标未满（如只命中 1/2）则不成长、伤害维持基础值。作用于 `ramp_per_hit` 的成长判定。
- `ramp_by_enemy_count`：**伤害随当前场上敌方存活数提升**（不随时间累积）——单发伤害 = `damage` + `ramp_per_hit` × 当前敌方存活数，有 `max_damage` 则封顶；敌方阵亡越多，单发越低（如密集弹幕）。作用于伤害结算的增量解释。
- `alliance`：**同盟护盾**——本模块的时长型大护盾（`shield_cap_bonus`+`duration_ticks`，施放者共享层）是一个**供整支友方共用**的共享盾池。友方任一单位**自身盾空、伤害将扣血**时，由友方各同盟护盾的共享层按**释放顺序**逐层吸收（层值扣在施放者盾池上）。指挥栏**单独一行**显示友方同盟护盾合计护盾条；施放者同盟层被吸收时其护盾条**深蓝闪烁**。叠层 / 破盾机制适用。
- `no_break`：**破盾后仍保持激活**——层被打空也不提前结束、不进入冷却，维持激活直到自然持续到期；通常与 `alliance` 搭配（共享池不因被抽干而提前失效）。
- `blast`：**爆炸型伤害**标记——带此钩子的模块造成的伤害属于“爆炸伤害”（如火箭爆炸/导弹爆炸），可被**防爆护盾**吸收；主目标命中若被防爆池吸收，则该爆炸的 `blast_range` 被抑制（不再波及相邻单位）。
- `blastproof`：**防爆护盾**标记——本模块是防爆型共享层：**只吸收爆炸型伤害**（有 `blast` 钩子的伤害），不吸收普通伤害；指挥栏单独一行列出防爆池合计。仅含 `blastproof`（不带 `alliance`）时＝仅单个单位的防爆护盾（非跨友方共享）。

> 语义注意：作用于自身的词条（`shield_gain`/`shield_cap_bonus`）**只作用于模块所属自身**；若要给其它单位（含自身也需指向）用，须用带 `_target` 的目标词条。负值抑制必须用 `_target` 词条，**勿对自身词条取负**。

## 3. target 目标词条

| 字段 | 值 | 含义 |
|------|----|------|
| `kinds` | array | 可用目标：`self`(自身) / `ally`(同阵营其它存活) / `enemy`(敌方存活) / `any`(敌我任意含自身)；目标池=对应存活单位按全队策略排序 |
| `countMode` | `single` / `multi` / `all` | `single`=单体；`multi`=有限多目标(用 `maxCount`)；`all`=全队命中所有符合条件的存活目标 |
| `maxCount` | number | `countMode='multi'` 时的目标数上限（`single`/`all` 忽略） |

> 解析链：模块手动目标 → 所属船 `targetId` → 指挥栏全队策略（order/最低血量/最低护盾）。空位由上游自动补足；无可用目标则不激活；手动指定目标阵亡自动回落并写战报。

## 4. levels 与等级语义

- 模块文件里 `effects`/`target` 即 **Lv1**（基底）。
- `levels[]` 是**逐级绝对表**，只写“该等级与上一级的差异”，例如：
  ```js
  levels: [
    { level: 2, effects: { damage: 20 } },                 // Lv2 仅覆盖 damage
    { level: 3, target: { kinds: ['enemy','ally'] } },     // Lv3 覆盖 target
  ],
  ```
- **未填的字段回退“上一级”**（递归向上，最终以 Lv1/基底兜底）——高阶等级不必重复全部字段。
- 机制类字段（`type` 钩子、`target` kinds 等）**允许随等级变**（在某级覆盖即可）。
- 实例默认等级 = 1；引擎按实例等级把 base 与各级差异合并成该级可用的 `cfg`（`resolveModuleCfg`）。
- “升级(耗资源)”操作在 M3/M4 船坞实现；M2 只落地数据级模型 + 等级生效 + 演练开战可选等级。

## 5. 命名与数据约定
- effects 词条 / 后续通用字段一律下划线命名。
- 新增模块 = 四处联动：模块数据文件(含 levels) + 引擎(如需新词条/钩子) + i18n 词条 + UI 呈现。
- 数值一律进 `data/`，禁止散落硬编码。
