# 模块配置字段说明（data/modules/<id>.js）

> 供开发/填表参考：解释每个模块文件里的字段与 `effects` 词条含义。
> 命名铁律：**effects 词条与通用配置字段一律【下划线】命名**（如 `shield_cap_target`）；`target` 内 `kinds/countMode/maxCount` 目前仍为 camelCase（是否并入下划线未定）。
> 数值铁律：所有模块的**功能与数值由用户人工设定**，仅存于各模块数据文件。

## 1. 模块对象字段总表

| 字段 | 类型 | 含义 | 是否跨等级共享 |
|------|------|------|:---:|
| `id` | string | 模块唯一标识（引擎引用） | ✔ |
| `nameKey` | string | 名称 i18n 词条 key（如 `module.cannon`） | ✔ |
| `name` | string(可选) | 名称降级占位：i18n 表中无该 `nameKey` 词条时用此文本（仍未填则回退 `id`）。**目前各模块均无需使用** | ✔ |
| `desc` | string(可选) | 描述降级占位：i18n 表中无该模块描述词条时用此文本。**目前不使用** | ✔ |
| `icon` | string(可选) | SVG 图标路径（如 `assets/img/...svg`，相对引用）。**有则模块槽/详情用图标；无则降级显示“名称首字”**（取本地化名称第一个字符） | ✔ |
| `glyph` | string(废弃建议) | 旧“槽短图标字符”（如 `炮`）。已由 `icon`→名称首字 取代展示，保留字段仅为兼容、不再作为渲染依据 | ✔ |
| `category` | string | 分类：`attack / shield / function / transport / mining / drone` | ✔ |
| `target` | object | 目标词条，见 §3 | 基底=Lv1，可被 levels 覆盖 |
| `effects` | object | 效果词条表，见 §2；模块文件里的即为 Lv1 | 基底=Lv1，可被 levels 覆盖 |
| `maxLevel` | number | 该模块最高可用等级；缺省时由 levels 最高等级+1 推算（至少 1） | ✔ |
| `levels` | array | 逐级绝对表（高阶等级与上一级的**差异**），见 §4 | —— |

## 2. effects 效果词条（下划线风格，词条驱动执行器）

> 引擎依据**存在哪些词条**自动执行，不做按 `type` 的硬编码；`type` 只是特殊效果钩子的标记列表。

### 2.1 伤害类
| 词条 | 类型 | 含义 |
|------|------|------|
| `damage` | number | 对每个选定目标造成伤害（乘**该船对应分类的系数**） |
| `max_damage` | number | （可选）逐步伤害封顶：伤害持续同目标累计，封顶 `max_damage` |
| `ramp_per_hit` | number | 逐步伤害成长率：攻击目标不变时，每次成功激活 +`ramp_per_hit`；目标改变/失去、模块**能量不足**、或停用 → 成长清零（回到基础值）。另：被含负 `energy_cap_target`（“瘫痪”）的影响命中时亦被**一次性清零**（见 2.3） |
| `shield_gain` | number | **自身词条（无 `_target` 后缀 → 硬作用于模块所属自身，已由引擎强制）**：正=自身回盾（乘护盾系数）。要作用到目标请用 §2.2 的 `shield_gain_target` |
| `shield_cap_bonus` | number | **仅作用于自身**：提升自身护盾上限（安装即生效；若同时有 `duration_ticks` 则改为只在持续期生效） |
| `shield_reflect` | number | **仅作用于自身**：反射护盾——本模块护盾叠层被攻击消耗时，把落在本层内的伤害按此系数返还给攻击者（乘盾系数后的实际层量；`1`=全反射）。返还对攻击者按护盾→血正常结算，且不再二次反射 |
| `blast_range` | number | 范围波及：除每个选定目标外，再作用于其所在队列**视觉顺序**前后各 N 个存活单位（同额伤害；爆炸型可被防爆抑制，见 §2.5 `blast`）。同一词条也可把**上限词条**的作用集合扩为“目标及波及”，见 §2.3 |

### 2.2 指向目标的量值词条（触发型，每次激活结算一次）
| 词条 | 含义 |
|------|------|
| `shield_gain_target` | 指向选定目标：正=为其回盾，负=削减其**当前护盾**（乘对应系数） |
| `hp_target` | 指向选定目标：正=加血，负=扣**当前血量**（负值可致死，绕护盾） |
| `energy_target` | 指向选定目标：正=加能，负=扣**当前能量** |

### 2.3 指向目标的上限词条（单次一次性、仅对当前所选目标）
| 词条 | 含义 |
|------|------|
| `shield_cap_target` | 抬(+)/压(−)目标**护盾上限** |
| `hp_cap_target` | 抬(+)/压(−)目标**血量上限** |
| `energy_cap_target` | 抬(+)/压(−)目标**能量上限**。用**大负值占位**（如 `-1000000`）即“压到 0”＝**清空该上限**（引擎按目标基准重算后 clamp 至 0，当前值随之 clamp 到上限），电磁脉冲即此用法 |
| ~~`include_self`~~ | ★ **已从词条改为 `type` 标签**（见 §2.5）：本模块的效果（上限类/时间加速等）**同时施加于自身**。不把 `self` 放进 `kinds`，以免破坏“必须有目标才激活”的语义 |

> 上限类语义（★）：**单次一次性**——每次触发先把上一次施加在(旧)目标上的影响撤销（`dropSourceMods`），再对本次解析到的作用集合重新施加；不随多次触发累加；切换目标后于**下一次触发**时作用到新目标（旧目标复原）；携带者阵亡 / 模块停用 / 时长结束亦撤销。所有上限每次从该船基准（`baseShieldCap/baseHpMax/baseEnergyCap`）重算后再叠加，故非累加。
> **所有数值修改统一到结算阶段（★，对等性 · 跨单位统一顺序）**：Pass1 **只做“决策 + 读 tick 起始快照 + 记账”，不产生任何可见数值变化**。逐项意图记入 `__pending`：`dmg`（伤害）/ `shieldHeals`（补·汲取盾）/ `energyDeltas`（`*_target` 量值）/ `hpDeltas` / `selfDestruct` / `capOps`（上限修改）/ `coeffOps`（系数/受伤减免修饰）/ `hastenOps`（时间加速）/ `expiries`（时长到期撤销·恢复）/ `forceOps`（强制目标）/ `energyRegen`（回充）/ `energySpends`（模块能量消耗）/ `poolFills`（时长护盾池“创建+填满”）/ `tempTick`（临时单位寿命递减）。Pass2 的 **Phase A 只用一遍单位 for 收集**这些意图成若干**记录数组**，随后按**固定的全局顺序**（迭代记录数组，跨单位一致）落地：
> 1. **计时推进**（模块时长/冷却递减 + 窗口累计）——本引擎保留在 Pass1 就地完成，因为它是**模块私有计时器**、对任何单位数值无可见影响、也不波及其它单位；其中唯一有跨单位影响的部分（**到期撤销/恢复**）已改为意图，并入步骤 2。
> 2. **上限与系数/受伤减免/时间加速统一落地**：到期撤销（`clearAllSourceMods` + `recalcDerived`，含删池） + `capOps`（`dropSourceMods` → 逐目标 `setOverlay`） + `coeffOps`（系数加性 → `coeffMods`；受伤减免 → `damageTakeMulMods`） + `hastenOps`（时间加速 → `hastenMods`，多来源取 max）一次做完，**必须先于伤害结算**（Pass2 承伤读到最新护盾池/上限与最新减免）；2b 再落地 `forceOps`（强制来源栈）。
> 3. **能量统一落地**：回充 → 模块消耗（按记录序）→ `energy_target` 量值。
> 4. **护盾/模块池填充/血量/自毁/临时寿命统一落地**：4a 模块护盾池创建+填满 → 4b 补/汲取盾（含汲取抽空破盾）→ 4c 血量 → 4d 自毁 → 4e 临时单位寿命递减/到期判死。
> 效果：本 tick 所有单位的行动判定/耗能/回能都在**同一 tick 起始状态**下完成，跨单位同类操作顺序统一，镜像编队结果一致；施放方本 tick 已判死其意图仍照常落地；目标已死者跳过必要施加。
> **作用集合（激活瞬间冻结）**＝ 解析到的目标 ∪（`blast_range>0`：各目标所在队列**视觉顺序**前后各 N 个存活单位）∪（`type` 标签 **`include_self`**：模块所属自身）。★ **“自身是否产生溅射”取决于它是怎么进集合的**：**由 `include_self` 标签补入的自身不产生溅射**（它不进“解析到的目标”，只在最后并入集合）；但若**目标选择器正常解析出了自身**（`kinds` 含 `self`/`any` 时被 `prefer_self` 默认取到，或玩家手动把自身选为目标），则它与任何别的目标同权，**`blast_range` 照常对自身所在队列前后各 N 个存活单位生效**。持续期内新入场单位不受影响。
> **“瘫痪”附加行为（★，仅当词条含负的 `energy_cap_target`，即把能量上限压到 0；电磁脉冲即此用法）**：与上限应用**同处同批**（结算步骤 2）对**作用集合内每个受影响单位** —— ①各记一条战报 `{actor}的{module}使{target}瘫痪`（每次激活、每单位一条，持续期内不重复）；②**一次性清空**其身上 `ramp_per_hit` 渐增型武器的成长计数（`_ramp` 归零 → 伤害回到基础值；与“目标改变/能量不足/停用 → 成长清零”同一套语义）。持续期内其能量上限为 0，本就无法再激活/成长。
> **上限重算只动上限、不动护盾【池值】（★）**：目标级上限叠加写入本体池 `capExtra` 后由 `recalcDerived` 以**正确 permanentBonus** 统一重算 cap 与汇总；**绝不**借用 `baseShieldPoolOf`（其内部 `ensureBasePool(…,undefined)` 会把常驻护盾如再生护盾的 permanentBonus 视作 0 而算小 cap，进而被 `value=min(value,cap)` 钳掉真实池值）。
> **上限词条的大负值 = 清空（展示层规则）**：上限类词条给到极大负值（如 EMP 的 `energy_cap_target: -1000000`）在引擎里等价于“把该上限压到 0”；UI 明细**不再显示这个占位数字**，而是显示 `清空目标XX上限`（阈值规则：数值 ≤ `-1000` 即判为“清空”，对任意上限词条通用，见 `battleView` 的 `CAP_CLEAR_MAX`/`capTermText`）。纯展示层，**计算语义不变**。

### 2.4 数值/节奏词条
| 词条 | 含义 |
|------|------|
| `cooldown_ticks` | 激活间隔（tick 数）；配置里没有则默认 1 tick（每 tick 判定一次） |
| `energy_cost` | 每次激活消耗能量；能量不足则不触发、不扣能、无部分执行 |
| `duration_ticks` | 效果持续 tick 数；时长型模块持续期内不可重复触发，到期自动进冷却；持续期外 `shield_cap_bonus` 不贡献上限。**持续型的唯一判据就是它**（`duration_ticks > 0`；`'duration'` 不是 `type` 标签，勿再写）。**所有"持续型修饰词条"（`attack_coeff_add`/`damage_coeff_mul` 及 `*_target` 版本、`force_target_self` 标签）都要求它**——持续期结束是唯一的自动撤销时机 |
| `attack_coeff_add` | **自身词条**（无 `_target` 后缀）：**类别系数加性增益**（**加法值**）——加法提升**模块所属自身**单位的 `attack`（攻击类）系数，即其攻击类模块每次伤害/效果量的**乘算基数**（如 base 1.0 → 1.2 表示 +20%）。不影响其它类别（`shield`/`function`…）系数，也不改动护盾池/时长逻辑。**唯一口径**：由 `ship.js` 的 `coeff(ship, category)` 在返回时叠加（`(基础系数 + Σ 同类别加性修饰) × Π 同类别乘性修饰`），战斗结算（`maybeActivate` 的伤害/效果量）、护盾池容量（`modulePoolCapOf`）与 UI 明细（`battleView.perActText`）全部走该函数。修饰以**模块实例 id 为 key 覆盖式**写入 `ship.coeffMods`，多来源叠加；撤销路径＝条件失效 / 模块停用 / 携带者阵亡 / 移出场景。**时序**：Pass1 按当时的系数记账并开出伤害，系数变化在**本 tick 结算阶段**落地 → 因此**要到下一 tick 的激活才体现**在伤害上。条件型状态模块（`solo`）用本词条，条件满足/失效由引擎按标签判定。可扩展：新增词条名 → `coeff()` 类别的映射表（`battle.js` 内 `COEFF_ADD`）。 |
| `damage_coeff_mul` | **自身词条（★受击向）**：**受伤减免系数**——**乘法**降低**模块所属自身**单位**受到的**伤害，如 `0.95` = 只承受 95%（即减免 5%）。**不分类别、不经 `coeff()`**：存入独立的受伤减免表 `ship.damageTakeMulMods`（key＝模块实例 id，覆盖式；多来源**连乘** → `ship.damageTakeMul = Π`，缺省 1）。**唯一结算点**＝受伤入口 `battle.js applyHit(target, amount, …)`：在**任何吸收（防爆拦截 / 护盾池 / 共享同盟·防爆 / 舰体）之前**执行 `amount * damageTakeMul(target)` → 主目标命中、爆炸波及、反射返程等**所有来源**自动一并减免，出伤侧无需逐点相乘。**减免范围**：`applyHit` 的一切命中，以及**负值量值词条**（`hp_target` 扣血、`shield_gain_target` 削盾）；**正值为增益/治疗不减免**。**豁免（★）**：自毁 `self_destruct_damage`（自伤，走独立路径）、**能量削减**（`energy_target` 负值 / 抽能）、上限类 `*_cap_target`（控制效果，不是血/盾伤害）。**必须与 `duration_ticks` 搭配**（激活时记账 → 结算步骤 2 落地 → **下一 tick** 生效；到期/停用/阵亡/移出场景按来源撤销）。映射表：`DAMAGE_TAKE_MUL`。 |
| `attack_coeff_add_target` | **目标词条**（带 `_target` 后缀 → **遵循目标选择器**）：对**每个解析目标**的 `attack` 系数做**加性**修饰（提升被选中目标的伤害基数）。逐目标一条记录（`__pending.coeffOps` 的 `rec.ship` ＝被作用单位），结算步骤 2 落地，故同样**下一 tick 生效**；**必须与 `duration_ticks` 搭配**。映射表：`COEFF_ADD_T`。 |
| `damage_coeff_mul_target` | **目标词条（受击向）**：对**每个解析目标**做**受伤减免**修饰（如 `0.5` = 让被选中的目标只承受一半伤害），口径与 `damage_coeff_mul` **完全一致**（写被作用单位自己的 `damageTakeMulMods`，不分类别、不经 `coeff()`、豁免自毁/能量削减/上限类）。同上：逐目标一条、结算步骤 2 落地、下一 tick 生效、需 `duration_ticks`。映射表：`DAMAGE_TAKE_MUL_T`。 |

| `time_coeff` | **时间系数（加速/放缓统一词条，取代旧 `hasten_ticks`/`slow_ticks`）**：**负值＝加速、正值＝放缓**；语义＝**缩放“需求量”**（**每 tick 恒推进 1**）：`需求量 = timeScaled(基础量, 系数) = max(0, round(基础量 × (1 + 系数)))`、`剩余 = 需求量 − 已推进 tick 数`（整数 tick 取值）。作用于三处计时：① 模块**持续时间** `inst.durationLeft`；② 模块**冷却** `inst.cooldown`；③ 临时单位**存在时间** `u.tempLeft`。**整数 tick 语义、不经类别系数缩放**（需求量取整后每 tick 递减 1）。**多来源**：单位上按来源记账 `ship.timeCoeffMods: Map<来源key, 系数>` → 派生 `ship.timeCoeff`（缺省 0，撤销某来源后重算；唯一读口径 `ship.js timeCoeffOf(ship)`）。**★ 组合规则＝正、负两向各取一个「绝对值最大」的来源，再把两者相加**：同向多来源**不叠加**（−0.1 与 −0.2 → −0.2），异向各取最强后相抵（−0.2 与 +0.1 → −0.1）。**作用集合**＝目标选择器解析出的目标 ∪ `blast_range` 波及 ∪ `include_self`（**`type` 标签**）补入的自身（与上限类词条共用 `effectSetOf`；**标签补入的自身不产生溅射，选择器解析出的自身照常溅射**）。属**时长型修饰**：**必须搭配 `duration_ticks`**（离开持续期才有唯一撤销时机：到期/停用/阵亡/移出场景/重新激活前）；Pass1 只记账（`__pending.timeOps`），**结算步骤 2** 统一落地 → **下一 tick 起**生效（本 tick 的计时已按 tick 起始快照 `_timeCoeffTick` 重算完毕）。映射表：`TIME`。**战报低频**：仅“从无→有”记 `battle.log.hastenStart`、“从有→无”记 `battle.log.hastenEnd`（同 tick 到期并重新激活则整体静默；`n`＝真正生效单位数），**不逐次激活播报**。 |
| `hp_below_activate` | **低血触发门控**（比例 0~1，如 `0.2`；缺省/0＝无门控）：**仅当单位血量比例 ≤ 该值时可激活**（比例＝`hull.hp / hull.hpMax`，读 tick 起始值）。激活即给模块打**结构标记** `inst._firedOnce`，**血量回升到阈值以上时清除标记**才可再次激活 —— 即“**一个低血区间只触发一次**”。判定在 `maybeActivate()` **顶部**（先于冷却/持续期判定，故持续期内血量回升也能清标记）；Pass1 内血量恒定，与遍历位置无关、镜像对等；**只读写结构标记、零数值变化**。无此词条的模块行为不变。 |
| `delayed_trigger`（**type 标签**） | **后触发**（见 §2.5）：激活时**不产生效果**，只冻结「作用集合（复用 `effectSetOf`）+ 出伤数值（按激活瞬间系数折算）+ 主目标集合」；持续期**自然到期**那一刻由 `fireDelayedEffect` 按既有 `__pending.dmg` 格式记账（主目标 `splash:false`、波及 `splash:true` 带 gate），随后走既有 Phase B `settleHits` → `applyHit` → 战报链路（**无新伤害体系**）。**提前结束不触发**（停用/阵亡/移出场景/破盾等 `endDuration` 路径清空载荷）。需搭配 `duration_ticks`。 |
| `hp_cap_bonus` / `energy_cap_bonus` / `energy_regen_bonus` | **自身常驻静态加成**（与 `shield_cap_bonus` 同族，**无 `_target` 后缀＝自身词条**）：分别提升**自身血量上限**、**自身能量上限**、**自身能量恢复**（`energy_regen_bonus` 单位＝**每秒**，与船型 `base.energyRegen` 同口径）。**均乘模块类别系数**（与既有 `shield_cap_bonus` 经 `modulePoolCapOf` 的口径一致）。**常驻判据＝模块启用中 且 无 `duration_ticks`**（装上即生效，若搭配 `duration_ticks` 则退化为“仅持续期生效”，与 `shield_cap_bonus` 同一规则）。**唯一取值口径** `ship.js selfStaticBonus(ship,key)`；**唯一落地口径** `ship.js syncHullCaps(ship, overlayHp, overlayEnergy)`（`hpMax = baseHpMax + Σ词条 + 目标级叠加`、`energyRegenPerSec = baseEnergyRegen + Σ词条`，**非累加**），只在**结构变化**时重算（`installModule`／战斗内启停模块），**不逐 tick**。**提升上限只做 `min(当前, 上限)` 钳制、不白送当前值**（沿用 `shield_cap_bonus` 既有口径）；登场满血/满能量由 `fillHullVitals` 负责（与满盾 `fillShieldPools` 同体例）。**不产生战报**（无激活事件）。 |
| `shield_coeff_add` | **自身·类别系数加性**（护盾类别，与 `attack_coeff_add` 同族同表）：给**自身护盾系数**加性提升，作用于护盾池容量（`coeff(ship,'shield')` 同口径）。词条→类别映射表 `battle.js COEFF_ADD = { attack_coeff_add:'attack', shield_coeff_add:'shield' }`；走**常驻静态系数**路径 `ship.js syncStaticCoeffs()`（启用中、无 `duration_ticks`、**显式排除 `solo`**——条件型模块的加性由结算阶段逐 tick 写/撤同一张 `coeffMods` 表），写入既有 `coeffMods`（来源 key＝模块实例 id、撤销走 `clearAllSourceMods`），类别真变化后才 `recalcDerived`。 |
| `hp_regen_per_death` | **自身·按阵亡数回血**：`恢复量 = 词条值 × 上一 tick 全场（敌我合计）阵亡数 × 模块类别系数`。**阵亡计数口径**：唯一累加点＝判死唯一出口 `battle.js onDeath(ship)`，**排除临时单位**（判据 `!ship.summonMod && !ship.isSummon`，与 `solo` 的“召唤物不计入”同判据）；`step()` 收尾提交为 `lastTickDeaths`（`start()` 重置），**Pass1 只读该快照** ⇒ 同一 tick 内不存在“死→回血”反馈环、与遍历顺序无关、镜像对等。**回血落地**：Pass1 只记 `__pending.hpDeltas`（带 `regen:true`）→ **结算步骤 4c** 经 `applyHpTo` 真实回血（**钳到 `hpMax` 不超额、正值不乘受伤减免、不经护盾**）。**`0 阵亡` → 不记账、零数值变化**。低频战报：`battle.log.recycleRegen`（仅实际回血 > 0 时每 tick 每模块至多一条，带模块拥有者）。 |
| `time_coeff` 已统一承载加速与放缓两向：**正、负各取一个绝对值最大的来源，再求和**（同向不叠加、异向相抵）。

> **词条命名铁律（★）**：**无 `_target` 后缀＝自身词条**（作用对象＝模块所属单位：`attack_coeff_add`/`damage_coeff_mul`/`shield_gain`/`shield_cap_bonus`）；**带 `_target` 后缀＝目标词条**（作用对象＝`target` 选择器解析出的单位：`attack_coeff_add_target`/`damage_coeff_mul_target`/`shield_gain_target`/`shield_cap_target`/`hp_target`/`hp_cap_target`/`energy_target`/`energy_cap_target`）。负值抑制一律用 `_target` 词条，**勿对自身词条取负**。
> **受伤减免只有一个结算点（★）**：`damage_coeff_mul`/`damage_coeff_mul_target` 是**受击向**词条（改 `ship.damageTakeMul`，缺省 1），只在 `applyHit` **入口**按**受击方**乘一次；**不得**在出伤侧（`maybeActivate`/波及/反射记账）另行相乘，否则会重复减免。
> **四种叠加方式别混（★）**：① **类别系数加性**（`attack_coeff_add`…）→ 同类别**相加**（`ship.coeffMods`）；② **类别系数乘性**（预留，`ship.coeffMulMods`）→ 同类别**连乘**，当前无词条映射（**不再承载 `damage_coeff_mul`**）；③ **受伤减免**（`damage_coeff_mul`…）→ 各来源**连乘**（`ship.damageTakeMulMods`）；④ **时间加速/放缓**（`time_coeff`）→ 同一个词条的正负两向：各来源按 `ship.timeCoeffMods` 组合（**正、负各取绝对值最大者再求和**，同向不叠加）后**乘在需求量上**：`需求量 = max(0, round(基础量 × (1 + 时间系数)))`，每 tick 恒推进 1。各表彼此独立、互不影响。
> **“作用集合”只有一个实现（★）**：`blast_range` 波及 + `include_self`（**`type` 标签**）自身的集合扩展统一由 **`battle.js effectSetOf(ship, targets, fx)`** 计算（＝解析到的目标 ∪ 各目标**所在队列**视觉顺序前后各 N 个存活单位 ∪ `include_self` 标签补入的自身；按单位 id 去重、冻结于激活瞬间）。**上限类词条与 `time_coeff` 等“按集合成批落地”的词条组共用它**；新增同类词条**必须复用该 helper**，不得再写一遍扩展逻辑。（①波及用**目标自己那一侧**的队列：对“选中友方/任意”的词条才正确，对 EMP 这类“选中敌方”的词条二者等价、行为不变；②**自身是否溅射看来源**：溅射循环**只遍历 `targets`（选择器解析结果）**，故 `include_self` 标签补入的自身**不产生溅射**，而选择器正常解析出的自身（`prefer_self` 默认/手动选中自己）**照常有溅射** —— 无需任何 `primary === ship` 特判。）
> **修饰键与覆盖（★）**：各修饰都以**模块实例 id 为 key**写入被作用单位的对应表；同一 key 覆盖式（非累加），不同 key 之间按其固有方式合成（加性叠加 / 乘性连乘 / 加速取 max）。因此**同一模块不宜对同一单位同时用自身词条与 `_target` 词条修饰同一量**（同一 key 互相覆盖，以最后落地者为准）。`shield` 类别若被系数词条波及，`applyCoeffOp` 会顺带 `recalcDerived`（模块护盾池容量依赖 `coeff(ship,'shield')`）；受伤减免与时间加速都与 `coeff()` 无关，无需重算派生值。撤销路径统一走 `clearAllSourceMods(单位, 模块实例 id)`（系数加性/乘性 + 受伤减免 + 时间加速一起回退）；**时间加速另需**按 `inst._hastenRefs`（结算阶段写入的作用集合）逐单位 `clearHastenMod` 回退，见 `releaseHasten(inst)`。

> **控制类/目标类机制不是词条（★）**：以下四者都写在 `effects.type` **标签**里（引擎按标签识别、不按模块 id 硬编码），登记见 §2.5：
> `force_target_self`（强制目标）、`include_self`（效果同时作用于自身）、`prefer_self`（优先自己）、`lock_target_on_activate`（激活后锁定目标）、`delayed_trigger`（**后触发**：激活时不出效果，**持续期到期结算那一刻**才产生效果）、`stealth`（**潜行**：激活后在结算阶段给目标单位加**结构标记**；被标记单位**不能成为主要攻击目标**，但**仍受 `blast_range` 溅射与伤害结算影响**，且**激活瞬间已锁定的目标豁免**）、`passive`（**常驻被动**：**跳过“激活—触发”流程**（同 `solo` 做法），无冷却/无持续/无耗能，装上即生效；UI 据该标签显示为「状态型」并走 `moduleEffective` 判据）。

### 2.5 特殊效果钩子
| 词条 | 类型 | 含义 |
|------|------|------|
| `type` | array\<string\> | **列表参数**，仅为特殊效果钩子的标记（如 `projectile`）；一个模块可有多个；未来新特殊效果据此接入，勿按它硬编码伤害/回复 |

> **持续型不是标签（★）**：**是否持续型一律由 `duration_ticks > 0` 判定**；`'duration'` **不是合法标签**，
> 不要再写进 `type` 数组（引擎内已无任何按 `'duration'` 标签分支的判定，写了也无效、只会造成歧义）。

已定义/保留的 `type` 标记：
- `projectile` / `beam`：仅作表现类标识（当前不触发额外逻辑）。
- `summon`：**召唤类模块**——激活时按其 `effects.summon` 配置补召一个临时单位。`effects.summon` 字段：
  - `type`：被召唤单位模板 id（`data/ships.js` 中模板；无人机统一用通用 `drone` 模板）；
  - `modules`：召唤单位携带的模组数组（数量**不受该单位 slots 上限**约束；未给 level 的模组沿用召唤模块等级）；
  - `attrs`（可选）：按船型模板结构对 `type` 模板做**全条目覆写**（缺省的条目沿用模板），键与 `data/ships.js` 单船一致：
    - `nameKey`：可选，为召唤单位显式指定名称词条；模块**未指定**时才把召唤单位显示名覆写为所属召唤模块 `nameKey`；
    - `base.{hp, shieldCap, energyCap, energyRegen}`：覆写基础三围；
    - `coefficients.{attack,shield,function,transport,mining,drone,...}`：覆写类别系数；
  - 召唤单位三围等属性可在模块 `levels` 中按级调整（`levels` 里重写 `summon` 即可）；
  - `lifespan_ticks`：临时单位存在时间(tick)，到期自动死亡；
  - `maxSummoned`：该阵营同类型单位“存活最大数”，达到后不再补召（保持待命，有空位即补）；
  - `temp`：召唤单位性质。缺省 `true`=临时单位——受 `lifespan_ticks` 存在时间约束、到期自动死亡，且**阵亡/到期后直接移出场景**（不保留灰色卡片）；`false`=普通单位——**无存在时间限制**（忽略 `lifespan_ticks`），持续作战至死亡，阵亡后**保留灰色卡片**（与普通舰一致）。
  - `projectile`（可选）：`true` = 召唤体属**弹体类单位**（火箭/导弹弹体），被打上标签后可被其它模块的 `target.exclude:['projectile']` 排除（发射器据此不以弹体为目标）。
  - 召唤单位**图标 = 所属召唤模块的 `icon`**；模块无 `icon` 时该单位降级显示 `▲`。
- `ramp_full`：**逐步伤害的满目标条件**——仅在“目标选择的所有槽位都有目标”（如 multi maxCount=2 时两目标都在）才逐击增伤；目标未满（如只命中 1/2）则不成长、伤害维持基础值。作用于 `ramp_per_hit` 的成长判定。
- `ramp_by_enemy_count`：**伤害随当前场上敌方存活数提升**（不随时间累积）——单发伤害 = `damage` + `ramp_per_hit` × 当前敌方存活数，有 `max_damage` 则封顶；敌方阵亡越多，单发越低（如密集弹幕）。作用于伤害结算的增量解释。
- `alliance`：**同盟护盾**——本模块的时长型大护盾（`shield_cap_bonus`+`duration_ticks`，施放者共享层）是一个**供整支友方共用**的共享盾池。友方任一单位**自身盾空、伤害将扣血**时，由友方各同盟护盾的共享层按**释放顺序**逐层吸收（层值扣在施放者盾池上）。指挥栏**单独一行**显示友方同盟护盾合计护盾条；施放者同盟层被吸收时其护盾条**深蓝闪烁**。叠层 / 破盾机制适用。
- `no_break`：**破盾后仍保持激活**——层被打空也不提前结束、不进入冷却，维持激活直到自然持续到期；通常与 `alliance` 搭配（共享池不因被抽干而提前失效）。
- `blast`：**爆炸型伤害**标记——带此钩子的模块造成的伤害属于“爆炸伤害”（如火箭爆炸/导弹爆炸），可被**防爆护盾**吸收；主目标命中若被防爆池吸收，则该爆炸的 `blast_range` 被抑制（不再波及相邻单位）。
- `blastproof`：**防爆护盾**标记——本模块是防爆型共享层：**只吸收爆炸型伤害**（有 `blast` 钩子的伤害），不吸收普通伤害；指挥栏单独一行列出防爆池合计。仅含 `blastproof`（不带 `alliance`）时＝仅单个单位的防爆护盾（非跨友方共享）。
- `solo`：**条件型自身增益**——**仅当“非召唤的友方存活单位”只有自己一个**时本模块生效（“单枪匹马”即此用法）。语义与落地：
  - 条件是“**状态**”而非“触发”：满足即生效、不再满足即失效，故通常**不配** `cooldown_ticks` / `energy_cost` / `duration_ticks`（无冷却、不耗能、不进持续期），也**不进“激活-触发”流程**（引擎对带 `solo` 的模块跳过 `maybeActivate`）；
  - 生效效果由加性词条给出（见 2.4 的 `attack_coeff_add`），作用于**自身单位**；
  - **判定在 Pass1 记账**（只记“期望生效状态”，读 tick 起始存活快照；本 tick 全部判死都在结算阶段，故 Pass1 内该计数稳定、与遍历位置无关）；**系数的加/减统一在 Pass2 结算阶段的“上限与系数”步骤落地**（与 `capOps` 同批、先于伤害结算，跨单位同类修改顺序一致），仅在**状态发生变化**时写入/撤销（幂等）；
  - 撤销路径与既有撤销一致：条件不再满足 / 模块停用 / 携带者阵亡 / 移出场景 → 系数回退基础值；
  - **计数口径：排除召唤物** —— 统计“本阵营**非召唤**存活单位数 ≤ 1（含自己）”。召唤物以 `summonMod`（`doSummon` 打的模块标记）或 `isSummon`（`spawnSummoned` 打的标记）识别，**临时单位与弹体类单位天然被覆盖** → 即“**自己召唤出单位不会让本效果失效**”。（与 `type` 无关，只认这两个标记。）
  - **UI 状态显示（唯一判据）**：引擎导出 `battle.moduleEffective(inst)` → `true`=生效中 / `false`=条件未满足 / `null`=非状态型（或战斗未进行中）。**UI 一律读它，禁止在 UI 里重算条件**（避免两套口径）；明细行的“是否生效”与芯片发光 class 都走该判据，而明细行的数值（如 `攻击系数 +0.2`）展示的是**该等级解析后的配置值**、不用运行期生效值——两者不混淆。
- `force_target_self`：**强制目标（控制机制）** —— 把 `target` 选择器**解析出的每个目标**拉入“强制攻击施放者”的**来源栈**（＝敌人被迫集火施放者）。**这是 `type` 标签，不是 effects 词条**（`type:['force_target_self']`；持续型另由 `duration_ticks` 表达，**不要**再写 `'duration'` 标签），引擎**按标签识别、不按模块 id 硬编码**；必须搭配 `duration_ticks`（持续期结束是唯一的自动撤销时机）。语义与落地：
  - **作用集合**：`moduleTargetList` 的结果（`kinds`/`countMode`/`exclude` 决定，激活瞬间冻结为 `inst._forcedRefs`）；**不含施放者自己**（`t !== ship` 过滤），`lockTargetId` 非空的单位（一次性火箭/导弹弹体）**跳过**——其目标在召唤时固定、永不可改。
  - **落地**：Pass1 激活时只记账（施放方 `__pending.forceOps = {inst, actor, targets}`），**结算步骤 2b** 统一落地（与上限/系数同批、跨单位顺序一致，先于能量/伤害）→ 效果从**下一 tick 的目标解析**起体现。
  - **目标优先级**：**激活锁定 > 强制目标 > 模块手动目标 > 优先自己 > 船 `targetId` > 全队策略**（见 §3 解析链）；`single` 直接命中该目标，`multi`/`all` 仅置首位。
  - **实现（★ 不改写 `targetId`）**：引擎**不再**写被强制单位的 `targetId`，而是在 `moduleTargetList` / `shipEffectiveTarget` 里优先读“强制来源栈栈顶”，因此玩家/既有选定的主要目标**原样保留**。
  - **有序强制来源栈（★ 多来源集火与回落链）**：每单位 `u.forceStack = [{instId, actorId, seq}]`，按**激活先后**排序（`seq` 单调递增）→ **栈顶＝最后激活且仍生效的来源**（后激活者优先被集火：C → B → A）。
    - 某来源**提前取消/到期/停用/阵亡/离场** → 从各被强制单位的栈中移除该来源：移除的是栈顶则**回落到下一个仍生效的来源**（C 消失 → B）；**栈空**则清空强制状态、按**正常优先级**重新解析（A）。
    - **不恢复“被强制前的快照”**：强制期间从不改写 `targetId`，故栈空后该单位按正常优先级解析（其中原本就有的 `targetId`/模块手动目标自然重新生效）——这与“确认目标后目标不自动改变”不冲突（那是**自动选择**的粘性规则，不针对强制）。
    - 同一来源重复激活 → 其旧条目先被移除再压栈顶（保持“后激活优先”）。
  - **单位标签**：`u.forcedTargetId`（栈顶来源的施放者单位 id）、`u.forcedBy`（栈顶来源的模块实例 id）、`u.forceStack`（有序栈本体）。**旧版 `u.forceSrcs`（Map）已废弃**。
  - **战报**（不刷屏）：施加记 1 条 `{actor} 的 {module} 生效：{n} 个目标被强制攻击它`（`battle.log.forceTarget`）；撤销**按“本次撤销事件”聚合**各 1 条 —— 有回落时 `battle.log.forceFallback`（{n} 个单位集火对象回落至仍生效来源）、栈空时 `battle.log.forceRelease`（{n} 个单位恢复按正常优先级选择目标），均只统计**栈顶发生变化**的单位。
  - **代价词条**：常与 `damage_coeff_mul`（自身受到伤害系数乘性降低）搭配形成取舍（“固若金汤”即此用法）。
- `include_self`：**效果同时施加于自身**（★ 由 effects 布尔词条**改为 `type` 标签**；登记于此）。引擎**按标签识别**、与目标选择器**无关**：即使 `kinds` 不含 `self`/`any`（如 EMP 的 `['enemy']`），带该标签也一定作用于自身。它在**共享的“作用集合” helper** `effectSetOf` 内并入（＝解析到的目标 ∪ `blast_range` 波及 ∪ 自身，按 id 去重），且**它补入的自身只精确作用于自己、不产生 `blast_range` 溅射**（溅射只从“解析到的目标”出发；若自身是被**选择器正常解析**出来的目标，则照常有溅射，见 §2.3）。电磁脉冲、时间扭曲即此用法。它**不改动“必须有目标才激活”**的语义（自身由该标签补入，而非来自目标池）。
- `prefer_self`：**优先自己** —— 在“自身**可作为目标**”（`kinds` 含 `self` 或 `any` → 自身在候选池中）的前提下，**默认解析目标时优先取自己**（优先级桶 rank 2：位于“模块手动目标”之后、“船 `targetId`”之前）；玩家**手动指定**其它目标时以手动为准（手动 rank 1 更高）。自身不在池中则本标签无效果。
- `lock_target_on_activate`：**激活后锁定目标** —— 模块**激活瞬间**把 `moduleTargetList` 的解析结果记为锁定集合 `inst._lockIds`（**只写结构引用、不产生数值变化**，符合 Pass1 零数值变化）；**本次持续期内**（`inst.durationLeft > 0`）目标**固定为该集合**，优先级**高于强制目标与手动目标**（仅次于“锁定单位 `lockTargetId`”）。玩家在持续期内点选的目标只被**记录**（`inst.target`），**下一次激活**才按正常链采用。锁定目标阵亡 → 只保留其余存活的锁定目标（**全部阵亡则返回空、绝不改选**）。持续期结束锁定自动失效（无需额外撤销）。**需搭配 `duration_ticks`**。UI 判据＝引擎导出 `battle.moduleTargetLocked(inst)`（据它显示「已锁定 / 新选择于下次激活生效」）。

> 语义注意：**无 `_target` 后缀的词条＝自身词条**（`shield_gain`/`shield_cap_bonus`/`attack_coeff_add`/`damage_coeff_mul`）——**只作用于模块所属自身**；要作用到目标必须用带 `_target` 的目标词条（`shield_gain_target`/`attack_coeff_add_target`/`damage_coeff_mul_target`/…）。负值抑制必须用 `_target` 词条，**勿对自身词条取负**。

## 3. target 目标词条

| 字段 | 值 | 含义 |
|------|----|------|
| `kinds` | array | 可用目标：`self`(自身) / `ally`(同阵营其它存活) / `enemy`(敌方存活) / `any`(敌我任意含自身)；目标池=对应存活单位按全队策略排序 |
| `countMode` | `single` / `multi` / `all` | `single`=单体；`multi`=有限多目标(用 `maxCount`)；`all`=全队命中所有符合条件的存活目标 |
| `maxCount` | number | `countMode='multi'` 时的目标数上限（`single`/`all` 忽略） |
| `exclude` | array | **目标排除标签**：从目标池中剔除带该标签的单位。当前标签：`projectile`＝召唤弹体类单位（火箭/导弹弹体，由召唤配置 `summon.projectile:true` 标记）——发射器用 `exclude:['projectile']` 避免以弹体为目标 |

> 解析链（单模块目标优先级，★ 新顺序）：**激活锁定目标（`lock_target_on_activate` 标签 · 持续期内） > 强制目标（`force_target_self` 来源栈栈顶） > 模块手动目标（`target.mode:'unit'/'units'`） > 优先自己（`prefer_self` 标签 · 自身在池中时） > 所属船 `targetId` > 自动粘性（上一轮已选中的存活目标） > 指挥栏全队策略（order/最低血量/最低护盾）**。另：**锁定单位（`lockTargetId`，一次性火箭/导弹弹体）最先直接返回、永不被夺**。空位由上游自动补足；无可用目标则不激活；手动指定目标阵亡自动回落并写战报。★ **潜行过滤（`stealth` 标签）**：被标记潜行的单位**不作为主要攻击目标**——唯一判据 `battle.js stealthBlocksTargeting(ship,u)` 同时落在上方链（模块侧 `moduleTargetList`：**锁定分支之后**过滤候选池，故强制/手动/优先自己/船 `targetId`/粘性/策略各桶都被跳过）与船级 `shipEffectiveTarget`（强制、船 `targetId`、策略队首均跳过，`targetId` 不被改写、潜行结束后自然恢复）；**豁免＝激活瞬间已锁定的目标**（`lockTargetId` 锁定单位、`lock_target_on_activate` 的 `_lockIds` 集合，两分支均在过滤之前直接返回）；**自身永远可选**；**溅射不受影响**（`effectSetOf` 的 `blast_range` 波及与 `applyHit` 完全不走该判据，无任何潜行减免）；因此无可用目标时按既有规则不激活、不报错。
> · **实现**：`moduleTargetList` 对候选池按上述优先级打**桶**（rank 0…5）后一次稳定排序，`all`/`multi`/`single` 三个分支同取这一条链（`all` 取全部、`multi` 取前 N、`single` 取首位）；`single` 与 `multi` 的旧行为与之等价（仅 `all` 在“同时存在 `ship.targetId` 与自动粘性”时，粘性不再抢到 `targetId` 之前——与优先级链一致）。
> · **手动目标的存活校验按全场单位判定**（★ 曾经的 BUG）：模块手动目标的范围由该模块 `target.kinds` 决定，**可以是自身/友军/任意单位**，故 `clearShipDeadRefs` 用 `aliveUnitId`（全场查找）而不是“敌方队列”来判存活；只有**船级 `targetId`** 仍按敌方队列校验（它只可能选敌）。
> · **`countMode:'single'`**：优先级最高者**直接命中**（激活锁定/强制目标夺走模块手动单目标）；**`multi`/`all`**：这些高优先级目标仅占**前位**，不缩减 AoE/多目标覆盖。
> · 被强制单位的 `targetId` **不被改写**（见 §2.5 的 `force_target_self`）：强制只影响本次解析，全部来源失效后自然回到上述正常优先级。

## 4. levels 与等级语义

- 模块文件里 `effects`/`target` 即 **Lv1**（基底）。
- `levels[]` 是**逐级绝对表**，只写“该等级与上一级的差异”，例如：
  ```js
  levels: [
    { level: 2, effects: { damage: 20 } },                 // Lv2 仅覆盖 damage
    { level: 3, target: { kinds: ['enemy','ally'] } },     // Lv3 覆盖 target
  ],
  ```
- **未填的字段回退“上一级”**（递归向上，最终以 Lv1/基底兜底）——高阶等级不必重复全部字段。
- 机制类字段（`type` 钩子、`target` kinds 等）**允许随等级变**（在某级覆盖即可）。
- 实例默认等级 = 1；引擎按实例等级把 base 与各级差异合并成该级可用的 `cfg`（`resolveModuleCfg`）。
- “升级(耗资源)”操作在 M3/M4 船坞实现；M2 只落地数据级模型 + 等级生效 + 演练开战可选等级。

## 5. 命名与数据约定
- effects 词条 / 后续通用字段一律下划线命名。
- 新增模块 = 四处联动：模块数据文件(含 levels) + 引擎(如需新词条/钩子) + i18n 词条 + UI 呈现。
- 数值一律进 `data/`，禁止散落硬编码。
