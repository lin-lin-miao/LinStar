# LinStar 开发步骤与开发清单

> 版本：v0.1（规划版）｜配套源文件：`游戏实现大纲.txt`
> 状态标记：`☐ 未开始`　`✍ 进行中`　`✔ 完成`
> 本文档即项目的"活清单"，随开发持续更新；每个里程碑的验收以本文档勾选为准。

---

## 1. 项目概述

LinStar：太空背景的增量（incremental）+ 回合式战斗网页游戏。

- 纯静态网页，可整体部署到 GitHub Pages（github.io）。
- 所有资源/脚本引用**必须使用相对路径**。
- 功能模块尽量**各功能独立成文件**，职责单一、低耦合。
- 存档支持 **本地保存 / 导出 / 导入**。
- 界面文本支持**多语言替换框架**（当前仅完成中文，其余语言留接口）。

### 1.1 已确认决策（2025 本会话确认）

| # | 决策 | 内容 |
|---|------|------|
| D1 | 技术栈 | 原生 HTML + CSS + JavaScript（ES Modules，相对导入），无构建工具、无运行时依赖 |
| D2 | 第一阶段范围 | 仅做「对战 Demo」：对战场景 + 战斗舰 + 火炮模块 + 再生护盾模块（不含主基地内容） |
| D3 | 采矿船系数（大纲笔误修正） | 采矿船：对**采矿类**模块系数 = 1，对**攻击类 / 运输类** = 0.5（护盾/功能见 Q1） |
| D4 | 测试方式 | 全程人工测试；需要测试时由 AI 先给出《测试期望》与《测试方案》，经确认后再测 |

### 1.2 术语表

| 术语 | 含义 |
|------|------|
| tick | 游戏刻，最小时间单位。默认 1 秒 = 20 tick（同《我的世界》） |
| 船类系数 | 船只对某类模块效果的加成乘数（见 3.2 船舰表） |
| 模块槽 | 1 级船可安装模块数 = 3（各类船一致） |
| 行（lane） | 战斗场景中从上到下的分区：敌方单位 → 分隔线 → 我方战斗单位 → 我方后勤单位 → 技能 UI |

---

## 2. 架构与文件规划

### 2.1 目录结构（规划）

```
LinStar/
├─ index.html                 # 唯一入口页
├─ assets/                    # 独立素材目录（SVG 图标等一律放这里，禁止内联散落）
│  └─ img/                    # ship-<船型>-<阵营>.svg，如 ship-combat-ally.svg / ship-combat-enemy.svg
├─ css/
│  ├─ base.css                # 全局样式/变量/布局
│  ├─ battle.css              # 战斗场景样式
│  ├─ base-ui.css             # 主基地界面样式
│  └─ screens.css             # 菜单/通用面板
├─ js/
│  ├─ main.js                 # 启动引导：初始化各系统、接管页面
│  ├─ core/
│  │  ├─ tick.js              # 游戏刻主循环（20tps、暂停/倍速）
│  │  ├─ eventBus.js          # 事件总线（系统解耦）
│  │  ├─ utils.js             # 工具函数（纯函数）
│  │  ├─ rng.js               # 随机数（可播种，便于复现测试）
│  │  └─ log.js               # 战斗/事件日志
│  ├─ entities/
│  │  ├─ ship.js              # 船实体（HP/护盾/能量/模块实例/系数）
│  │  ├─ module.js            # 模块实例（配置引用 + 运行状态）
│  │  └─ enemy.js             # 敌人实体（可复用 ship 逻辑）
│  ├─ data/
│  │  ├─ ships.js             # 船型静态配置
│  │  ├─ modules.js           # 模块静态配置总表（所有分类）
│  │  ├─ enemies.js           # 敌人/区域编队配置
│  │  └─ areas.js             # 星区等级配置（M5 起使用）
│  ├─ systems/
│  │  ├─ battle.js            # 战斗系统核心（tick 结算/行动/AI）
│  │  ├─ energy.js            # 能量结算（并入 battle 或独立，看实现）
│  │  ├─ save.js              # 存档：序列化/本地保存/导出/导入/版本迁移
│  │  ├─ fleet.js             # 我方舰队（基地侧，M3 起）
│  │  ├─ base.js              # 主基地状态（M3 起）
│  │  ├─ stargate.js          # 星门/区域进出（M3 起）
│  │  ├─ research.js          # 研究站（M4 起）
│  │  ├─ shipyard.js          # 船坞建造/改装/升级（M3 起）
│  │  ├─ planet.js            # 星球产业（M4 起）
│  │  └─ economy.js           # 资源与产量结算（M4 起）
│  ├─ i18n/
│  │  ├─ index.js             # 词条查询 i18n.t(key)，语言切换
│  │  ├─ zh-CN.js             # 中文词条（当前唯一完整语言）
│  │  └─ en.js                # 英文骨架（占位，M6 补齐）
│  └─ ui/
│     ├─ router.js            # 屏幕切换（菜单/战斗/主基地…）
│     ├─ hud.js               # 顶部信息条（tick/资源/按钮）
│     ├─ battleView.js        # 战斗场景渲染与交互
│     ├─ menuView.js          # 主菜单
│     ├─ baseView.js          # 主基地视图（M3 起）
│     └─ widgets.js           # 通用 UI 组件（进度条/弹窗/按钮）
├─ README.md                  # 项目说明 + 部署说明
└─ 开发步骤与开发清单.md        # 本文档
```

> 原则：目录按"职责"而非"页面"组织；`entities` 只描述状态与规则，`systems` 推进状态，`ui` 只做渲染与输入，`data` 只放静态配置，i18n 词条只进 `i18n/`。

### 2.2 核心数据流（设计约定）

- 单一全局状态对象 `state`（base / fleet / battle / settings / locale），系统按 tick 推进；
- UI 订阅事件或每帧读取渲染（增量渲染优化留到 M6）；
- 逻辑层不碰 DOM，UI 层不写规则；
- 数值在 `data/` 静态配置中集中定义，运行中只读 + 存档覆盖层（便于后续平衡）。

### 2.3 i18n 设计要点（随 M0 搭建）

- 所有可见文本走 `i18n.t('key', params)`；字符串永不硬编码进逻辑/数据。
- 词条文件为 `{ key: string }`；无当前语言词条时回退 zh-CN。
- 当前语言存于存档 settings.locale。
- 模块/船只名称、描述都是词条 key。

### 2.4 存档设计要点（随 M0 搭建）

- 存档 = 单个 JSON 对象，含 `schemaVersion` 字段，后续结构变更走版本迁移函数。
- 本地保存：localStorage（键名如 `linsar.save.v1`）+ 定时/退出自动保存；战斗进行中不写档（Q9）。
- 导出：生成 JSON 文件下载（Blob + a 标签下载）。
- 导入：文件选择 → JSON 解析校验 → 版本迁移 → 替换当前存档并重载。
- 本地直接双击打开（file://）时 localStorage 行为因浏览器而异 → 测试建议用本地静态服务器（见 8.4）。

---

## 3. 核心规则与数值（初稿，M1 实现依据）

> 以下数值均为**占位初值**，用于做出可玩 Demo；最终平衡在 M6 统一调整。

### 3.1 tick 与计时

- 1 秒 = 20 tick；内部以 tick 驱动，涉及秒的换算一律 1s=20tick。
- 战斗可暂停 / 倍速（1x、2x）便于人工测试。
- 能量回复等按 tick 结算，内部用浮点累计、显示取整。

### 3.2 船舰（1 级，三类）

| 船类 | 攻击类 | 护盾类 | 功能类 | 运输类 | 采矿类 | 模块槽 | 说明 |
|------|:---:|:---:|:---:|:---:|:---:|:---:|------|
| 战斗舰 | 1 | 1 | 1 | 0.5 | 0.5 | 3 | 主要战斗单位 |
| 运输舰 | 0.5 | 1 | 1 | 1 | 0.5 | 3 | 主要运输单位 |
| 采矿船 | 0.5 | 1 | 1 | 0.5 | 1 | 3 | 主要采矿单位（D3 修正；护盾/功能见 Q1） |

统一基础属性（1 级，占位）：血量 120 ／ 护盾上限 40 ／ 能量上限 100 ／ 基础能量回复 5/秒。
说明：护盾先于血量承伤；无模块时护盾不自动回复。系数的作用对象见 Q3。

### 3.3 Demo 首批两个模块规格（占位数值）

**火炮（攻击类）**
- 效果：对敌方单位造成伤害；
- 伤害 14 / 发，冷却 20 tick（1 秒），能耗 6 / 发；
- 目标选择：最近的存活敌方单位（Q4）。

**再生护盾（护盾类）**
- 效果：提升护盾上限 +40，并开启持续回复；
- 回复速率 4 护盾/秒，能耗 1 能量/秒（按 20tps 折算）；
- 能量不足时暂停回复，能量恢复后自动继续。

### 3.4 战斗场景布局（M1 起实现）

自上而下分区（HTML/CSS 按行实现，空行保留占位）：

```
┌───────────────────────────────┐
│  敌方单位行（敌人船卡）          │
│  ──── 分隔线 ────             │
│  我方战斗单位行                  │
│  我方后勤单位行（运输/采矿，M2 起有内容）│
│  技能 UI 行（功能类技能，M2 起） │
└───────────────────────────────┘
```

船卡显示：名称、HP 条、护盾条、能量条、已装模块图标列表。

### 3.5 战斗 tick 结算顺序（定稿 · 已实现）

> 已实现于 `systems/battle.js` 的 `step()`。核心范式：**行动阶段只记账、结算阶段统一落地**（数值先进目标的挂账缓冲 `__pending`，全部模块激活后才结算，结算完再判死）。
> **Pass1 零数值变化（硬约束）**：Pass1 只允许写「模块私有计时器 / 闪标 / 结构引用 / 记账（`__pending`）」；一切**可见数值改动**——能量回充与消耗、护盾与护盾池、上限修改及其恢复、系数修饰、受伤减免、时间加速、强制目标入栈、临时单位寿命、以及伤害本身——都只记意图，统一下沉到**结算阶段按固定全局顺序**落地（跨单位一致），从而同 tick 双方不会因队列位置不同吃到不同规则、镜像局面严格对等。
> 每 tick 对「全体单位」的遍历固定为 **3 遍**：Pass1 单遍、Phase A 单遍、Phase C 单遍；其余皆为“相内子步”（只迭代命中条目/模块），不随舰队规模扩张。

1. **Pass1 行动（单遍单位 for，读 tick 起始快照）**：闪标递减 → 清理自身指向已死目标的引用(`clearShipDeadRefs`) → 能量回充记账 → 模块结构推进(时长/冷却/到期撤销/窗口累计) → 模块激活**只记账**(`maybeActivate`：伤害/补盾/能量/上限叠加/系数修饰/受伤减免/时间加速/强制目标写入 `__pending`；召唤为结构性立即生成，本 tick 不行动) → 临时单位存在时间记账。
   - 挂账 `__pending` 必须在任何记账**之前**为全体存活单位一次性建好（否则后处理单位会清掉先处理单位记到它身上的伤害）。
   - 行动所读的挂账量（能量门控、系数、受伤减免、时间加速推进量）一律取 **tick 起始快照**（如 `_takeMulTick`/`_hastenTick`）→ 本 tick 落地的改动**下一 tick** 才体现。
2. **Phase A（单遍单位 for，只收集意图）→ 结算固定顺序落地**：对本 tick 全体（含新召/已死者）把各单位 `__pending` 上的意图并入记录数组（伤害命中 / 上限 / 到期撤销 / 系数·受伤减免 / 时间加速 / 强制目标 / 能量 / 非伤害数值 / 护盾池填充 / 临时寿命），随后按**固定全局顺序**落地（跨单位一致）：
   **① 计时推进 → ② 上限修改与恢复 + 系数修饰 + 受伤减免 + 时间加速（含到期撤销）→ ②b 强制目标来源栈 → ③ 能量（回充 → 模块消耗 → `energy_target` 量值）→ ④ 护盾/模块池填充/血量/自毁/临时寿命**（④ 内固定为 池填充 → 补·汲取盾 → 血量 → 自毁 → 临时寿命判死）。
   - ② 与 ②b **必须先于**伤害结算：Pass2 承伤要读到最新的护盾池/上限与最新减免；只改目标指向的 ②b 从**下一 tick 的目标解析**起体现。
   - 伤害记账挂在“被命中方”，攻击方同 tick 已死也照常结算。
3. **Phase B（相内子步）**：主目标先结算（确定爆炸的防爆抑制）→ 后爆炸波及；`applyHit` 内护盾吸收顺序：爆炸先友方防爆拦截 → 目标自身时长盾池(激活序，先激活先用) → 舰载护盾(基地池) → 共享同盟/防爆 → 舰体(hp)。**破盾就地结算**：抽空某时长盾池即结束其持续、删池降 cap（`no_break` 型则仅标“已耗尽”不再计入 cap，持续期照走）。
4. **Phase B2（相内子步）**：反射返程统一在本 tick 武器/爆炸全部结算完后补打回（`noReflect` 不二次反射）。
5. **Phase C（单遍单位 for 收尾）**：模块统计写回(`finalizeModule`) → 清 `__pending` → 收集并移出阵亡/到期的临时单位。
6. **判死与胜负**：全部数值结算完再检查死亡（`onDeath` 就地撤销死者残留效果/cap）；双方同 tick 同归 → 平局(draw)。
   - 开战/召唤即满盾就位（首个可行动 tick 起自身时长盾即为满池）。
   - 显示顺序：命中句先于“被击毁”句；反射句先于其导致的被击毁。

---

## 4. 里程碑总览

| 里程碑 | 名称 | 范围要点 | 完成判定（可玩到） |
|:---:|------|----------|------------------|
| M0 | 脚手架与基础框架 | 目录、入口、tick 循环、屏幕路由、i18n 框架、存档框架（含导出/导入）、调试控制台 | 空页面外壳可运行：显示 tick 在走、切屏、切换语言不回退、导出/导入空档成功 |
| M1 | 对战 Demo | 战斗舰、火炮、再生护盾、对战行布局、1v1 对战与胜负 | 进入战斗 → 我方一战斗舰（火炮+再生护盾）击败/被击败敌方一战斗舰 → 结算重开 |
| M2 | 战斗系统扩展 | 其余模块实现、后勤行单位入场、功能类技能行、多单位、buff/日志/结算面板 | 可配置多船多模块编队进行一场较完整战斗 |
| M3 | 主基地框架 | 主基地界面、星门/指挥中心/船坞/研究站/星球入口与基本流程、编队进出战斗 | 基地 → 船坞配船 → 星门开战 → 回基地，状态全程可存档 |
| M4 | 经济与养成循环 | 运输/采矿玩法、资源产出、星球产业、研究站分析、船升级 | 形成"战斗/采集→资源→研究→更强编队"闭环 |
| M5 | 区域与敌人扩张 | 多等级星区、敌人编队配置、难度曲线 | 星门可在多个等级区域选择并挑战 |
| M6 | 打磨与发布 | 数值平衡、英文词条、性能、GitHub Pages 部署验证、README | 可在 github.io 直接游玩，存档可迁移 |

> 依赖：M0 → M1 顺序完成；M2 依赖 M1 的战斗框架；M3 依赖 M2 的战斗可被"外部编队"驱动（或先并行做界面）；M4 依赖 M3；M5/M6 依赖 M4。

---

## 5. 详细开发步骤（按里程碑）

### M0 脚手架与基础框架

| 步骤 | 内容 | 产出/验收 |
|:---:|------|----------|
| M0.1 | 建立目录结构（按 2.1），全部文件用相对路径引用；git init 与提交由开发者（用户）人工执行 | 双击 index.html 或经静态服务器打开无 404；git 仓库由用户管理 |
| M0.2 | index.html 骨架 + 通用 CSS（布局变量、字体、深色太空主题占位） | 页面显示标题「LinStar」占位 |
| M0.3 | `core/tick.js`：tick 主循环（20tps 累加器），暴露 tick 计数，支持暂停/1x/2x | 调试面板显示 tick 数递增，倍速生效 |
| M0.4 | `core/eventBus.js` + `core/log.js`：事件发布订阅与日志队列 | 代码级可验证（控制台测试） |
| M0.5 | `i18n/`：zh-CN 词条 + `i18n.t()` + 语言切换（默认中文）；菜单按钮文本走词条 | 切换语言（仅 zh-CN/en 占位）后按钮文本变化 |
| M0.6 | `ui/router.js` + `menuView.js`：菜单屏（开始游戏/导入存档）+ 占位战斗屏 | 按钮可切到空战斗屏并返回 |
| M0.7 | `systems/save.js`：单例 JSON 存档、localStorage 自动保存、导出下载、导入读取（含校验与 schemaVersion） | 导出文件可再导入，空档往返一致 |
| M0.8 | 调试辅助：`window.LS` 控制台对象（查看状态、造单位、跳转） | 控制台可调 LS 修改状态并刷新 UI |

**M0 验收（人工测试 #0）**：见 8.1。

> **M0 状态：✔ 已通过人工测试 #0（1~7 项正常）；收尾修正（tick/s 显示、调速按钮仅战斗中显示、战斗中禁存档→结算后保存）已实现待复查。git 由用户人工提交。**

### M1 对战 Demo（首个可玩版本）

| 步骤 | 内容 | 产出/验收 |
|:---:|------|----------|
| M1.1 | `data/ships.js`：战斗舰配置（3.2 属性）；`entities/ship.js`：船实体（HP/护盾/能量/模块实例/船类系数） | 实体工厂可产出战斗舰，数值符合配置 |
| M1.2 | `data/modules.js` 表结构 + `entities/module.js`：模块实例（配置 + 冷却/能耗状态）；先注册 火炮、再生护盾 | 模块可被安装到船，显示在船卡上 |
| M1.3 | `systems/battle.js`：战斗状态机（准备→进行→胜负结算）、按 3.5 顺序做 tick 结算；能量系统并入 | 战斗可推进，能量按规则增减 |
| M1.4 | `ui/battleView.js`：按 3.4 实现五区布局 + 船卡（名称/HP/护盾/能量条/模块图标）+ tick 与状态 HUD；我方一船（火炮+再生护盾）vs 敌方一船（火炮）预设 | 页面呈现两船卡与完整行结构 |
| M1.5 | 敌方 AI：冷却就绪且能量足够即开火（目标最近敌方） | 敌方按 1 秒节奏开火 |
| M1.6 | 伤害/护盾结算、死亡判定、胜负弹窗（胜/败 + 重新开始/返回菜单） | 一方 HP 归零后正确结算 |
| M1.7 | 将战斗接入菜单「开始对战」；暂停/倍速按钮可用 | 全流程：菜单→战斗→结算→再来 |
| M1.8 | 把本 Demo 战斗所有文本收进 i18n；存档中记录基础设定（不记录对战中状态，Q9） | 全程无硬编码可见文本 |
| M1.9 | 战斗场景视觉与交互增强：场景搭建；SVG 舰船图标（战斗舰=正三角形，我方蓝/敌方红，素材独立放 assets/img/）；单位实体矩形卡（自上而下：单位图标→状态条→简约模块图标）；模块冷却倒计时徽标与就绪/生效提示；单位"下一步行动"提示（敌方即将做什么）；点击单位→场景下方详情面板（数值/模块状态/能耗）；可为己方单位指定主要攻击目标并实时生效 | 选中单位详情正确刷新、目标切换影响实际开火 |
| M1.9b | 模块交互与结算收尾：详情面板可停用/启用己方模块（停用后不结算、不耗能、冷却冻结）；每个模块行加齿轮按钮 = 模块 AI 策略预留入口（点击仅战报占位提示）；战斗结束改为顶部非阻挡横幅——仍可浏览战斗场景与战报，各单位数值冻结（等同暂停），模块/目标操作锁定 | 停用模块后其不再行动且能量不再扣减；结算后数值定格、场景可浏览、操作锁定 |

**M1 验收（人工测试 #1）**：见 8.2。

> **M1 状态：✔ 已完成整场回归测试并收尾（含 M1.9 全批交互增强与演练沙盘改造），M1 里程碑关闭，进入 M2。git 由用户人工提交。**

### M2 战斗系统扩展

| 步骤 | 内容 | 产出/验收 |
|:---:|------|----------|
| M2.1 | 补齐**攻击类**模块（见 6.2 清单 C 项）并逐一定制数值草稿、实现效果；**召唤框架已提前建立**（见进度日志）供火箭发射器等"召唤一次性单位"型攻击模块即用 | 每种攻击模块可安装并产生差异效果 |
| M2.2 | 补齐**护盾类**模块（除再生护盾外 6 种），实现不同机制（反射/防爆/同盟等需要事件钩子：受击、友方承伤等） | 每类机制在测试场景可见效 |
| M2.3 | 引入**无人机类**：无人机作为"生成物"出现在战斗行内，随战斗结算行动 | 无人机行/单位出现在场上并可被攻击或自毁 |
| M2.4 | **功能类**模块框架：区分主动技能（技能 UI 行，可点按、耗能）与被动/条件触发；实现首批 1-2 个（如 反应堆增幅/单枪匹马）验证框架（Q7） | 技能行出现按钮，点击触发，被动生效 |
| M2.5 | **后勤行**实装：运输舰/采矿船入场（系数与槽位按 3.2），先做"在场即提供增益/产出"的最小规则 | 后勤单位出现在第 4 行并产生占位效果 |
| M2.6 | 多单位编队：战斗支持多船多行敌方；编队由配置（M1 内写死）改为 `data/enemies.js` 场景选择 | 可切换 1v1 / 2v2 等预设场景 |
| M2.7 | buff/debuff 与效果结算优先级表；战斗日志（`log.js`）输出到战斗下方区域 | 关键事件（开火/击杀/盾破）有日志 |
| M2.8 | 战斗结算面板：耗时（tick）、双方战损、奖励占位（掉落数据留空，M4 接资源） | 结算面板数据正确 |

**M2 验收（人工测试 #2）**：见 8.3。

### M3 主基地框架

> ★ **M3 口径已于开发前定稿**：细则见 **`主基地框架说明.md`**（资源体系 / 界面形态 / 各建筑细则 / **出征与返回闭环** / 数据文件 / 占位数值 / 拆分）。下表为**按该文档拆分后的开发步骤**。

| 步骤 | 内容 | 产出/验收 |
|:---:|------|----------|
| M3a ✅ | 路由 `base` + `ui/baseView.js` 骨架（**左列表 5 项 + 右面板 + 顶部资源栏**）+ `systems/base.js`（基地状态、**五资源账户与消耗校验**）+ `data/resources.js`（资源注册表）+ `data/baseBuildings/*`（一建筑一文件）+ `data/baseConfig.js`（**只放初始状态**：初始额度与**容量上限**）；**主菜单收敛为仅「主基地」**（星域配置/编队屏改为**仅控制台**，见 `调试与特殊命令.md`） | ✅ 实装：主菜单进基地、资源显示（悬浮"当前 / 上限"）、不足禁灰 + 原因；自检 8 项 |
| M3b ✅ | **船坞（舰队并入）**：**抽象"单位配置 + 数量"**（`fleetConfigs[]`，同配置合并、手动命名 + `#N`、**已建造不可改 / 编辑＝另存**、删除限 `count=out=0`、**顺序＝数组顺序 + `(↑ ⠿ ↓)` 把手拖动排序**）；**模块可重复 + 各自等级**；**两级弹窗**（配置编辑 + 模块选择：分类筛选/图标列表/等级/**装配与拆下费用**，按钮＝确认·新增·移除·取消）；**建造**（可选等级；造价＝船型逐级累计 + Σ模块该等级 `installCost`；容量＝**船坞 `fleetCapacity`**）；**拆解**（返还＝`floor(花费×n×船坞 `scrapRefundRatio`)`，仅拆未出征部分）；列表**仅资源图标+数量**、`N 艘 \| M 在外`、可换行、造价/返还右对齐、左编辑·删除 / 右建造·拆解、**灰按钮 + 短语原因**；**数值全进配置文件**（战斗舰建造/升级费用、火炮逐级费用已落位【占位预填 · 待用户调校】） | ✅ 实装：自检 **28 项**（㉑–㉘：重复模块与等级、弹窗数据源、默认名 i18n、改名合并、排序往返、资源配色、模块造价同源、槽位三动作） |
| M3c | **指挥中心**：升级（**消耗读基地建筑配置文件**）+ **出战上限**（`levels[].effect.maxFleet`，**与船坞容量独立、互不换算**）；舰队**出征登记**（`LS.base.fleet.setOut`，**数量保留 + 另记在外数**） | 指挥中心升级后出战上限提升；配置表显示 `N 艘 \| M 在外` |
| M3d | **星门 + 返回闭环**：战区（难度档）选择、每次出征**随机"星域编号"**（**界面不出现"种子"字样、不提供输入**）、出征装配（**基地舰队实例 ⇒ 星域 `playerUnits`**）、进入星域；**侧栏「返回基地」按钮**（仅单位位于**星门星区**时可用）、**星域结束自动返回**（仍在星门星区存活者）、**回基地即修复**（满血满盾、模块配置保留）、携带矿物/货物转换入库、未返回 ⇒ `lost` 需重建；★ `starfieldConfigView` 入口**只留控制台函数** `LS.ui.openStarfieldConfig()` | 基地造的船能进星域；在星门星区返回后回舰队且满状态；未返回则需重建 |
| M3e | **研究站实装**：返回**货物全部按类型+等级堆叠入库**、**一次研究一个货物**（研究位 1，未来可升级）、研究＝**消耗货物 + 能量币 + 耗时（均读货物配置文件）**⇒ 产出**一定数量的对应种类蓝图**（数量由货物等级+加成决定，规则读货物配置）；**蓝图＝门槛（数量达标即解锁/升级，不消耗，门槛值读模块配置）**、**科研点＝消耗（读模块配置）**；在船坞生效；★ **科研点当前无产出（未来由玩法生产）** | 研究一个货物 ⇒ 蓝图达标 ⇒ 船坞可安装/升级该模块 |
| M3f | **导航闭环 + 存档扩展**：任意屏 ↔ 基地 ↔ 菜单、战斗/星域结束回基地、基地/舰队/研究/蓝图/进行中研究全部入档与恢复（`schemaVersion` 预留） | 关页重开状态一致（含进行中的研究） |

> **M3 明确不做**：星球产业（入口**占位不可用**）、基地自动产出、完整科技树、船只改名/退役、多研究位。


**M3 验收（人工测试 #3）**：见 8.4。

### M4 经济与养成循环

| 步骤 | 内容 | 产出/验收 |
|:---:|------|----------|
| M4.1 | 资源体系落地：金属 / 氢气等（占位名）资源项、产量计算、UI 顶部资源条 | 资源随设施/船活动增减 |
| M4.2 | `systems/planet.js`：星球产业——建造/升级产业建筑并产出资源（增量核心） | 产业按 tick 产出并可升级 |
| M4.3 | 运输舰玩法：离线/在线运输任务或在场增益转资源（按你的设定，先给最小闭环） | 运输行为产生可感知收益 |
| M4.4 | 采矿船玩法：采矿产出接入资源系统 | 采矿行为产生资源 |
| M4.5 | 战斗掉落正式化：胜利获得材料/物品进背包 | 打完一场有掉落列表 |
| M4.6 | 研究站实装：物品分析 → 解锁科技树条目（解锁模块/船型/产业效率），科技树配置化 | 分析后可解锁至少一项可见内容 |
| M4.7 | 船坞消费资源；升级与新建成本曲线初值 | 经济负反馈循环可玩 |

**M4 验收（人工测试 #4）**：见 8.5。

### M5 区域与敌人扩张

| 步骤 | 内容 | 产出/验收 |
|:---:|------|----------|
| M5.1 | `data/areas.js` 区域/等级定义：解锁条件（指挥中心/战损/科技）、区域主题占位 | 星门可列出多个区域 |
| M5.2 | 敌人配置化：以战斗舰为主的编队模板（含数量、模块、强化系数），不同区域数值曲线 | 每个区域有可辨识的敌人强度 |
| M5.3 | 特殊敌人/波次规则框架（占位：精英、首领标记） | 至少一个特殊敌人出场 |
| M5.4 | 难度曲线参数表 + 简单动态调整（若多次失败给提示/降低占位） | 前几区难度平滑 |

**M5 验收（人工测试 #5）**：见 8.6。

### M6 打磨与发布

| 步骤 | 内容 | 产出/验收 |
|:---:|------|----------|
| M6.1 | 数值平衡：全模块/船/敌人/成本统一走配置表并跑回归场景 | 平衡表成文、数值集中可调 |
| M6.2 | i18n 英文词条补全 + 语言切换全界面可用（含长文本排版） | 切 en 后无缺词（fallback 除外） |
| M6.3 | 存档版本迁移测试：v1→最新多档验证 | 老档可无损升级 |
| M6.4 | 性能与渲染优化（增量 DOM 更新、避免每 tick 全量重绘） | 大编队战斗不掉帧明显 |
| M6.5 | GitHub Pages 部署验证：仓库根目录即站点，全相对路径无 404 | 在 github.io 全流程可玩 |
| M6.6 | README：玩法说明、目录说明、部署步骤、测试指引 | 文档可用 |

**M6 验收（人工测试 #6 + 线上回归）**：见 8.7。

---

## 6. 功能开发清单（总表）

> 勾选状态随开发更新。里程碑列见第 4 节。模块按大纲 1:1 收录，重复/歧义项见 7 节 Q 列表。

### 6.1 基础设施清单

| ID | 功能 | 机制/说明 | 里程碑 | 状态 |
|----|------|-----------|:---:|:---:|
| A1 | tick 主循环 | 20tps、暂停、倍速；HUD 显示实测每秒 tick（tick/s）；暂停/调速控件仅在战斗中显示 | M0 | ✔ |
| A2 | 事件总线与日志 | 解耦 + 战斗日志 | M0 | ✔ |
| A3 | i18n 框架 | 词条表 + 回退 + 切换 | M0 | ✔ |
| A4 | 存档系统 | 序列化/自动保存/导出/导入/版本迁移；战斗中禁止保存/导出/导入，战斗结算完成后自动保存一次 | M0 | ✔ |
| A5 | 屏幕路由 | 菜单/战斗/基地切换 | M0/M3 | ✔ |
| A6 | 调试控制台 LS | 测试辅助 | M0 | ✔ |

### 6.2 模块总表（全部实现项）

> 分类与大纲一致；机制栏为设计草稿，数值在所属里程碑内定制。
> 状态口径：模块机制与数值链路已实现并通过人工测试即记 `✔`；各模块数据文件中的具体数值目前均为**占位值，待统一调参**，不影响本表状态。

| ID | 模块 | 分类 | 机制草稿 | 里程碑 | 状态 |
|----|------|------|----------|:---:|:---:|
| C01 | 火炮 | 攻击 | 单发伤害 + 冷却 + 能耗（见 3.3） | M1 | ✔ |
| C02 | 大型火炮 | 攻击 | 高伤、长冷却、可能带溅射 | M2 | ☐ |
| C03 | 激光 | 攻击 | 短冷却、持续光束伤害 | M2 | ☐ |
| C04 | 双向激光 | 攻击 | 激光双向齐射/前后均可打 | M2 | ☐ |
| C05 | 密集弹幕 | 攻击 | 多目标散射、低单发 | M2 | ☐ |
| C06 | 火箭发射器 | 攻击 | 召唤一次性火箭单位：目标在召唤瞬间按发射器模块目标锁定并**不可再改**（即使目标阵亡也不切换）；携带一次性"自毁弹药"模块，触发对固定目标造成大量伤害后令自身死亡（effects 新增自毁词条）。本实现替代"延迟弹道/AOE"草稿；大纲中火箭重复项见 Q5 | M2 | ✔ |
| C07 | 脉冲发生器 | 攻击 | 穿透或无视部分护盾 | M2 | ☐ |
| C08 | 阿尔法护盾 | 护盾 | 高额护盾上限型 | M2 | ☐ |
| C09 | 脉冲护盾 | 护盾 | 周期性抵消/间隔无敌帧 | M2 | ☐ |
| C10 | 再生护盾 | 护盾 | 持续回复型（见 3.3） | M1 | ✔ |
| C11 | 欧米茄护盾 | 护盾 | 高上限+特殊（如破盾返还） | M2 | ☐ |
| C12 | 防爆护盾 | 护盾 | 针对 AOE/火箭减免 | M2 | ☐ |
| C13 | 反射护盾 | 护盾 | 受击反弹伤害（受击钩子） | M2 | ☐ |
| C14 | 同盟护盾 | 护盾 | 为友方分摊/提供护盾（友方钩子） | M2 | ☐ |
| C15 | 电磁脉冲 | 功能 | 主动冷却型：对目标及 `blast_range` 波及范围内单位的能量上限置 0（`energy_cap_target` 取极小值后钳制）并持续 `duration_ticks`；`include_self` 使自身同受影响；结束自动恢复上限 | M2 | ✔ |
| C16 | 单枪匹马 | 功能 | 条件被动 `solo`：己方场上仅剩自身时，提升自身攻击系数（`attack_coeff_add`，自身单侧系数叠加）；条件不成立自动失效 | M2 | ✔ |
| C17 | 固若金汤 | 功能 | 主动：以 `force_target_self` 强制敌方全体集火自身（多来源按激活顺序后进先出，绝不改写 `targetId`，失效即回退），并通过 `damage_coeff_mul` 降低自身受到的伤害（仅在受伤结算点生效，自毁/能量吸取/上限类词条豁免） | M2 | ✔ |
| C18 | 时间扭曲 | 功能 | 主动：对目标及 `blast_range` 波及单位施加 `time_coeff` ＝ **−0.1 时间加速**（冷却/持续时间/临时单位存在时间的**需求量 ×0.9**，整数 tick 取值，每 tick 恒推进 1；与 C21 共用同一词条的正负两向）；`include_self` 自身生效，`prefer_self` 默认指向自身，`lock_target_on_activate` 使激活瞬间锁定目标且持续期内不可改选 | M2 | ✔ |
| C19 | 命中注定 | 功能 | 必中/暴击类 | M2 | ☐ |
| C20 | 辐能过载 | 攻击 | 主动：**仅在自身血量 ≤ 20% 时可激活一次**（`hp_below_activate: 0.2`，激活即打结构标记 `inst._firedOnce`、血量回升过阈值才清除标记）；激活后进入 `duration_ticks` 持续期，**持续期到期结算那一刻才产生效果**（`type: ['delayed_trigger','blast','explosive']`）——对**激活瞬间冻结**的主目标造成大量**爆炸伤害**并按 `blast_range` 溅射（防爆护盾可吸收），完全走既有伤害结算链路。替代原“队友死亡触发增伤”草案 | M2 | ✔ |
| C21 | 放缓时间 | 功能 | 主动：`time_coeff` ＝ **+0.1 时间放缓**（与 C18 镜像反向），对目标及 `blast_range` 波及单位施加，使冷却/持续时间/临时单位存在时间的**需求量 ×1.1**（整数 tick 取值，每 tick 恒推进 1）；加速与放缓为**同一词条的正负两向**，多来源按同一组合规则（**正、负各取绝对值最大者再求和**）；`include_self`/`prefer_self`/`lock_target_on_activate` 与 C18 相同 | M2 | ✔ |
| C22 | 欧米茄导弹发射器 | 功能 | 主动：召唤**非弹体类**「欧米茄导弹」（通用 `drone` 模板 + `attrs` 覆写名/图标/三围/护盾；`temp:true`、`lifespan_ticks`），**`maxSummoned:2`**（按**模块**计数，与普通导弹发射器互不挤占；达上限不召唤也不耗能）；**不写 `bind_target`/`projectile`** ⇒ **不锁定目标**（爆炸模块每次激活按正常目标链重解析，自动粘性目标阵亡/潜行即改选），登场**满盾**（`attrs.base.shieldCap`）；冷却/耗能明显高于普通导弹发射器（占位）。配套内部模块「欧米茄导弹爆炸」`omegaMissileWarhead`（`picker:false`）：与既有导弹爆炸同构但**伤害更高、`blast_range` 更大、引信 `cooldown_ticks` 更久** | M2 | ✔ |
| C23 | 潜行 | 功能 | 主动：目标为**自身单体**、持续型（`duration_ticks`），激活后**清空自身能量上限**（`energy_cap_target` 负大值＝压到 0）并给自身打上 `type:['stealth']` **潜行标记**（结算阶段添加、来源记账、到期/停用/阵亡/离场对称撤销）。被标记单位**不能成为主要攻击目标**（模块目标链与船级目标链同口径跳过：强制/手动/优先自己/船 `targetId`/粘性/策略各桶），但**激活瞬间已锁定的目标豁免**（`lockTargetId`、`lock_target_on_activate`），且**仍受 `blast_range` 溅射与伤害结算影响**；自身永远可选 | M2 | ✔ |
| C24 | 反应堆线圈（增幅器三件） | 功能 | **常驻自身增幅**（仅对自身、**无冷却/无持续/无耗能**，`type:['passive']` 跳过激活流程）：①**强辐线圈**（原「反应堆线圈」）`reactorCoil`（功能）＝`energy_cap_bonus` + `energy_regen_bonus`（能量上限＋每秒恢复）；②**护盾电池** `shieldBattery`（功能）＝`shield_coeff_add` 护盾系数加性（+0.1）+ `energy_cap_bonus` **负值削减能量上限**（−100；上限下降时当前能量即时钳制）；③**船体装甲** `hullArmor`（功能）＝`hp_cap_bonus`（血量上限）。三者均**乘模块类别系数**，唯一取值口径 `selfStaticBonus`、唯一落地口径 `syncHullCaps`（基准 + Σ自身常驻 + Σ目标级叠加，非累加，仅在安装/启停等结构变化时重算）；**提升上限只钳制当前值、不白送**（沿用 `shield_cap_bonus` 口径），登场满血/满能量由 `fillHullVitals` 负责；**不产生战报** | M2 | ✔ |
| C43 | 回收利用 | 功能 | 常驻被动：**场上任何单位阵亡（敌我合计、排除临时单位/召唤物）→ 下一 tick 按“上一 tick 阵亡数”恢复自身生命**（`hp_regen_per_death × 类别系数`）；死亡计数唯一累加点＝`onDeath`（判死唯一出口），`step()` 收尾提交 `lastTickDeaths` 快照，Pass1 只读（无反馈环、与遍历顺序无关、镜像对等）；回血走既有 `__pending.hpDeltas` → **结算步骤 4c** `applyHpTo`（钳到上限、不乘减免、不经护盾）；**0 阵亡零变化**；低频战报 `recycleRegen` | M2 | ✔ |
| C44 | 能量输送 | 功能 | 主动：**单体、仅友方**（`kinds:['ally']` 天然排除自身，引擎与 UI 候选池同口径）；**消耗自身能量 → 增加目标能量**——自身消耗走 `energy_cost`+`payEnergy`，目标增益走目标级量值词条 **`energy_target`**；Pass1 只记账（零数值变化），**结算步骤 3 先扣后给**、给予经 `applyEnergyTo` **钳到目标上限**；能量不足不激活不耗能；**目标能量已满时按既有 `canImpact` 口径不激活**（部分溢出则照常消耗并钳掉）；零引擎改动 | M2 | ✔ |
| C25 | 货舱 | 运输 | **已实装**：`transport/cargoHold.js`＝本体货舱容量提升（`cargo_cap_bonus` 类词条，乘类别系数）；容量口径单一来源 `cargoCapPartsOf`/`cargoLoadOf`（单位本体 + Σ模块词条×系数），与矿物容量为**两套独立容器**（见 Q6） | M2 | ✔ |
| C26 | 货物维修 | 运输 | **已实装（口径修订）**：`transport/cargoRepair.js`＝**消耗自身货舱中「已入舱」的一件货物（吨位最小优先）恢复目标生命值**，回血＝`round(货物吨位 × hp_per_ton)`（`hp_per_ton` 不进 `AMOUNT` 表、**不乘类别系数**）；目标 `ally+self`（**无 `prefer_self` ⇒ 自身无隐式优先级**）；三条门控＝无未强化可用货物/能量不足/目标满血；**结算 3d-4 扣货（不返还星区）→ 步骤 4c 回血**；战报 `battle.log.cargoRepair` | M2 | ✔ |
| C27 | 急速货运 | 运输 | 运输速度提升 | M4 | ☐ |
| C28 | 恒星生命延长 | 运输 | 收益时间延长类 | M4 | ☐ |
| C29 | 运输无人机 | 运输 | 自动运输加成 | M4 | ☐ |
| C30 | 调度 | 运输 | 运输效率/路线调度 | M4 | ☐ |
| C31 | 采矿探头 | 采矿 | 发现矿藏/提升采矿基础 | M4 | ☐ |
| C32 | 采矿增速 | 采矿 | 采矿速率提升 | M4 | ☐ |
| C33 | 氢气精炼 | 采矿 | 氢气产出精炼加成 | M4 | ☐ |
| C34 | **货物强化**（原「货物强化器」） | 采矿 | **已实装**：`mining/cargoEnhance.js`＝**消耗矿物+能量**提升**目标单位货舱中一件「已入舱」货物**的加成系数（词条 `bonus_add` 加性、逐级递增、**不乘类别系数**、FIFO 取第一件未强化者）；**每件货物只能被强化一次**（一次性标记 `enhanced` **随实体走**，传输/返还/再装载均不清除；幂等）；目标 `ally+self`（无 `prefer_self`）；门控＝无未强化货物/矿物不足/能量不足；**结算 3d-5 写入**；芯片**末尾占位格显示 `※`**（悬停「已强化」）；战报 `battle.log.cargoEnhance` | M2 | ✔ |
| C35 | 矿藏富集 | 采矿 | **已实装**：`mining/oreEnrichment.js`＝对星区施加**储量乘算**词条 `sector_ore_mul`（纯比例、储量 0 时不激活）+ 星区冷却 `sector_cd_ticks`；结算步骤 **3c**（先加后乘）、无存活门控 | M2 | ✔ |
| C36 | 氢气上传 | 采矿 | 氢气直传基地 | M4 | ☐ |
| C37 | 氢气火箭 | 采矿 | 消耗氢气换取收益 | M4 | ☐ |
| C38 | **壁垒无人机**（原「护盾无人机」） | 无人机 | **已实装**：`drone/bulwarkDroneSpawn.js`＝召唤壁垒无人机，携带**固若金汤 + 硬化护盾**；**文档硬数值：`hp 50`、`energyRegen 50`、`energyCap 2000`**（`shieldCap 20` 占位）；`temp:true`、`lifespan 1200t`（占位）、`maxSummoned 2/3/4/5` | M2 | ✔ |
| C39 | 维修无人机 | 无人机 | **已实装**：`drone/repairDroneSpawn.js`＝召唤维修无人机，携带**维修光束**（新模块 `function/repairBeam`，见 C55）；三围/存在时间/在场上限为**占位** | M2 | ✔ |
| C40 | 火箭无人机 | 无人机 | **已实装**：`drone/rocketDroneSpawn.js`＝召唤火箭无人机，携带**火箭发射器**；**文档硬数值：`energyRegen 50`、`energyCap 1000`**（`hp 30`/`shieldCap 10` 占位）；`lifespan 1200t`（占位） | M2 | ✔ |
| C41 | **激光炮塔**（原「激光无人机」） | 无人机 | **已实装**：`drone/laserTurretSpawn.js`＝召唤激光炮塔，携带**`laser` ×2（两个独立实例、不受槽限）+ 再生护盾**；**文档硬数值：`energyRegen 30`、`energyCap 1000`、`lifespan_ticks 2400`**（`hp 40`/`shieldCap 20` 占位）；图标＝**实心正六边形 SVG** `assets/img/激光炮塔.svg`（无人机类棕） | M2 | ✔ |
| C42 | **哨戒炮塔**（原「火炮无人机」） | 无人机 | **已实装**：`drone/sentryTurretSpawn.js`＝召唤哨戒炮塔，携带**`cannon` ×1 + 同盟护盾**；**文档硬数值：`energyRegen 30`、`energyCap 1000`、`lifespan_ticks 2400`**（`hp 40`/`shieldCap 20` 占位）；图标＝**实心正六边形 SVG** `assets/img/哨戒炮塔.svg` | M2 | ✔ |
| C55 | 维修光束 | 功能 | **已实装**：`function/repairBeam.js`＝**`picker:false` 仅无人机专属**（玩家不可安装）；单体**仅友方**（`kinds:['ally']`，**天然排除自身**，与矿物/货物维修口径不同）、耗能、含冷却；回血＝`hp_target` + **`exact_amount`**（词条原值、**不乘类别系数**）经 `applyHpTo` 落地（按 `hpMax` 截断、不受受伤减免）；门控＝能量不足／目标满血不激活；**结算沿用步骤 4c（无新步骤）**；**暂不产生战报**（如需低频战报请明确） | M2 | ✔ |
| C56 | 纳米无人机 | 无人机 | **已实装**：`drone/nanoDroneSpawn.js`＝携带**火炮 ×1 ＋ 本模块自身 ×1**（**可链式/递归召唤**）；**文档硬数值：`hp 50`、无护盾（`shieldCap:0`）、`energyRegen 20`、`energyCap 500`、`lifespan_ticks 400`（＝冷却 200×2，字面预写、调冷却需同步）**；占位＝冷却 200／耗能 50／`maxSummoned 2/3/4/5`；★ **不会无限增长**：`maxSummoned` 按 `u.summonMod === inst.moduleId` 计数 ⇒ **父子实例同 id ⇒ 共用同一上限**（每阵营总存活 ≤ 上限），另有 `cool_first`（子体安装即冷却 200t）、每 200t 至多 1 架、`temp:true`+400t 寿命、**Pass1 行动名单为 tick 起始快照**（本 tick 新召出者本 tick 不行动）四重自然约束 ⇒ **无需额外防递归限制** | M2 | ✔ |
| C45 | 矿物舱 | 采矿 | **已实装**：`mining/oreHold.js`＝矿物容量提升（`ore_cap_bonus` 类词条乘类别系数）；与运输类「货舱」（C25）为**两套独立容器**；名录归属见 Q6 | M2 | ✔ |
| C46 | 矿物压缩 | 采矿 | **已实装**：`mining/oreCompressor.js`＝采矿系数加性词条 `mining_coeff_add`（+0.1/+0.15/+0.2/+0.3 占位），使 `实际采矿量 = round(ore_gain × 采矿系数)` | M2 | ✔ |
| C47 | 创世纪 | 采矿 | **已实装**：`mining/genesis.js`＝星区储量**加性**词条 `sector_ore_add`（+100/+200/+350/+600 占位，**乘采矿系数**、Pass1 快照算好、只取整一次）+ 星区冷却 `sector_cd_ticks`（60t） | M2 | ✔ |
| C48 | 采矿激光 | 采矿 | **已实装**：`mining/miningLaser.js`＝`ore_gain` 入库、按星区储量截断、按单位矿物容量截断、不足按比例拆分（floor + 余数给较大请求） | M2 | ✔ |
| C49 | 矿石输送 | 采矿 | **已实装**：`mining/oreTransfer.js`＝单体**仅友方**（排除自身）、耗能；把自身携带矿物 **1:1** 输送给目标（**无系数**、无 `exact_amount` 需求）；受 tick 起始携带预算 `ctx.oreAvail` 约束；**结算 3d-2**（原子 ±N）；战报 `battle.log.oreTransfer` | M2 | ✔ |
| C50 | 矿物维修 | 采矿 | **已实装**：`mining/oreRepair.js`＝耗矿物（`ore_cost`）+耗能，按 `hp_target` + `exact_amount`（**目标级量值用词条原值、不乘类别系数**）回血；目标 `ally+self`（**无 `prefer_self`**）；门控含目标满血不激活；**结算 3d-1 扣矿 → 步骤 4c 回血**；战报 `battle.log.oreRepair` | M2 | ✔ |
| C51 | 矿渣导弹发生器 + 专用弹头 | 采矿 | **已实装**：`mining/slagMissileLauncher.js`（**耗矿不耗能** `ore_cost`、召唤通用无人机模板、`lifespan_ticks`/`maxSummoned:2`）+ `attack/slagMissileWarhead.js`（`picker:false`、高伤 + `blast_range`）；图标紫色副本 | M2 | ✔ |
| C52 | 装载光束 | 运输 | **已实装**：`transport/loadingBeam.js`＝`type:['cargo_loader']` 标签 + `cargo_load` 词条；**无需目标**（不进目标链）、**无自身冷却**、`energy_cost` 每次开始装载扣一次；装载时长＝`max(1, round(货物装载时间 ÷ (1+cargo_load+运输系数−1)))`；对象顺序＝**优先队列队首 → 星区列表顺序**，逐件排除（已锁定/本 tick 已认领/**本舰装不下**/同阵营手动卸载未过期）；**一次一件**、启动即锁定、停用或阵亡**解锁并进度归零（能量不退）**；完成 ⇒ 入舱 + **`loadTicks` 永久改写为 20t**；**同 tick 争抢按装载速度降序**优先认领；**结算步骤 5**；战报 `battle.log.cargoLoad` | M2 | ✔ |
| C53 | 货物传输 | 运输 | **已实装**：`transport/cargoTransfer.js`＝`type:['cargo_transfer']`；单体**仅友方**（排除自身）、耗能+冷却；**一次整件原子搬运**（保留 id/吨位/等级/`loadTicks`）、**容量不足按货舱列表顺序顺延**取第一件装得下的（全装不下则不激活）；目标与货物 Pass1 冻结、**结算 3d-3** 防御性复核；战报 `battle.log.cargoTransfer` | M2 | ✔ |
| C54 | 卸载货物（交互） | 运输 | **已实装**：单位详情「装载货物」栏点击已入舱芯片 ⇒ `battle.unloadCargo` ⇒ 货物**追加到星区列表队尾**（队列式、不恢复初始位置）；启动**同阵营 + 限时 600t(30s)** 的手动卸载标记（仅排除同阵营装载器的自动选取、敌方不受影响、入队即清除）；战报 `battle.log.cargoUnload` | M2 | ✔ |

> 说明：运输类/采矿类模块在 M2 阶段可先注册为"占位模块"（可安装但效果挂空），到 M4 接入资源系统后再实装效果，避免拖慢战斗线。
> **现状（2025 收尾）**：**运输类与采矿类已在 M2 阶段实装效果**（不再挂空）——C25 货舱、C26 货物维修、C34 货物强化、C35 矿藏富集、C45–C54 全部可安装并产生完整结算（见 3.5 结算顺序中的 3b/3c/3d-1…3d-5/步骤 5）。**尚未实装**：C27 急速货运、C28 恒星生命延长、C29 运输无人机、C30 调度、C31 采矿探头、C32 采矿增速、C33 氢气精炼、C36 氢气上传、C37 氢气火箭（氢气系与经济收益类，待 M4 资源系统）；运输/采矿模块的**数值仍为占位**，待统一调参。
> **货物系统（非模块 · 已实装）**：星区货物＝一货一文件注册表（`None` + 6 种模块零件）＋等级解析（与模块/船型同构）；星区栏＝**标题行 + 芯片行**（队列式排列、**总件数不设上限**）；单位详情「装载货物」栏＝已入舱 + 在装（锁定中）；芯片＝`[序号] 名称 [·加成%] [·Lv] [·吨位t·装载s] [末尾占位/※]`，字号 `--chip-font-size: 13px`、高 32px；百分比两套口径＝`formatBonusPercent`（倍率）/`formatBonusDeltaPercent`（增量、带符号）。

### 6.3 船舰清单

| ID | 功能 | 机制/说明 | 里程碑 | 状态 |
|----|------|-----------|:---:|:---:|
| D1 | 战斗舰 | 系数/槽位见 3.2，数值占位 | M1 | ✔ |
| D2 | 运输舰 | 同上 | M2（上场）/M4（玩法） | ☐ |
| D3 | 采矿船 | 同上（D3 修正系数） | M2（上场）/M4（玩法） | ☐ |
| D4 | 敌人战斗舰 | 复用 ship，敌 AI 配置 | M1 | ✔ |
| D5 | 船升级成长 | 等级→属性/槽位/新模块解锁 | M3/M4 | ☐ |

### 6.4 主基地 / 系统清单

| ID | 功能 | 机制/说明 | 里程碑 | 状态 |
|----|------|-----------|:---:|:---:|
| E1 | 星门 | 战区（难度档）选择、**随机"星域编号"**（界面不称种子、不可输入）、出征装配（基地舰队 ⇒ 星域玩家单位）、**返回闭环**（星门星区主动返回 + 结束自动返回 + 修复 + 携带物入库 + 未返回即损失） | M3 | ☐ |
| E2 | 指挥中心 | **出战上限＝2 + 等级**（占位）；升级消耗能量币/合金/稀土；增益待 M4 定义（M3 只登记显示，**不伪造加成**） | M3 | ☐ |
| E3 | 研究站 | **实装**：返回货物按类型堆叠入库 → 研究（消耗货物 + 能量币 + 耗时，可投科研点加速）→ 产出**模块蓝图**（解锁可安装 / 提升可升级上限），在船坞生效 | M3 | ☐ |
| E4 | 船坞 | 建造（可建造性 `buildable`）/ 改装（槽位 + 蓝图校验）/ 升级（等级→属性与槽位：每 2 级 +1、上限 +2 占位） | M3 | ☐ |
| E5 | 星球产业 | 入口**占位不可用**（面板列出各资源种类与用途），产业建造与产出属 M4 | M4 | ☐ |
| F1 | 资源系统 | **五资源（M3 启用）**：能量币（一切活动消耗）/ 矿物（原料，星域采矿带回）/ 合金（建造材料）/ 稀土（高等级建造材料）/ 科研点（加速研究）；**账户与校验**在 M3，产出链在 M4 | M3 账户/M4 产出 | ☐ |
| F2 | 运输玩法 | 运输收益闭环 | M4 | ☐ |
| F3 | 采矿玩法 | 采矿收益闭环 | M4 | ☐ |
| F4 | 战斗掉落 | 胜利产出物品（M3 由星域**返回货物的转换**承接） | M4 | ☐ |
| F5 | 科技/解锁树 | 解锁模块/船/产业（M3 只做**模块蓝图**一条链路） | M4 | ☐ |

### 6.5 敌人 / 区域清单

| ID | 功能 | 机制/说明 | 里程碑 | 状态 |
|----|------|-----------|:---:|:---:|
| G1 | 敌人编队配置 | data/enemies.js 模板 | M2/M5 | ☐ |
| G2 | 区域等级 | 星区难度/解锁 | M5 | ☐ |
| G3 | 特殊敌人 | 精英/首领占位 | M5 | ☐ |
| G4 | 难度曲线 | 参数表 | M5 | ☐ |

### 6.6 发布/维护清单

| ID | 功能 | 机制/说明 | 里程碑 | 状态 |
|----|------|-----------|:---:|:---:|
| H1 | 全相对路径部署校验 | github.io 冒烟测试 | M0 起/M6 | ☐ |
| H2 | 英文词条 | en.js 补全 | M6 | ☐ |
| H3 | 存档迁移 | schemaVersion 升迁 | M6 | ☐ |
| H4 | 平衡性 | 数值走配置表 + 回归 | M6 | ☐ |
| H5 | 性能 | 增量渲染优化 | M6 | ☐ |
| H6 | README | 玩法/部署/测试说明 | M6 | ☐ |
| T1 | 人工测试 | 按第 8 节执行 | 每阶段末 | ✍（#0 已通过） |

---

## 7. 待确认事项（风险与开放问题）

| ID | 问题 | 现状（默认按此实现） | 需要确认的时机 |
|----|------|---------------------|---------------|
| Q1 | 采矿船对护盾类/功能类系数 | 按"全船通用项恒为 1"暂定：采矿船 = 采矿 1，攻击/运输 0.5，护盾/功能 1（D3 只修正了攻击/运输） | 实现 D3 前 |
| Q2 | 能量回复规则 | 基础回复 5/秒；模块消耗叠加；无"能量生产模块"概念前不回充加速 | M2 前 |
| Q3 | 船类系数作用对象 | 默认作用于模块"效果量"（伤害/回复/产出）乘区，不影响冷却与能耗 | M1 数据模型定稿前 |
| Q4 | 攻击目标选择 | 最近存活敌方单位；波及/穿刺类模块按自身规则 | M1 实现前 |
| Q5 | 大纲中"火箭发射器"出现两次 | 默认只做一个（C06）；若想区分为两种请在 M2 前说明 | M2 前 |
| Q6 | "货舱"同时出现在运输类与采矿类 | 建议：模块支持多分类标签（运输/采矿通用件），两类船系数取各自对该分类的值 | M2 数据模型前 |
| Q7 | 功能类模块的主动/被动定位 | 技能类（需点击、耗能、技能 UI 行）与条件/被动类并存；具体每个模块归属在 M2 逐一定义 | M2 前 |
| Q8 | 战斗奖励 → 研究站物品闭环 | 大纲未详述掉落与"探险物品"来源；先做占位闭环，等你补充设定 | M4 前 |
| Q9 | 存档是否含对战中状态 | 默认不记录进行中的战斗，战斗开始/结束时存档基地与编队状态 | M1 存档接入时 |
| Q10 | 区域等级与成长曲线 | 数值表等 M5 前由你补充或由我拟草稿再确认 | M5 前 |
| Q11 | 目录结构与命名 | 见 2.1，如不满意可调整 | M0 前 |
| Q12 | 后勤单位（运输/采矿）在战斗中处于"后勤行"的行为 | 先做最小规则（在场提供增益/产出），详细玩法 M4 前再定 | M2/M4 |

---

## 8. 人工测试计划

> 约定：里程碑收尾时，由 AI 先输出《测试期望》+《测试方案》，用户确认后执行；发现异常记录到本文档「8.8 缺陷记录」。

### 8.1 测试 #0（M0 验收）
- 期望：页面打开无报错；tick 计数稳定按 20/秒递增；暂停/倍速生效；菜单 ↔ 战斗占位屏切换正常；导出 JSON 再导入不报错且状态一致。
- 方案：静态服务器打开 index.html → 看控制台与页面 → 逐项操作导出/导入。

### 8.2 测试 #1（M1 Demo 验收）
- 期望：进入战斗后——① 双方每 1 秒开火一次（日志可见）；② 我方再生护盾按 4/秒回复且能耗 1/秒，能量不足时暂停、回复后自动续；③ 护盾先于血量承伤、盾破后掉血；④ 能量总量随时间下降（净消耗约 2/秒）可被观察；⑤ 一方 HP 归零弹结算、可重新开始；⑥ 战斗相关文本全为词条（切语言验证 key 覆盖）。
- 方案：以固定编队连续打 3 场，观察不同胜负走向；用控制台 LS 调整能量/血量做边界测试（能量 0 时武器停火）。

### 8.2b 测试 #1b（M1.9 战斗视觉与交互增强验收）
- 期望：① 战斗场景观感完整，舰船为单位图标：我方**蓝色正三角形**、敌方**红色**（SVG）；② 单位实体为矩形卡，自上而下为 单位图标→HP/护盾/能量状态条→简约模块图标；③ 实时对战信息：我方模块冷却以徽标数字显示、就绪时脉冲高亮；敌方卡有"下一步：开火 · Nt 后 / 开火就绪"等提示，结合其模块冷却可预判行为；④ 点击单位后场景下方出现详情面板：数值、每模块状态与能耗、目标信息；⑤ 点己方单位可把主要攻击目标指定给某敌方单位（或恢复"自动（最近）"），指定后实际开火立刻转向该目标；敌方单位详情只读。
- 方案：演练场开战 → 观察图标/条/徽标与"下一步"提示 → 点击我方船 → 目标指定"1. 战斗舰"→观察其开火对象与战报"将主要攻击目标设为…" → 恢复自动；挑战场（2 敌）重复指定其一验证转向；选中敌方卡核对只读提示；切英文复查无 `??`。

### 8.3 测试 #2（M2 验收）
- 期望：所有攻击/护盾模块可安装且机制可见（反射盾触发反弹、防爆盾对 AOE 减免等）；技能行按钮点击生效并扣能；后勤单位出现在第 4 行；2v2 场景可运行；结算面板数据正确。
- 方案：为每类机制单独搭测试场景；逐一验证后统一跑一场 2v2。

### 8.4 测试 #3（M3 验收）
- 期望：基地五建筑可进入；船坞能造/改/升船；星门选"战区 1"后带基地所造船只出战；指挥中心等级提升出战上限；研究站可分析占位物品；关页重开状态完整恢复。
- 方案：新建档 → 造 2 艘船 → 改装 → 出战 → 回基地 → 刷新页面核对。

### 8.5 测试 #4（M4 验收）
- 期望：产业按 tick 产出并在顶部资源条可见；采矿/运输行为产生资源；战斗掉落进入背包；分析物品解锁科技并产生可见影响；船坞消费资源后资源正确扣减。
- 方案：跑 10 分钟小循环（采/运/战/研各一轮）核对资源账目（手动记录关键数值对照）。

### 8.6 测试 #5（M5 验收）
- 期望：星门列出多区域且解锁条件生效；不同区域敌人编队与强度明显不同；特殊敌人至少在一区出现；低等级编队挑战高等级区域失败符合预期。
- 方案：按区域顺序各打一场记录战损与奖励。

### 8.7 测试 #6（M6 验收 + 线上回归）
- 期望：切换英文无缺词与排版错乱；v1 老档迁移成功；大编队战斗流畅；github.io 线上全流程（含导出/导入）可用且无 404。
- 方案：本地全流程回归一遍 → 推送 GitHub Pages → 线上复测。

### 8.8 缺陷记录

| 日期 | 来源测试 | 缺陷描述 | 状态 |
|------|:---:|----------|:---:|
|      |      |          |      |

---

## 9. 开发约定

1. 每次实现以里程碑为单位，一步一验证，避免大改。
2. 提交信息建议格式：`Mx: 一句话说明`（如 `M1: 完成火炮与再生护盾模块`）。
3. 新增模块 = 四处联动：`data/modules.js` 配置 + 效果逻辑 + i18n 词条 + UI 呈现，缺一不可（存为通用验收规则）。
4. 数值一律进 `data/` 配置，禁止散落硬编码。
5. 需要人工测试的节点（8.x），AI 必须先给期望与方案再执行。
6. 素材（SVG 图标/图片等）一律放 `assets/` 独立目录并用相对路径引用，禁止内联散落在代码或 CSS 中。

---

---

## 10. 开发进度日志

| 日期 | 里程碑 | 完成内容 | 备注 / 待办 |
|------|:---:|----------|-------------|
| 2025（日期待补） | M0 | M0.1–M0.8：目录结构、index.html + 基础 CSS、tick 主循环（20tps/暂停/倍速）、事件总线与日志、i18n 框架（zh-CN + en 骨架）、路由 + 菜单 / 战斗占位屏、存档系统（本地 / 导出 / 导入 / schemaVersion 迁移）、LS 调试控制台与顶栏 HUD | ✔ 人工测试 #0 通过（VS Code 环境 1~7 项正常）；git init/提交由用户人工执行，建议备注：`M0: 脚手架与基础框架（tick/事件总线/日志/i18n/存档/路由/HUD/LS 调试），测试#0通过` |
| 2025（日期待补） | M0 修正 | HUD tick 改为实测每秒 tick（tick/s）；暂停/调速按钮仅在战斗中显示；战斗中禁止保存/导出/导入，战斗结束（结算完成后）自动保存一次 | ✔ 已确认（VS Code 复查通过）；建议 git 备注：`M0: tick显示tick/s、调速按钮仅战斗显示、战斗中禁存档` |
| 2025（日期待补） | M1 | M1.1–M1.8：data/ships.js 与 data/modules.js、entities（ship/module）、systems/battle.js（tick 结算顺序/能量/敌AI/胜负 + combat:state 契约）、对战场景五区布局与船卡（HP/护盾/能量条/模块槽）、战报、结算浮层、演练/挑战两场景、战斗全程 i18n | ✔ 人工测试 #1 通过；建议 git 备注：`M1: 对战Demo（战斗舰+火炮+再生护盾、实时结算、胜负浮层）` |
| 2025（日期待补） | M1.9 | 战斗视觉与交互增强：SVG 舰船图标（我方蓝/敌方红正三角形）、单位矩形卡（图标→状态条→模块图标）、模块冷却徽标/就绪脉冲、"下一步"行为提示、点击单位→详情面板、己方主目标指定 | ✔ 人工测试 #1b 通过；建议 git 备注：`M1: 战斗场景视觉与交互增强（SVG舰船图标/冷却提示/详情面板/主目标指定）` |
| 2025（日期待补） | M1.9 | 统计与目标系统收尾：每模块 DPS 贡献行（activeTicks 窗口冻结）、详情面板模块启停 + 齿轮 AI 预留入口、effects 词条驱动执行器（勿按 type 硬编码）、目标词条 kinds(self/ally/enemy/any)+countMode(single/multi/maxCount/all)、统一目标解析链（模块→船→指挥栏全队策略） | ✔ 已确认；建议 git 备注：`M1: 统计DPS贡献行与目标系统（kinds/countMode/all 统一目标解析链）` |
| 2025（日期待补） | M1.9 | 彩色统一目标交互：所有目标选择器与命中/当前目标显示统一同一套着色（友方蓝 side-ally/敌方红 side-enemy）；"命中目标："实时行 / 单位卡"目标"/指挥栏"当前命中"一致；开战自动暂停（先调模块目标/启停再点继续） | ✔ 已确认 |
| 2025（日期待补） | M1.9 | 目标级三类词条与上限语义：量值 hp_target/energy_target（正加负减，hp 负可致死）；上限 shield_cap_target/hp_cap_target/energy_cap_target = 单次一次性、仅对当前所选目标（每次触发先撤销旧影响再对新目标重施；切换目标于下次触发迁移）；修复 hp/energy 上限未从基准重算导致的叠加 bug（hull 记录 baseShieldCap/baseHpMax/baseEnergyCap，capOverlays 三围记账，cleanDeadEffects 阵亡清理） | ✔ 已确认 |
| 2025（日期待补） | M1.9 | effects 词条全项目统一【下划线】命名（damage/max_damage/ramp_per_hit/shield_gain/shield_cap_bonus/shield_gain_target/shield_cap_target/hp_target/hp_cap_target/energy_target/energy_cap_target/duration_ticks/cooldown_ticks/energy_cost），后续通用命名沿用下划线 | ✔ 已确认 |
| 2025（日期待补） | M1.9 | 逐步伤害 ramp_per_hit + max_damage：每次成功激活 +ramp（有 max_damage 从基础 damage 起涨封顶 max_damage；无则从 0 起涨封顶 damage）；目标改变/阵亡/停用/模块能量不足时成长清零回到基础值 | ✔ 已确认 |
| 2025（日期待补） | M1.9 | 演练入口改造：由硬编码 SCENARIOS/ALLY_FLEET 改为"演练编队配置"沙盘——我方/敌方两列均可增删战斗舰、为每舰从已注册模块选装/卸除并按 SHIPS[].slots=3 限槽；无预设、仅自定义；开战与结算"再战"按同一配置快照重开 | ✔ 人工测试通过 |
| 2025（日期待补） | M1 | **M1 收尾**：M1.9 全批 + 演练沙盘整体回归通过，M1 关闭 | ✔ 里程碑完成；建议 git 备注：`M1: 战斗Demo收尾（目标系统/彩色选择/上限词条/逐步伤害/演练沙盘），整场回归通过` |
| 2025（日期待补） | M2.1 | **召唤框架·提前建立**（供 M2.1 火箭发射器等攻击模块即用，亦支撑后续 M2.3 无人机）：`ships.js` 通用 `drone` 召唤模板 + `createShip` 按 overrides 整条覆写模板；召唤模块按自身等级经 `resolveModuleCfg` 深合并 `attrs`(nameKey/base/coefficients) 与 `target`/`effects` diff；`effects.summon`（type/modules/lifespan_ticks/maxSummoned/temp/energy_cost/cooldown_ticks）驱动 doSummon 补召；temp=true 受存在时间限制、阵亡/到期直接移场，temp=false=普通单位（保留灰色卡）；单位图标=召唤模块图标否则 ▲ | ✔ 已确认 |
| 2025（日期待补） | M2.1 | **召唤物渲染与目标队列分离 + 自动目标策略**：召唤物在单位卡列**列尾**显示（渲染队列与目标队列分离，仅目标队列按策略排位）；出场编号整队统一递增、稳定不随队列/移除变化；自动目标策略集 order/最低血量/最低护盾/**优先无人机**/**优先舰船**；指挥栏全队策略 + 单舰“按自动策略/独立策略覆盖/跟随全队” + 手动锁定；自动“粘性”（旧目标不丢、丢失后重选）；召唤物继承母船目标与策略 | ✔ 已确认 |
| 2025（日期待补） | M2.1 | **召唤物显示与修复**：临时单位卡片/详情存活倒计时；战报单位名敌方红/我方蓝、战报框高度可拖；单位详情“跟随全队”按钮语义改“按自动策略”；修复召唤队列反复刷新致点不开详情、开战自动暂停时全队目标不可设 | ✔ 已确认 |
| 2025（日期待补） | M2.1 | **召唤框架提前小阶段收尾**（作为 M2.1 攻击模块的前置基建，回归通过）；**进入 火箭发射器 C06** 开发（召唤一次性火箭单位，固定目标不可改，effects 新增自毁词条） | ✔ 已确认 |
| 2025（日期待补） | M2.1 | **火箭发射器 C06 实装完成**：召唤一次性火箭单位（`temp` + `lifespan_ticks`），目标在召唤瞬间按发射器模块目标 **`lock_target_on_activate` 式锁定、永不改选**（目标阵亡亦不切换，仅作废）；火箭携带一次性弹头模块（`rocketWarhead`/`missileWarhead`，爆炸伤害标签），触发后对固定目标造成大量伤害并令自身死亡（`self_destruct` 词条）；召唤物继承母船目标与策略 | ✔ 已确认 |
| 2025（日期待补） | tick 系统 | **结算范式重构（两阶段 + 挂账缓冲）**：行动阶段只记账、结算阶段统一落地；数值先进目标 `__pending`，全部模块激活后才结算、结算完再判死；行动判定读 tick 起始快照、召唤下一 tick 才行动；开战/召唤即满盾就位；同 tick 双方同归判平局；已死单位本 tick 开出的伤害仍照常结算 | ✔ 已确认；建议 git 备注：`tick: 两阶段结算（挂账缓冲/同时结算/满盾就位/同归平局）` |
| 2025（日期待补） | tick 系统 | **step() 遍历收敛**：每 tick 对全体单位的遍历由 ~13 处收敛为 3 遍（Pass1 行动 / Phase A 结算 / Phase C 收尾）；`dropDeadTargets`→并入 Pass1 每单位 `clearShipDeadRefs`、`cleanDeadEffects`→各判死点就地 `onDeath`、`finalizeStats`→Phase C 的 `finalizeModule`；修复合账初始化顺序导致的“仅敌方能打出伤害” | ✔ 已确认；建议 git 备注：`tick: step遍历收敛为3遍（onDeath/clearShipDeadRefs 就地化）` |
| 2025（日期待补） | 护盾系统 | **独立池 + UI 池条**：每来源独立护盾池、按时长型模块激活序(`_shieldSeq`)吸收、长期/基地池最低优先级最后用；破盾后池条仍显示(值 0 + 由 `modulePoolCapOf` 取上限)避免跳变；护盾数值变化用“变色+同色辉光、缓动到目标宽度后回本色”取代闪烁 | ✔ 已确认 |
| 2025（日期待补） | 护盾系统 | **破盾就地结算 + no_break 耗尽不计 cap**：伤害/汲取把某时长盾池抽空即在 `applyHit`/`settleNonDamage` 就地破盾（结束持续+删池降 cap），不再事后全量扫描；`no_break`(同盟/防爆)打空仅标 `_shieldSpent`——不再计入总 cap 但不提前结束、不进冷却，重新激活清除标记并回满 | ✔ 已确认 |
| 2025（日期待补） | 反射 | **反射返程延迟到武器结算后**：反射触发只记账入 `reflectQueue`（即时闪标），本 tick 全部武器/爆炸命中结算完后统一以 `noReflect` 补打回，消除“同 tick 先吃反射返程致武器反射缺失”的不对等；反射不二次反射 | ✔ 已确认 |
| 2025（日期待补） | 战报日志 | **命中句拆分（逐吸收段）**：`{actor}的{武器}{开火命中/爆炸命中/溅射到}{target}` + 按实际吸收顺序逐段「，对{承接者}造成{N}点{伤害类型}伤害」，承接者=护盾模块名(绿)/舰载护盾/共享同盟护盾/共享防爆护盾/舰体；伤害类型取自来源模块 `fx.type` 标签(projectile→动能/beam→能量/explosive→爆炸/无→普通)；反射成句“{owner}的{模块}反射{N}点伤害给{attacker}+落地段”；“被击毁”改到命中句之后显示；0 伤害/无敌不产出句 | ✔ 已确认；建议 git 备注：`战报: 逐吸收段成句+伤害类型+绿模块名+击毁顺序修正` |
| 2025（日期待补） | 战报日志 | 弹头 `missileWarhead`/`rocketWarhead` 补爆炸伤害标签(`type` 加 `explosive`→“爆炸伤害”)；新建 `战报日志开发要点.md`（涉战报/日志任务先读，可更新，仅记关键要素与主要方法/修改位置） | ✔ 已确认 |
| 2025（日期待补） | M2.4 | **功能型模块首批四件**：`emp` 电磁脉冲（负 `energy_cap_target` 把能量上限压到 0＝“瘫痪”，同批一次性清空 `ramp_per_hit` 成长）、`singleHanded` 单枪匹马（`solo` 标签 + `attack_coeff_add` 类别系数加性增益）、`impregnable` 固若金汤（`force_target_self` 标签＝**多来源有序强制栈**（后激活者优先、失效回落下一来源）+ `damage_coeff_mul` 受伤减免）、`timeWarp` 时间扭曲（`time_coeff` 时间加速 ＝ −0.1：**缩放需求量**、整数 tick，每 tick 恒推进 1）；**新词条** `attack_coeff_add(_target)`、`damage_coeff_mul(_target)`、`time_coeff`；**新 `type` 标签** `solo`、`force_target_self`、`include_self`（由 effects 词条升级为标签）、`prefer_self`、`lock_target_on_activate`；统一**目标优先级链**（激活锁定 > 强制目标 > 模块手动目标 > 优先自己 > 船 `targetId` > 自动粘性 > 全队策略，`moduleTargetList` 与 `shipEffectiveTarget` 同口径）；修复**“模块手动目标改不动”**BUG（手动目标存活校验误用敌方队列 → 改为全场 `aliveUnitId`，模块目标可为自身/友军）；战报按事件聚合（EMP 逐目标一条、强制目标每次激活一条、强制回落/解除带模块 owner、时间加速仅“从无→有 / 从有→无”各一条） | ✔ 已确认；建议 git 备注：`M2.4 功能型模块四件 + 数值统一结算阶段与 UI 收尾` |
| 2025（日期待补） | 引擎/UI | **结算范式与 UI 收尾**：所有数值修改（含上限修改与恢复）统一到 tick 结算阶段、跨单位固定顺序（详见 3.5）；**受伤减免唯一结算点**＝`applyHit` 入口（受击方乘一次，豁免自毁/能量削减/上限类）；`effectSetOf` 共享“作用集合”helper（目标 ∪ `blast_range` 波及 ∪ `include_self` 自身，上限类与时间加速共用）；详情页**「单位系数」栏**（可折叠、默认折叠、只放数值不放解释文案，攻击/护盾/功能/运输/采矿/受伤减免/时间系数 同行 7 项 +「其它系数」单列，取值全走引擎导出 `coeffDetail`/`damageTakeMul`/`timeCoeffOf`）；功能类模块配色改绿；EMP 明细「清空」展示 | ✔ 已确认；建议 git 备注：`M2.4 功能型模块四件 + 数值统一结算阶段与 UI 收尾` |
| 2025（日期待补） | M2.4 | **放缓时间 C21 实装 + 时间系数重构**：时间扭曲/放缓统一为**同一词条 `time_coeff`**（负＝加速 / 正＝放缓；C18 用 −0.1、C21 用 +0.1）——语义改为**缩放“需求量”**而非每 tick 推进量：`需求量 = max(0, round(基础量 × (1 + 时间系数)))`、`剩余 = 需求量 − 已推进 tick 数`，**每 tick 恒推进 1**；作用于模块持续时间（`durElapsed`）、模块冷却（`cdElapsed`，钳 0）、临时单位存在时间（`tempLifeElapsed`/`tempLifeNeed`），9 处启动/重置点统一归零 elapsed；`timeCoeffMods` 派生 `timeCoeff`（**组合＝正、负两向各取绝对值最大者再求和**：同向不叠加、异向相抵），唯一读口径 `timeCoeffOf`，整数化 `timeScaled`；Pass1 只记账（`timeOps`）+ 快照 `u._timeCoeffTick`，**结算步骤 2** 落地、**下一 tick 起**生效，四条撤销路径对称；低频战报按系数符号选侧（`hastenStart/hastenEnd`、`slowStart/slowEnd`）；详情页「时间系数」（缺省 0、加速 −0.1、放缓 +0.1），模块摘要两行合并为一行 | ✔ 已确认；建议 git 备注：`M2.4 时间系数（加速/放缓统一词条）+ 放缓时间模块` |
| 2025（日期待补） | M2.4 | **辐能过载 C20 实装 + 详情页模块信息精简**：新词条 `hp_below_activate`（低血触发门控，`0.2`＝血量 ≤20% 才可激活；结构标记 `inst._firedOnce`、血量回升过阈值即清除 ⇒ 一个低血区间只触发一次）；新 `type` 标签 `delayed_trigger`（**后触发**：激活不出效果，持续期**自然到期**那一刻由 `fireDelayedEffect` 按既有 `__pending.dmg` 记账并走既有 `settleHits`→`applyHit`→战报链路，提前结束清载荷不触发）；作用集合与出伤数值在激活瞬间冻结（复用 `effectSetOf`）；模块 `js/data/modules/function/overload.js`（**功能性分类**）（`type:['delayed_trigger','blast','explosive']`、占位数值）；**详情页模块行只保留数值**（删除 `type` 标签派生说明句与括号解释，状态词保留极简形式） | ✔ 已确认；建议 git 备注：`M2.4 辐能过载模块（低血后触发爆炸）+ 详情页模块信息精简` |
| 2025（日期待补） | M2.4 | **潜行 C23 实装 + 辐能过载归入功能类 + 门控状态显示**：新 `type` 标签 `stealth`（激活后在**结算阶段**给目标单位加结构标记，来源记账 `ship.stealthMods`/派生 `isStealth`/唯一读口径 `isStealthed`）；被标记单位**不能成为主要攻击目标**——唯一口径 `stealthBlocksTargeting` 同时落到 `moduleTargetList`（锁定分支之后的候选池）与 `shipEffectiveTarget`（强制/船 `targetId`/策略队首），并同步 `fleetPreview` 与 UI 候选列表（`battle.targetableBy`）；**激活瞬间已锁定的目标豁免**、**溅射与伤害结算不受影响**、自身永远可选、全敌潜行时按既有规则不激活；模块 `js/data/modules/function/stealth.js`（自身单体、`energy_cap_target` 清空自身上限、占位数值）；辐能过载由 `attack/` **迁至 `function/`**（`category:'function'`）；新增引擎判据 `battle.moduleGateMet(inst)`，详情页/芯片/单位卡在**门控未满足时显示「条件未满足」**（不再显示就绪） | ✔ 已确认；建议 git 备注：`M2.4 潜行模块（不能成为主目标）+ 辐能过载归类 + 门控状态显示` |
| 2025（日期待补） | M2.4 | **欧米茄导弹发射器 C22 实装**：`js/data/modules/function/omegaMissileLauncher.js`（功能类；冷却 320/耗能 750，明显高于导弹的 220/500，占位）+ 内部模块 `js/data/modules/attack/omegaMissileWarhead.js`（伤害 320→3160、`blast_range` 2→5、引信 90→70，均高于导弹爆炸；`picker:false`）；召唤物用**通用 `drone` 模板 + `attrs` 全条目覆写**（遵守“勿在 ships.js 堆叠船型”铁律），带护盾（`shieldCap` 60）且**登场满盾**，`maxSummoned:2` 按模块计数；**无 `bind_target` ⇒ 不锁定目标**（爆炸模块每次激活按正常目标链重解析，粘性目标失效即改选）；**未新增任何词条/标签、引擎零改动**（全部由既有 `summon`/`cool_first`/`blast`/`explosive`/`damage`/`blast_range`/`self_destruct_damage` 等数据表达）；UI 召唤类数值行的召唤物名口径改为优先读 `summon.attrs.nameKey`（不再显示通用模板名“无人机”） | ✔ 已确认；建议 git 备注：`M2.4 欧米茄导弹发射器（召唤不锁定、带盾、上限2）` |
| 2025（日期待补） | M2.4 | **增幅器三件 C24 实装 + 欧米茄系图标**：`reactorCoil`（反应堆线圈·功能）、`shieldBattery`（护盾电池·护盾）、`hullArmor`（船体装甲·功能）——**仅对自身、无冷却/无持续/无耗能**的常驻增幅；复用 `shield_cap_bonus`，新增自身词条 `hp_cap_bonus`/`energy_cap_bonus`/`energy_regen_bonus`（均乘类别系数）与 `type` 标签 `passive`（跳过激活流程、UI 显示「状态型」走 `moduleEffective` 判据）；**唯一取值口径** `ship.js selfStaticBonus`、**唯一落地口径** `syncHullCaps`（非累加，仅结构变化时重算；`recomputeCap` 与 `enable/disableModule` 已改走该口径，避免抹掉常驻加成与电磁脉冲叠加）；上限提升**只钳制不白送**、登场满血满能量用 `fillHullVitals`（与满盾同体例）；**无战报**。另：新增绿色图标 `assets/img/欧米茄导弹.svg`、`欧米茄导弹发射器.svg`（既有 `导弹.svg` 的改色副本，`#7ee29a`），并把欧米茄导弹/发射器的 `icon` 指向新文件 | ✔ 已确认；建议 git 备注：`M2.4 增幅器三件（常驻自身增幅）+ 欧米茄系图标` |
| 2025（日期待补） | M2.4 | **护盾电池改造 + 回收利用实装**：护盾电池改为**功能性**（文件迁至 `function/`，旧路径占位待删），效果改为 `shield_coeff_add` **护盾系数加性（+0.1）** + `energy_cap_bonus` **负值削减能量上限（−100）**（新增 `syncStaticCoeffs` 常驻静态系数路径，写入同一张 `coeffMods` 表、**显式排除 `solo`** 以免把条件增益写死；`syncSelfStatics` 统一入口；上限下降即时钳制当前能量）；新增功能性模块**回收利用 `recycle`**（常驻被动）：**场上任何单位阵亡（敌我合计、排除召唤物）→ 下一 tick 按“上一 tick 阵亡数”恢复自身生命**（`hp_regen_per_death × 类别系数`），死亡计数唯一累加点＝`onDeath`、`step()` 收尾提交 `lastTickDeaths`、Pass1 只读快照（无反馈环、镜像对等），回血在**结算 4c** 经 `applyHpTo` 落地（钳到上限、不乘减免、不经护盾），**0 阵亡零变化**；低频战报 `recycleRegen` | ✔ 已确认；建议 git 备注：`M2.4 护盾电池改为功能类（护盾系数+/能量上限−）+ 回收利用模块` |
| 2025（日期待补） | M2.4 | **强辐线圈改名 + 能量输送实装**：①「反应堆线圈」显示名改为「**强辐线圈**」（en `Strong Radiation Coil`；`id`/文件/词条不变，显示处随 `nameKey` 自动同步）。②新功能性模块「**能量输送**」`energyTransfer`（`js/data/modules/function/energyTransfer.js`）：**单体、仅友方**（`kinds:['ally']` 天然排除自身，引擎与 UI 候选池同口径）、主动（占位 `cooldown_ticks`/`energy_cost`）——**消耗自身能量给目标加能量**：自身消耗走 `energy_cost`+`payEnergy`、目标增益走既有目标级量值词条 **`energy_target`**（该词条的**首个使用者**）；Pass1 只记账（`energySpends` + `energyDeltas`，零数值变化），**结算步骤 3 先扣后给**、给予经 `applyEnergyTo` **钳到目标能量上限**；**能量不足不激活、不进冷却**；复用既有 `canImpact` 口径——**目标能量已满时不激活**（不白耗自身能量；部分溢出则照常消耗并钳掉）；**零引擎/零 UI 代码改动、仅 1 个 i18n 键新增** | ✔ 已确认；建议 git 备注：`M2.4 强辐线圈改名 + 能量输送模块（消耗自身能量加给友方）` |
| 2025（日期待补） | M2.4 收尾 | **M2.4 功能类模块开发完毕（阶段收尾）**。本阶段落地模块一览：`emp` 电磁脉冲、`singleHanded` 单枪匹马、`impregnable` 固若金汤、`timeWarp` 时间扭曲、`slowTime` 放缓时间、`overload` 辐能过载（低血后触发爆炸）、`stealth` 潜行（不能成为主目标）、`hullArmor` 船体装甲、`reactorCoil` 强辐线圈、`shieldBattery` 护盾电池（护盾系数＋/能量上限−）、`recycle` 回收利用（按上一 tick 阵亡数回血）、`energyTransfer` 能量输送（耗自身能量加给友方）；配套引擎能力：**Pass1 零数值变化 + 结算阶段跨单位固定顺序**、`effectSetOf` 作用集合唯一实现、目标优先级链单一口径（含潜行过滤与锁定豁免）、`hp_below_activate`/`delayed_trigger`/`stealth`/`passive` 标签、`time_coeff` 时间系数、自身常驻静态加成 `syncSelfStatics`（系数加性 + 三围/恢复，非累加、结构变化才重算）；新增词条 `attack_coeff_add(_target)`、`damage_coeff_mul(_target)`、`time_coeff`、`hp_below_activate`、`hp_cap_bonus`、`energy_cap_bonus`、`energy_regen_bonus`、`shield_coeff_add`、`hp_regen_per_death`；战报新增 EMP/强制目标/时间加速·放缓/潜行/回收利用等低频条目，全部遵循“效果结束带模块拥有者、按事件聚合、不刷屏”规范；UI 详情页「单位系数」栏（可折叠、只放数值）与模块行数值化改造 | ✔ 已确认；建议 git 提交信息（一次性提交本次全部变更）：`M2.4 功能类模块（12 个）与引擎结算范式、UI 与文档同步` |
| 2025（日期待补） | 船型/等级 | **船型拆分 + 船等级系统**：船型改为**一个文件一个单位**、统一放 `js/data/ships/`（`combat.js`/`transport.js`/`mining.js`/`drone.js`），`js/data/ships/index.js` 提供注册表与**等级解析唯一口径**（`SHIPS`/`SHIP_IDS`/`getShip`/`shipMaxLevel`/`shipLevels`/`resolveShipAtLevel`），旧 `js/data/ships.js` 改为**纯转发层**（保持既有导出名与形状 ⇒ **全部调用点零改动**）；船等级模型与模块**完全同构**：`maxLevel` + `levels[]` **逐级绝对表**、未填条目**深合并回退上一级**（算法与模块统一，`core/utils.js` 新增 `deepMerge`），**可逐级替换任意条目**（三围 / 类别系数 / 模块槽位 / `nameKey` / `icon` / 将来新增字段，无白名单）；解析时机＝`createShip` 创建实例时**解析一次成为实例基准值**（`ship.level`/`ship.slots`/`ship.typeCfg`），其后派生链（`recalcDerived`/`ensureBasePool`/`syncHullCaps`/`syncStaticCoeffs`）均基于该基准；**召唤物 `attrs` 覆写仍在等级解析之后合并、优先级最高**（drone 无 `levels`，行为不变）；新增 `applyShipLevel(ship, level)` 供后续升级功能接入；**默认等级 Lv1 逐项核对零数值变化** | ✔ 已确认；建议 git 备注：`M2.5 船型拆分（一单位一文件）+ 船等级系统（与模块同构、任意条目可逐级替换）` |
| 2025（日期待补） | UI/接口 | **编队配置拆分 + 唯一开战接口 + 战斗屏调整**：①演练编队配置逻辑**整体分离**为新文件 `js/ui/setupView.js`（只做编队编辑与开战前预检，回调注入、不 import `battleView`，无循环依赖；`battleView` 移出约 164 行、只保留运行态）；②**唯一开战接口** `battle.js startBattle(formation)`（配套纯函数 `normalizeFormation`：未知船型丢弃、等级与模块等级钳制、**按该等级槽位截断模块**、返回 `warnings`；任一方为空 → `ok:false` 且不创建战斗），UI 侧入口 `battleView.enterBattle(formation)`；演练「开战」、结算「再来一场」与调试台全部改走它，`createBattle` 全仓仅被 `startBattle` 调用（**第二条开战路径已消除**）；校验口径唯一（UI 预检与引擎开战共用 `normalizeFormation`）；③**单位定位标识 `role`**（`'combat'`/`'logistics'`）：默认值来自**船型数据**（transport/mining＝logistics，combat/drone＝combat，可被 `levels[]` 逐级覆写），编队条目可覆盖（经既有 attrs 覆写落到实例 `ship.role`；**引擎不读该字段、对战斗行为零影响**），UI 只读 `ship.role` 决定进战斗栏或后勤栏，敌我同一函数严格对称；④战斗屏改为**四栏**（敌方后勤 → 敌方战斗 → 我方战斗 → 我方后勤），**空栏自动隐藏**（按单位卡数＝0 唯一规则，roster 变化时刷新）；⑤**平局显示**（双方全灭 → `result='draw'`，UI 三态 `win/lose/draw`，与“同 tick 互毁＝平局”同口径，引擎无需改动）；⑥**「离开」按钮**（战斗屏标题行 + 结算浮层），`leaveBattle()` 完整清理（停战斗与退订 tick、清运行态引用、关浮层、恢复 tick 速度与暂停、清护盾条变色状态机、重挂配置屏），可反复进出；⑦**单位卡紧凑化**（卡宽 220→168、图标 54→36、条高 7→5、模块格 22→17、字号整体降 1–2px）+ 新增 ≤900px/≤640px 小屏适配；船型可选列表改为 `SHIP_IDS` 动态生成（`drone` 以 `picker:false` 排除） | ✔ 已确认；建议 git 备注：`M2.5 编队配置拆分 + 唯一开战接口 + 战斗屏四栏/平局/离开/紧凑卡` |
| 2025（日期待补） | 单位定位 | **按 role 分离目标选择与溅射队列**（用户确认口径）：`role ∈ {'combat','logistics'}`——**船型文件独立字段给默认值（缺省＝战斗单位）**、**编队条目可覆盖**，落到实例 `ship.role` 唯一口径（`createShip`/`applyShipLevel`/`attrs` 覆写三点均核对）；**`enemy` 类目标受限**：敌方**仍有存活战斗单位**时不可选敌方后勤单位，战斗单位全灭后正常可选；**`self`/`ally`/`any` 不受限**；**溅射按主目标所属 role 队列分离**：`blast_range` 邻接只取**与主目标同侧、同 role** 的视觉顺序队列（战斗→只在战斗队列、后勤→只在后勤队列，**不跨 role**），`effectSetOf` 与即时爆炸循环**共用同一取法** `orderedQueueFor(primary)`；**锁定豁免照旧**（`lockTargetId`、`_lockIds` 在过滤前直接返回，role 分离无法推翻既有锁定）；**潜行过滤与 role 过滤在同一处叠加**（`targetAllowed(ship,u,kind)`，唯一过滤点；UI 只传桶名、零自算）；无可用目标按既有规则**不激活、不报错**；**默认无后勤单位时逐位零变化**（队列≡原整条阵营队列，全部「伤害+`blast_range`」模块均为 `kinds:['enemy']`） | ✔ 已确认；建议 git 备注：`M2.5 按 role 分离目标选择与溅射队列（后勤单位后排规则）` |
| 2025（日期待补） | 货仓/召唤 | **潜行叠加修正 + 召唤继承系数 + 货仓容量（货物/矿物）**：①**role 门控判据改为“是否存在「可选」战斗单位”**（`roleBlocksInFoes`：`alive && role==='combat' && 未被潜行屏蔽`）——「已潜行战斗单位 + 未潜行后勤单位」时**后勤解禁、`enemy` 可选后勤**，消除候选池为空的死局；潜行语义不变、UI 同口径；②**召唤物继承召唤者系数**：召唤瞬间把召唤者**当期 `coeff(ship,类别)` 结果**（含模块加性/乘性）**快照**写入召唤物 `coefficients`（与 `attrs` 同一覆写链、**全类别继承**），**此后不再同步**（效果失效不影响已召唤单位）；③**货仓容量词条** `cargo_cap_bonus`（货物）/ `ore_cap_bonus`（矿物）（自身静态加成，与 `hp_cap_bonus` 同族）；**唯一口径** `cargoCapacityOf`/`oreCapacityOf`/`cargoCapPartsOf`/`oreCapPartsOf`/`cargoLoadOf`/`oreLoadOf`，公式 `总量 = round(本体容量 + Σ(各来源 × 对应类别系数))`——**货物乘运输系数、矿物乘采矿系数，本体容量（`base.cargoCap`/`base.oreCap`，可逐级覆写）不受系数影响，各来源分别乘系数后再求和，取整只发生一次**；④新增实测载体模块 **`cargoHold` 货舱（运输类，C25）** 与 **`oreHold` 矿舱（采矿类，C34 语义）**；⑤**UI**：单位卡与详情新增同 `.bar` 体例的**货物/矿物容量条**（分色、详情条 `title` 显示本体/模块分解），**指挥栏在共享护盾条同一处新增货物/矿物总量行**（引擎阵营合计、只显示我方、与共享护盾条同做法），**容量为 0 一律隐藏** | ✔ 已确认；建议 git 备注：`M2.5 货仓容量（货物/矿物，本体不乘系数）+ 召唤继承系数 + role 门控潜行叠加修正` |
| 2025（日期待补） | 星区/采矿 | **不可停用标签 + 星区（场景命名/矿物储量/星区资源栏）+ 详情栏上移 + 采矿激光**：①新增 `type` 标签 **`undeactivatable`**（不可停用，按标签识别）：`disableModule` 首行**拒绝任何停用请求**，UI 走引擎判据 `battle.moduleUndeactivatable` **灰显开关并给出原因提示**；应用于 **`cargoHold` 货舱、`oreHold` 矿舱**；②**星区**：数据 `{ name, oreReserve }`（名称自定义原样显示、储量非负整数），缺省登记 `js/data/sector.js`（缺省名空串、缺省储量占位 5000），**唯一钳制口径 `normalizeSector()`** 随 `normalizeFormation`/`startBattle` 传入；只读口径 `battle.sector`（剩余/初始），**战斗场景以星区命名**（战斗标题 + 结算面板）；**指挥栏下方新增「星区资源栏」**（复用指挥栏行体例，剩余/初始；无数据整栏隐藏、储量 0 该行隐藏）；**编队界面新增星区设定**（名称输入 + 储量输入，`/^\d+$/` 校验、非法提示并禁用开战）；③**单位详情栏移到指挥栏上方**（仅 DOM 顺序，CSS 零改动、响应式与交互不变）；④**采矿激光** `miningLaser`（采矿类，**无需目标**、不进目标链）＋新词条 **`ore_gain`**：实采量 `round(ore_gain × 采矿系数)`、按**剩余矿物容量**截断、**容量或储量 0 则不激活**；Pass1 只记 `__pending.oreGains`，**结算步骤 3b** 统一分配入库（不足时**按请求量比例均分**：floor + 余数按“请求量大者优先”逐个 +1 ⇒ **整数、不超发**）；入库 `hull.ore`、星区扣减＝实际入库总量；**单位阵亡 → 携带矿物全额返还星区（幂等，只返矿物）**；采矿**不新增战报**（高频事件） | ✔ 已确认；建议 git 备注：`M2.5 星区（矿物储量/资源栏/编队设定）+ 采矿激光 + 不可停用标签 + 详情栏上移` |
| 2025（日期待补） | 星区/模块 | **矿物压缩 + 创世纪 + 矿藏富集（含星区冷却）**：①**矿物压缩** `oreCompressor`（采矿类，与护盾电池同体例的常驻自身增幅）＝新词条 **`mining_coeff_add`**（自身·采矿类别系数加性，登记进 `COEFF_ADD`）；靠 `_coeff_add` 后缀规则自动落地，**`solo` 排除未动**；**无需派生重算**（采矿系数影响面＝矿舱容量模块部分与采矿激光实采量，皆按需读取无缓存）；②**创世纪** `genesis`（采矿类）：新词条 **`sector_ore_add`**＝**星区剩余储量绝对增量（+100）**；③**矿藏富集** `oreEnrichment`（采矿类）：新词条 **`sector_ore_mul`**＝**增量比例（0.1 ⇒ 当前剩余 ×1.1）**，**作用于当前剩余**、每条**各取整一次**；两词条**既非自身也非目标词条，作用对象＝星区**（登记 `SECTOR_WORDS`，识别 `sectorWordOf`）；④**结算新增步骤 3c**（3b 采矿之后、判死之前）：**先加法**（求和后一次性加入）→**再乘法**（逐条作用于当前剩余、逐次取整），Pass1 只记 `__pending.sectorOps`（零数值变化）、不按 `alive` 门控；**星区储量无上限**（4 个写点已核对无封顶；“剩余 ≤ 初始”守恒句已修订，`oreReserveInit` 为初始快照）；⑤**混合冷却**：实例自身冷却照旧 + **星区侧按模块 id 独立冷却** `sectorCdUntil`（绝对到期 tick、无逐 tick 递减），**门控落在 `canImpact`**（未就绪→不激活不耗能不进冷却），**同 tick 同模块只接受第一个**（固定顺序 allies→enemies）；`moduleGateMet` 扩展为星区冷却未结束→「条件未满足」；⑥**UI**：星区储量条后**自动枚举**星区词条模块各一行冷却（模块名 + 剩余 tick），**仅冷却中显示、就绪隐藏、无星区整组隐藏**（CSS 零改动）；⑦占位：`mining_coeff_add` +0.1/+0.15/+0.2/+0.3；`sector_ore_add` +100/+200/+350/+600；`sector_ore_mul` ×1.1/×1.15/×1.2/×1.3；冷却 60t、耗能 25 | ✔ 已确认；建议 git 备注：`M2.5 矿物压缩（采矿系数）+ 创世纪/矿藏富集（星区储量加减乘 + 星区冷却）` |
| 2025（日期待补） | 星区/战报 | **三个采矿系战报 + 星区冷却词条化 + 冷却 UI 移位 + 创世纪乘采矿系数**：①新增战报 `battle.log.sectorOreAdd`（`{owner}的{module}：星区矿物 +{n}`，仅实际增量 > 0）、`battle.log.sectorOreMul`（`…星区矿物 ×{mul}（+{n}）`，记**实际乘数**与该条**落地前后差值**，仅真正改变储量时记）、`battle.log.miningGain`（`…采集 {n} 点矿物`，**仅实际入库量 > 0**，**按模块实例聚合**成本 tick ≤ 1 条，上限＝该 tick 单位×模块实例对数）；三者均在**结算阶段**落地（采矿 3b 末尾、星区 3c 内），`ownerTok` 着色 + `modTok` 绿字，属低频事件（受星区冷却限频）；②**星区冷却词条化**：新增独立词条 **`sector_cd_ticks`**——**词条存在即代表该模块参与星区冷却**（**按词条识别、不硬编码模块 id**；唯一识别口径在 `js/data/sector.js`：`SECTOR_CD_WORD`/`sectorCdTicksFx`/`hasSectorCdFx`，引擎 `hasSectorCd`/`sectorCdTicksOf`，UI 同函数），**星区侧冷却时长改取该词条**（不再取 `cooldown_ticks`），与实例自身冷却**同算法、两把需同时就绪**；门控仍在 `canImpact` 唯一出口且**提前到 `ore_gain` 分支之前**（杜绝绕过）；创世纪/矿藏富集各加 `sector_cd_ticks: 60`，其它模块未加；③**冷却 UI 移位**：星区栏改为「**冷却组（储量条上方、独立成组，小节标题 `battle.sector.cdTitle`「星区冷却」）+ 储量条**」，行集合＝带该词条的模块**自动枚举**，每行仅冷却中显示、就绪隐藏、**组内全就绪整组隐藏**、无星区数据整栏隐藏；CSS 仅新增 `.sector-cd-group` 两行；④**创世纪增量 × 采矿系数**：`round(词条值 × coeff(拥有者,'mining'))`，**在 Pass1 记账时取系数快照并取整一次**（与采矿激光 `ore_gain` 同口径同位置；不可放到 3c，因同 tick `coeffOps` 会改写该量），3c 只做求和；拥有者 3c 前阵亡则意图/冷却/战报照常落地；**矿藏富集仍为纯比例、不乘系数** | ✔ 已确认；建议 git 备注：`M2.5 星区冷却词条化（sector_cd_ticks）+ 采矿系战报 + 冷却 UI 移位 + 创世纪乘采矿系数` |
| 2025（日期待补） | 矿物模块 | **矿渣导弹发生器 + 专用弹头 + 矿物输送**：①**矿渣导弹发生器** `slagMissileLauncher`（功能类，召唤型）＝**与欧米茄同类同参数**（`lifespan_ticks:500`、`maxSummoned:2`、`temp:true`、`drone` 模板 + `attrs`、不写 `bind_target`/`projectile`），**不消耗能量**（不写 `energy_cost`）、**消耗单位自身携带矿物**（新词条 **`ore_cost`** 占位 500/550/600/650）；**专用爆炸模块** `slagMissileWarhead`（攻击类、`picker:false`，**固定占位伤害 4740** ＝欧米茄 3160×1.5、`blast_range:5`、`cooldown_ticks:70`）；成本门控在 Pass1 成本门控处（不足→不激活、不扣矿、不进冷却），扣减走**新结算步骤 3d-1**；**同单位多模块共用单位内预算 `ctx.oreAvail`**（与矿物输送共用）⇒ 总支出 ≤ tick 起始携带量；该模块**必须装在能携矿的单位上**（否则携带恒 0 → 永不激活）；②**矿物输送** `oreTransfer`（功能类，单体仅友方）：**消耗能量**（`energy_cost`+`payEnergy`+`ctx.avail` 门控），**自身矿物 1:1 送目标**（新目标级量值词条 **`ore_target`**，登记进 `AMOUNT` 表，落地**单独成支、不乘任何系数**）；三条门控全在 `canImpact`（**放在通用量值循环之前**）：自身不足→不激活、**目标已满→不激活不耗能**、容量不足但 >0→按 `min(词条值, 自身剩余携带, 目标剩余容量)` 截断；转移记录只写施放方 pending（`__pending.oreTransfers`）→ **结算步骤 3d-2** 原子落地（一条＝自身 −N/目标 +N 同额、不重复计数）；③**步骤全序**：3（能量）→ 3b（采矿）→ 3c（星区）→ **3d（3d-1 成本 → 3d-2 输送）** → 4（护盾/血量/判死），三者写入对象互不重叠、3d 在 3b 之后（新采矿物可即时用于成本）且**判死全在 3d 之后**；④**UI**：含 `ore_cost` 的模块成本列显示「耗矿 500 · 每 600t」（既有模块走原整句键，**零回归**），矿物输送显示「目标矿物 +50」；新增 9 个 zh/en 成对键、无 CSS 改动；⑤**均未写 `icon`**（降级为首字/▲，未新增美术）；**战报暂不新增**（矿渣导弹复用既有 `battle.log.summon`，矿物输送与能量输送同口径不记） | ✔ 已确认；建议 git 备注：`M2.5 矿渣导弹发生器/专用弹头（耗矿不耗能）+ 矿物输送（1:1 搬运）+ 结算步骤 3d` |
| 2025（日期待补） | 采矿系 | **采矿系收口：分类/代表色/图标 + 矿物输送战报 + 矿物维修**：①**分类改采矿**：`slagMissileLauncher`、`oreTransfer` 的 `category` 改为 `'mining'`、`modules.js` 注册分组同步（文件**已由用户手动移动到 `js/data/modules/mining/`**，目录与分类一致、引用已核对无遗漏；弹头仍为攻击类）；**分类改动对数值零影响**（逐点核对：`ore_cost` 读原值、`ore_target` 1:1 单独成支、冷却/时长走**时间系数**、弹头伤害走弹头自身 attack 类别 × 召唤单位攻击系数——采矿船 mining 系数最高 1.6 也不受影响）；②**采矿类代表色＝紫**：新增 CSS 变量 `--cat-mining: #b06bff`，`.module-chip.mining` 改用变量（唯一分类色定义处，单位卡与详情页同变；**其余五个分类逐字未动**）；资源色 `--ore` 仍为绿（语义色，注释已更正）；③**图标**：美术新增紫色 `assets/img/矿渣导弹.svg`、`矿渣导弹发射器.svg`（由欧米茄图标**复制改色** `#7ee29a`→`#B06BFF`、几何未改），模块 `icon` 与 `summon.attrs.icon` 已接线；④**矿物输送新增战报** `battle.log.oreTransfer`（`{owner}的{module}：输送 {n} 点矿物给 {target}`，仅实际量 > 0、每模块每 tick ≤ 1 条、成句在**结算 3d-2** 与数值同批）；⑤**新模块「矿物维修」`oreRepair`**（采矿类）：主动、`cooldown_ticks`+`energy_cost`+`ore_cost`，**单体仅友方且含自身**（`kinds:['ally','self']` 既有组合、默认目标是自己），回血 `hp_target` **按原值不乘系数**（新 `type` 标签 **`exact_amount`**，仅作用于目标级量值、当前唯一使用者、对既有模块零影响）；三条门控（矿物不足/能量不足/**目标满血**）均不激活不扣矿不耗能；扣矿走 3d-1、回血走步骤 4c ⇒ **先扣矿后回血**；占位 `ore_cost` 30/55/90/140、`hp_target` 60/110/180/280、`energy_cost` 60/90/130/180、冷却 60t；**默认不加战报**（如需在 4c 按实际回血量成句） | ✔ 已确认；建议 git 备注：`M2.5 采矿系收口（分类/紫色/图标）+ 矿物输送战报 + 矿物维修模块` |
| 2025（日期待补） | 采矿系收尾 | **采矿系开发收尾（用户确认符合测试期望）**：①**矿渣/矿物输送文件已由用户手动移动到 `js/data/modules/mining/`**（分类与目录一致，`modules.js` 引用核对无遗漏；顺手修正移动后残留的过期注释）；②**矿物维修新增战报** `battle.log.oreRepair`（zh `{owner}的{module}：消耗 {n} 点矿物，修复 {target} {amount} 点生命`）：**判据全部取自本 tick 实际事实**（① 实际回血 > 0＝`applyHpTo` 前后差值、含 `hpMax` 截断 ② 该实例本 tick **实际扣矿 > 0**，由 3d-1 写回 `_orePaidAmt`/`_orePaidTick` 并做 tick 号校验 ⇒ 将来任何“耗矿换血”模块自动获得该战报、不硬编码模块 id；既有「回收利用」因无 `ore_cost` 天然不重复），`n`＝**实际扣矿量**、`amount`＝**实际回血量**；**每模块每 tick ≤ 1 条**（本 tick 局部 Set 去重）、成句在**结算步骤 4c** 与数值同批；因触发已保证 `n ≥ 1`，不设“0 的第二套文案”（单键单口径）；③**模块筹码缺图回退**：`moduleGlyphEl` 抽出唯一降级分支 `firstCharEl()`，`<img>` 加 `error` 监听后 `replaceWith(首字节点)`——与“无 `icon`”分支**共用同一渲染/同一类名**、与召唤单位 `unitIcon` 同一写法体例，**原位替换**不重复插入、不残留破图，**正常路径逐字不变、无新依赖**；④欧米茄发射器与欧米茄导弹**共用 `assets/img/欧米茄导弹.svg`**（保持现状，用户裁决；清单早期“新增发射器图标”的记录以本行为准） | ✔ 已确认；建议 git 提交信息（一次性提交本次全部变更）：`M2.5 采矿系（采矿/星区储量/矿物成本与搬运/矿物维修）+ 分类紫色与图标 + 结算步骤 3b~3d` |
| 2025（日期待补） | 货物（实体化） | **货物体系第一轮：实体化 + 星区货物栏 + 点击优先队列**：①**新增 `js/data/cargo.js`**（体例同 `data/sector.js`，数值集中登记）：**通用模板类货物**字段＝`nameKey`/`name`（自定义名、原样显示不 i18n，空串回退模板名）/`type`/`tons`（**吨位＝占据货仓值，默认 1 个货物 5t**）/`loadTicks`（**装载需要时间**）/`level`（**等级**）/`bonus`（**加成系数**，本轮**仅数据承载**）；缺省 `CARGO_DEFAULTS`、**唯一上限来源 `CARGO_LIMITS`**（引擎钳制与编队校验共用）；模板 `general` 通用货物 5t/30t、`crate` 标准货箱 5t/60t、`container` 重型货柜 20t/180t（后两者占位）；②**星区中以个体形式存在**：每件货物＝**独立实体实例**（**不是数值累积**），**id＝`cargo-<序号>`**（序号＝规范化后位置、1 起 ⇒ **确定性、可复现**，UI 预检与开战得到同一组 id）；③**规范化**：`normalizeSector(raw, warnings?)` 扩展出 `cargos`（**仍为纯函数、不改入参**），新导出 **`normalizeSectorCargos`**（模板缺省/未登记丢弃、`count` 展开为 N 个实例、逐字段覆写+钳制、总数 ≤40 截断），5 个 `side:'sector'` 告警码（`cargoInvalid`/`cargoUnknownTemplate`/`cargoCountClamped`/`cargoClamped`/`cargoOverflow`）；④**只读口径** `battle.sector.{cargos(含 queued/queueIndex), cargoQueue}`（`queueIndex` 1 起、未入队 0，每次读取新对象）；**唯一切换接口 `battle.toggleCargoQueue(id) → {ok,queued,queueIndex}`**（未入队→**追加队尾**、已入队→**移除**且其余相对顺序不变；`ok:false`＝id 不存在），**UI 不自算队列**；⑤**运输类标识＝亮黄 `--cat-transport: #ffd24a`**（原 `#ffe9a8`；模块芯片/星区货运行/编队货物小节**共用同一变量**；其它分类逐字未动、采矿紫不变；资源语义色 `--cargo` 橙未改）；⑥**编队界面**：星区设定面板内新增货物小节（模板下拉 + 数量批量添加 → 展开 N 条独立条目，逐条改名称/类型/吨位/等级、可移除；**装载时间与加成系数本轮不可编辑**；吨位/等级非法 → 红框 + 提示 + **禁用开战**；提交 `Formation.sector.cargos`＝已展开的实例数组）；⑦**战斗屏**：星区资源栏内新增「**星区货物**」独立区块（行复用指挥栏行体例：`{name} · {type} · {tons}t · Lv{level}` + 右侧**队列序号 `#{n}`**；**点击入队、再点取消**，已入队亮黄高亮；**无货物整组隐藏**；随星区栏每 tick 刷新）；⑧**本轮明确未实现**：无装载计时、无运载进程、**不占用任何单位货仓**、不写 `__pending`、`step()` 中无货物变量 ⇒ **Pass1 零数值变化**；下一轮预留队列顺序、实例字段与 `battle.cargoPool(side)` 货仓容量口径 | ✔ 已确认；建议 git 备注：`M2.5 货物体系第一轮（货运实体化/星区货物栏/点击优先队列）+ 运输类亮黄` |
| 2025（日期待补） | 货物（第二轮） | **货物体系第二轮：6 种零件货物 + 一货一文件 + 星区栏横向重排**：①**一类货物一个文件**：新增 `js/data/cargos/{none,weaponPart,fieldComponent,functionDevice,droneDebris,miningRig,freightBlueprint,index}.js`（注册表 `CARGOS`/`CARGO_IDS`/`getCargo`/`cargoMaxLevel`/`cargoLevels`/`resolveCargoAtLevel`，逐条对齐 `data/ships/index.js`）；`js/data/cargo.js` 改为**纯转发层＋缺省值/上限常量**（不留第二套定义）；旧三件套占位模板为数组项、无独立文件需删；②**6 种模块零件货物 + None**：武器零件（攻击粉）/力场组件（护盾蓝）/功能设备（功能绿）/**机骸碎片（无人机棕 `#b07a4a`，本轮由 `#ffd1a8` 改）**/采矿器械（采矿紫）/货运蓝图（运输亮黄）+ None（**默认类型·灰**，复用 `--text-dim`）；`colorKey` **只存变量名、货物文件零色值**，色值唯一来源 `css/base.css :root`（攻击/护盾/功能亦已变量化、取值未变）；③**货物等级解析＝与模块/船型同构**（`maxLevel`+`levels[]` 逐级绝对值、升序 `deepMerge` 继承、输出删 `levels`、钳制到 `[1,cargoMaxLevel]`，额外写回 `out.level`；**解析只在实例创建/规范化一次**，编队界面同一函数）；占位统一 `tons:5`/`loadTicks:300`/`bonus:1`/`maxLevel:16`；④**星区栏横向重排**：`〔标题〕→〔.sector-line：冷却组｜货物组 并排〕→〔储量条整行〕`，货物组 `flex:1 1 60%` **预留足够宽度**、**体例与目标选择器 UI 相近**（容器同 `.mod-target-ctl`、芯片同 `.target-chk`），**≤900px 断点纵向堆叠**、不溢出；⑤**货物品＝小芯片网格**：**不显示队列序号**（`queueIndex` 数据层保留、UI 只读 `queued`），内容＝`名称 · 吨位t · 装载秒数`（等级 >1 才附 `Lv`）；**边框默认即有、颜色由类型决定（None 灰）**，**选入队列 ⇒ 边缘发光+内部填充**（`color-mix` 派生，原生 CSS 无新依赖）；⑥**装载时间换算秒的唯一口径**：`js/core/tick.js` 新增 `TICK_MS`/`TICK_SECONDS`/`ticksToSeconds`/`formatTickSeconds`（0.05 只出现一处，**300t→15s**），两界面均只调该函数；⑦**编队界面**：类型下拉由注册表动态生成、**类型不可编辑**（类型色芯片）、吨位/等级可编辑（上限＝该类型 `maxLevel`）、**装载时间只读**随等级重算；**提交结构收紧为 `{ templateId, name, tons, level }`**（其余由引擎唯一解析落地，杜绝第二口径）；`CARGO_LIMITS` 精简为 `{nameLen:40,maxTons:9999,maxCountPerEntry:20,maxTotal:40}`（**取消统一等级上限**，改由各类型 `maxLevel` 决定），告警 `cargoClamped.field ∈ tons|level`；⑧**铁律**：货物/队列仍为星区侧纯状态（`step()` 不出现、不写 `__pending`、**不占货仓**）⇒ **Pass1 零数值变化**；**装载/运载结算仍未实装** | ✔ 已确认；建议 git 备注：`M2.5 货物体系第二轮（6 种零件/一货一文件/等级解析/星区栏横向芯片布局）+ 无人机改棕` |
| 2025（日期待补） | 货物（显示调整） | **货物品（芯片）显示调整**：①**字号调大并与目标选择器统一**：新增全局变量 **`--chip-font-size: 11px`**（`css/base.css :root`），**`.target-chk`（目标选择器按钮）由字面量 `11px` 改读该变量（渲染值不变、零视觉变化）**，`.cargo-chip` 与编队 `.drill-cargo-type` 同读该变量，并逐项对齐 `padding:1px 7px`／`border-radius:6px`／`1px` 边框／`var(--panel)`／`line-height:normal`（**无第二套字号**），加 `font-variant-numeric: tabular-nums`；②**显示优先序号**：**位于名称之前**、读引擎派生的 `queueIndex`（1 起、未入队为空），**定宽预留空位**（`.cargo-chip-seq`，`inline-block` 显式宽 `2.2ch`＝2 位数（实例上限 40）、**右对齐**、`margin-right:4px`）⇒ **入队/取消宽度与文字位置不跳动**；③**加成值在名称之后以百分比显示**：换算走**唯一函数 `formatBonusPercent(bonus)`**（`js/core/utils.js` 导出，体例同 `formatTickSeconds`：**只返回数字、`%` 由 i18n 提供**；公式 `(bonus−1)×100` → `Number(x.toFixed(1))`，四舍五入 1 位小数去尾 `.0`、负值不特判、非数值按 1），示例 `1→不显示`／`1.1→10%`／`1.15→15%`／`1.125→12.5%`／`0.9→-10%`；`bonus=1` 整段 `.hidden`；颜色沿用正文类型色，分隔符为 CSS `::before{content:'·'}`（语言无关、不占 i18n）；④**DOM 三段独立元素** `[.cargo-chip-seq][.cargo-chip-text][.cargo-chip-bonus]`（序号/加成**不硬拼进 i18n 模板**）；新增键 `battle.sector.cargoSeq`＝`'{n}'`、`battle.sector.cargoBonus`＝`'{v}%'`（zh/en 成对）；⑤其余未动：吨位/装载秒数/`Lv{n}`、类型色边框、选中发光+填充、点击切换队列、空态隐藏、铁律（货物仍为星区侧纯状态、不参与 tick、不写 `__pending`、不占货仓）；当前 7 类货物 `bonus` 均为占位 1 ⇒ **加成段暂不显示**，改数据即生效 | ✔ 已确认；建议 git 备注：`M2.5 货物品显示调整（字号对齐目标选择器 + 优先序号定宽占位 + 加成百分比）` |
| 2025（日期待补） | 货物（芯片结构） | **货物品芯片结构与高度调整**：①**六段独立元素、顺序固定**：`[.cargo-chip-seq 序号/占位] → [.cargo-chip-name 名称] → [.cargo-chip-bonus · 加成%] → [.cargo-chip-lv · Lv{n}] → [.cargo-chip-meta · 吨位t · 装载s] → [.cargo-chip-pad 末尾填充]`（**加成移到名称之后、Lv 之前**；吨位/装载移到 Lv 之后）；②**末尾对称填充** `.cargo-chip-pad` ＝**与序号列同宽的空占位**，**两者共用同一宽度口径**（`.cargo-chip` 上只声明一次 **`--cargo-seq-w: 2.2ch`**，改宽只改一处；两侧间距对称 ⇒「名称+内容」视觉居中）；③**高度固定 `32px`**：`.cargo-chip{display:inline-flex;align-items:center;height:32px;padding:0 7px}`（`border-box` ⇒ 含边框恰 32px；内容盒 30px、11px 单行上下各余 ≈8.4px ⇒ **不裁切不溢出**）；**高度只由固定高度决定、与字号行高无关**；**字号本轮未变**（`--chip-font-size: 11px`，`base.css` 零改动），`.target-chk` **本轮未触碰**（其高度仍 ≈17px，是否统一待用户裁决）；过长文字在 `name`/`meta` 段内省略号收尾，`seq`/`pad`/`bonus`/`lv` 四段 `flex:0 0 auto` 不收缩 ⇒ 定宽留白恒定、不溢出；④**i18n 改为分段**：删除拼串键 `cargoChip`/`cargoChipLv`，改为 `cargoSeq='{n}'`、`cargoBonus='{v}%'`、**`cargoLv='Lv{level}'`**、**`cargoMeta='{tons}t · {load}s'`**（名称段无模板、空串回退类型名）；**段间 `·` 由 CSS `::before` 提供** ⇒ 段隐藏时分隔符一并消失、不留孤立 `·`；⑤其余未动：序号在名称前且定宽不跳动（只读 `queueIndex`）、边框＝类型色（None 灰）、选中发光+填充、点击切换队列、`bonus=1` 不显示加成、空态整组隐藏、星区栏横向布局与 ≤900px 堆叠、铁律（货物星区侧纯状态、不参与 tick、不写 `__pending`、不占货仓） | ✔ 已确认；建议 git 备注：`M2.5 货物品芯片结构（六段顺序/末尾对称填充/固定 32px 高度/i18n 分段）` |
| 2025（日期待补） | 装载体系 | **装载光束模块 + 货物装载体系（第一版）**：①**模块** `js/data/modules/transport/loadingBeam.js`（运输类·亮黄、**无需目标**不进目标链、`target:{}`、**不写 `cooldown_ticks`** 故无自身冷却、`energy_cost` 占位 20）；**标签/词条边界**＝`type:['cargo_loader']`（**行为标签**：具备装载能力，引擎全部按标签判定、**无 id 硬编码**）＋ **`effects.cargo_load`**（**数值词条**：装载速度加成，占位 0.1，Lv5 0.2/Lv10 0.35/Lv16 0.5）；②**装载时间公式（唯一实现 `cargoLoadNeedTicks`）**＝**速度 `1 + cargo_load + (coeff(拥有者,'transport') − 1)`（加性）**、**时长 `max(1, round(货物装载时间 ÷ 速度))`**（取整与下限**只在这一处**、随意图冻结、结算阶段不重算；速度非正按 1 防除零）⇒ 默认 1.1 倍速、300t → **273t（≈13.7s）**，运输系数 1.2 ⇒ **231t**，已装过的 20t 货物 ⇒ **18t/15t**；③**对象选择**＝**优先队列队首最先 > 未入队者按星区列表顺序**，逐件判定、**不可用即跳过并试下一件**（已锁定／本 tick 已认领／**本舰剩余货舱 < tons**），全部不可用则**不激活不耗能不进冷却**；**一次只装一件**（`inst._load` 非空即忙）、同单位多装载器由 `ctx.cargoClaimed` 累加截断**不超装**；④**锁定/解锁**：启动即锁定（`inst._load`＋`cargo._loadBy`），锁定期间**不可被再次锁定**且 `toggleCargoQueue` **拒绝**（`reason:'locked'`）；解锁＝**停用／阵亡／离场／完成** ⇒ **解锁 + 进度归零、能量不退**；返还货物恒解锁；⑤**结算步骤 5（新增，Phase B2 后、Phase C 前）**：只读 tick 终态 ⇒ **拥有者本 tick 死亡则不启动/不完成、在装者解锁归零**（确定性、镜像对等）；三小步＝启动（锁定+冻结时长）→ 推进/解除（启动那 tick 不推进）→ 完成（存活且货舱容得下才入舱，容不下则保持锁定与进度等待）；同 tick 争抢同一件＝**allies 先到先得**；⑥**完成落地 `landCargoOn`（唯一写入者）**：货出星区+出队 → 入 `ship.cargos` 与 `hull.cargo`（`cargoLoadOf`/`cargoListOf` 同处同写）→ **`loadTicks` 永久改写为 `CARGO_FAST_LOAD_TICKS`(20t)**（常量在 `data/cargo.js`；写入货物实体本身 ⇒ **返还后仍 20t**）；⑦**阵亡返还** `returnCargoToSector`（与矿物返还同一出口体例）：已装货物**全额返还**（同一 id、按序号确定性插回）、`ship.cargos`/`hull.cargo` 清空归零 ⇒ **幂等**、**不自动重新入队**；在装货物在死亡/停用/离场时**解锁归零不完成**；矿物返还链未动；⑧**只读口径/接口**：星区 `cargos[]` 新增 **`locked`/`lockedBy`/`loadProgressTicks`/`loadNeedTicks`**；单位 `cargoListOf`；模块 `battle.cargoLoadingOf(inst)`；新接口 **`battle.unloadCargo(shipId, cargoId)`**（幂等）、`toggleCargoQueue` 增 `reason`；⑨**UI**：`--chip-font-size` **11→13px（同源三处一并变：货物芯片/目标选择器/编队类型芯片）**、星区栏改**标题行+芯片行**两行、**锁定芯片**置灰虚线+剩余秒数悬停+点击无效、**单位详情「单位系数」下方新增「装载货物」栏**（有货物才显示、点芯片返还星区、仅我方存活可点；在装未完成项由星区栏 `.locked` 表达）、模块行「装载中 {done}/{need}t」；⑩**未实装**：运载/卸载收益、装载战报（如需补键请指示）；「暂停」按模块停用理解（整体暂停＝冻结不解锁）；`bonus` 仍仅展示 | ✔ 已确认；建议 git 备注：`M2.5 装载光束（cargo_loader 标签 + cargo_load 词条）+ 货物装载体系（锁定/进度/入舱/返还）+ 详情装载货物栏 + 芯片 13px` |
| 2025（日期待补） | 装载体系（收尾） | **装载体系收尾三改**：①**装完的货物必须离开星区货物栏**——核对结论＝**数据层本就正确**（`landCargoOn` 已移出 `cargos` 与 `cargoQueue`），**真因是 CSS 缺陷**：本项目**没有全局 `.hidden{display:none}`**（各可隐藏元素各自声明），而上轮给 `.cargo-chip` 加了 `display:inline-flex` 却漏了同名组合规则 ⇒ `classList.add('hidden')` 对芯片**完全不生效**；已补 **`.cargo-chip.hidden{display:none}`**（同时修好详情栏芯片隐藏）；另**健壮性修正**：UI 的 tick 刷新改到**微任务**执行（本帧全部同步回调之后、渲染之前）⇒ **无论引擎/UI 注册先后，UI 恒读本 tick 结算后的状态**（含“装载完成⇒货物离开星区栏”）；复核无残留节点/缓存（芯片节点按 ≤40 件建一次并复用、隐藏而非销毁），空态整组隐藏仍生效；②**单位详情「装载货物」栏**改为列出 **`cargoListOf(ship)`（已入舱）＋ `battle.sector.cargos` 中 `lockedBy === ship.id`（在装/锁定中）**、按 id 去重、**复用既有只读派生**（未新增引擎接口，UI 只筛选展示、不自算锁定与进度）；在装芯片加 `.cargo-chip.locked`（置灰虚线，与星区栏同体例）、**点击无效**（引擎直接拒绝 `reason:'cargo'`）、悬停显示剩余秒数；已入舱芯片点击返还星区；**`.detail-cargos` 改纵向 ⇒ 标题单独一行、芯片另起一行**（与星区栏同体例）；两类皆无 ⇒ 整栏隐藏；③**同 tick 装载优先级＝装载速度高者优先（跨阵营统一）**：Pass1 **两阶段**——Stage A 装载器在**成本支付/冷却之前**提前返回、只登记认领申请（`{ship,inst,ctx,speed,order}`，**不耗能不进冷却不写 pending**），Pass1 末尾 **`resolveLoadClaims()`** 按 `speed` **降序**（**同速按固定遍历序** allies→enemies、组内既定顺序）逐条裁决，败者整条作废并继续尝试下一件，**赢家才记账**（耗能/冷却/pending/激活统计）；速度**只有 `cargoLoadSpeedOf()` 一个口径**（与时长公式共用）；**镜像对等**：速度不同只由速度决定、同速沿用既有“固定顺序先到先得”约定；⚠ 副作用：同单位内装载器改为 Pass1 末尾消费能量预算 ⇒ 能量紧张时**非装载器模块优先**（跨单位不受影响）；④i18n **无新增/删除键**（复用 `cargoLockedHint`/`detail.cargos`/`detail.cargoUnloadHint`） | ✔ 已确认；建议 git 备注：`M2.5 装载收尾（星区栏移除修复/CSS .hidden/详情含在装货物/同 tick 速度优先）` |
| 2025（日期待补） | 货物模块 | **货物传输 + 货物维修（运输类）**：①**共同硬约束**：**两者只使用「已装载完成（已入舱）」的货物**（只读 `cargoListOf`＝`ship.cargos`；在装货物仍在星区列表、`landCargoOn` 是唯一入舱写入者 ⇒ **天然不可用**），另有**三重防护**（数据源／候选过滤 `!c._loadBy`／落地复核）并以“实体是否仍在货舱内”为幂等判据；②**货物传输 `cargoTransfer`**（`type:['cargo_transfer']` 标签、运输类亮黄、`energy_cost`+`cooldown_ticks`、**无数值词条**）：目标 `kinds:['ally']` 单体**排除自身**；门控＝**无已入舱货物／目标剩余货舱装不下该件（含为 0）／能量不足** ⇒ 不激活；**选择口径＝FIFO（先入舱先转出）**〔★ 待用户裁决是否改为“吨位最小”，切换＝`pickCargoOf(ship,ctx,'fifo')`→`'min'` 一处〕；**结算新增 3d-3**：**成对原子**搬运整件（同实体先出后入、无中间态）、`hull.cargo` 与实体清单**同写同源不漂移**、**不改任何货物字段（含不重置 20t）**、落地再按剩余货舱防御性钳制（**整件不转**）；**战报 `battle.log.cargoTransfer`**（仅实际成功 1 条、3d-3 成句）；③**货物维修 `cargoRepair`**（`type:['cargo_repair']` 标签、运输类、`energy_cost`+`cooldown_ticks`+**新词条 `hp_per_ton`** 逐级 1/2/3/5）：目标 `kinds:['ally','self']`（**含自身**、默认自己）；**回血＝`Math.round(tons × hp_per_ton)` 在 Pass1 算好冻结**、**不进 `AMOUNT` 表 ⇒ 不乘类别系数**（只被 `cargo_repair` 标签分支读取，**既有模块零影响**）；门控＝**无已入舱货物／能量不足／目标满血**；**消耗口径＝吨位最小优先（唯一函数 `pickCargoOf(ship,ctx,'min')`，严格小于 ⇒ 同吨位先入舱者）**；**结算新增 3d-4** 销毁整件（`splice` + `hull.cargo -= tons`、**不返还星区**、幂等），回血走既有 **4c `applyHpTo`** ⇒ **先扣货、后回血**（与 `oreRepair` 同口径）；**战报 `battle.log.cargoRepair`**（实际回血 > 0 且本 tick 确实销毁货物、4c 成句）；④**结算全序更新**：`3b → 3c → 3d-1 → 3d-2 → **3d-3 货物传输** → **3d-4 货物维修消耗** → 4a/4b/**4c 回血**/4d/4e → Phase B/B2 → 步骤 5 装载完成 → Phase C`（整段 3d 在步骤 4 与步骤 5 之前、作用对象不相交；Pass1 只从 tick 起始货舱选货 ⇒ 本 tick 新入舱货物本 tick 不可被搬运/消耗，反向的“腾舱供入舱判定”允许）；⑤**UI/i18n**：成本列沿用「耗能 N · 每 Xt」（无新键），效果列新增 `统计：传输 1 件货物` 与 `每吨回血 +{v}`（词条原值、不走 `× coef`），模块名与两条战报共 **6 个 zh/en 成对键**、**无新样式**；两模块**不需要新增 UI 判据** | ✔ 已确认；建议 git 备注：`M2.5 货物传输（整件原子搬运）+ 货物维修（hp_per_ton）+ 结算步骤 3d-3/3d-4` |
| 2025（日期待补） | 目标选择 + 货物强化 | **①目标选择逻辑修正（缺陷修复）**：根因＝候选池把 **`self` 桶写在最前**，而无 `prefer_self` 时全候选 rank 相同、稳定排序**保持池序** ⇒ 自身获得**未声明的置顶优先级**（`kinds:['ally','self']` 且无 `prefer_self` 的 `oreRepair`/`cargoRepair` **默认恒选自身**）；修正＝引擎 `moduleTargetList` 与 UI `candList` 改为 **`self`/`ally` 同源同序**（两者合并为一次按己方单位数组自然顺序的遍历，自身只在**自己的那一格**、**轮到自身才是自身**），`prefer_self` 为**唯一**提权标签；`shipEffectiveTarget`/`fleetPreview` **无需改动、零影响**；**受影响模块仅 `oreRepair`/`cargoRepair`（及新 `cargoEnhance`）**，`self` 单桶/`ally`/`enemy`/`any` 各模块与带 `prefer_self` 的 `timeWarp`/`slowTime` **逐字不变**；“轮到自身”已确认（自身队首/队友全灭/只剩自身 ⇒ 仍选到自己）；**无 i18n 变更**；②**新模块「货物强化」`cargoEnhance`**：**采矿类·紫**、主动、**耗矿物 + 耗能**；**标签 `cargo_enhance`** ＋ **新词条 `bonus_add`**（加性、逐级递增，占位 Lv1 0.1→Lv16 0.3；`energy_cost 60`/`ore_cost 20`）；目标 `kinds:['ally','self']` 单体（**不带 `prefer_self`**）；**强化对象＝目标单位货舱中已入舱的货物**（与传输/维修同一数据源 `cargoListOf(target)`、三重防护保证**在装货物不可强化**）；**一次激活只提高目标一个货物**、**选择口径＝FIFO（第一件「尚未被强化」者）**〔★ 待裁决是否改吨位最小，切换一处〕；三条门控＝**无未强化货物／矿物不足／能量不足**；**回写 `cargo.bonus += bonus_add` 且置一次性标记 `enhanced`**（**随实体走**：传输、返还星区、再装载均不清除 ⇒ **每件只能强化一次**；**幂等**）；`bonus_add` **不进 `AMOUNT` 表 ⇒ 不乘类别系数**（无通用 `fx` 遍历 ⇒ 对既有模块**按构造零影响**）；**结算新增 3d-5**（成本恒在其前：能量步骤 3、矿物 3d-1；3d-5 在步骤 4/5 之前、只对 tick 起始已入舱货物生效、单向）；③**同 tick 货物预留升级为全局集合 `cargoClaimedTick`**（tick 起始清空、体例同 `sectorClaimedTick`，**货物链三处（传输/维修/强化）共用**、**先到先得**、收集顺序固定 allies→enemies；挡住“强化他人货物”与“该目标自己维修/传输”的争用；对既有两模块只增保护、不改语义；原 `ctx.cargoTaken` 移除）；④**只读口径**：货物实体新增 **`enhanced`**（已随星区快照与单位货舱清单暴露，UI 直接读、不自算）；⑤**UI/i18n**：效果列新增 `加成 {v}%`（走 `formatBonusPercent`）、成本列零改动、**无新样式**（芯片加成段写入后自动由隐藏变 `+10%`）、新增 3 键（模块名/效果/战报），战报 `battle.log.cargoEnhance`＝`{owner}的{module}：强化了 {cargo}（加成 +{v}%）`（仅真正写入 1 条） | ✔ 已确认；建议 git 备注：`M2.5 目标选择修正（self 无隐式优先级）+ 货物强化（bonus_add/enhanced/3d-5）` |
| 2025（日期待补） | 已强化徽标 / 加成表述 / 货物队列 | **三处修正**：①**「已强化」独立徽标**：**落点＝芯片已有第六段末尾占位格 `.cargo-chip-pad`**（六段结构不变、不新增元素）、**显示语言无关符号 `※`**（JS 常量 `CARGO_ENHANCED_MARK`，不占 i18n 键；悬停说明用新键 `battle.sector.cargoEnhanced`＝「已强化」），**判据＝引擎只读 `enhanced`**；星区栏与详情栏**共用同一构造器 ⇒ 两处同时生效**（在装 locked 芯片亦照显）；CSS 补 `text-align:center; min-width:0; overflow:hidden` **兜底**（flex 自动最小尺寸会因 `※` 宽于 `2.2ch` 撑宽芯片）⇒ **宽度恒为 `--cargo-seq-w`、不跳动**；沿用类型色、**无新配色**；`sig` 含 `enhanced` ⇒ 仅强化那刻重写 DOM；②**加成值表述修正**（原显示「加成 −90%」，应为「加成 +10%」）：根因＝`bonus_add` 是**增量**却被**倍率口径**换算；新增唯一函数 **`formatBonusDeltaPercent(delta)`**（增量 `d×100`、**带 `+`/`−` 符号**：`0→'0'`(不显示)、`0.1→'+10'`、`0.15→'+15'`、`0.3→'+30'`、`−0.05→'−5'`；只返回数字、`%` 由 i18n 给），**2 处调用点**（详情效果列 `statBonusAdd`、战报 `cargoEnhance` 的 `{v}`）全部改用它，i18n 模板**去掉写死的 `+`**（避免 `++10%`），效果列判据 `>0` → `!==0`（负增量正常显示）；**货物芯片 `bonus` 仍走 `formatBonusPercent`（倍率，`1.1→10%`）**、芯片未改一行，两个函数分工写入 jsdoc；其它百分比点（`hp_below_activate` 阈值、各进度条 `pct`）核对**不受影响**；③**星区货物列表＝队列式（前出后入）+ 容量不限**：`insertCargoIntoSector` → **`appendCargoToSector`**（`push` 追加队尾、幂等、无 id 时防御补号）⇒ **卸载/阵亡返还一律排队尾、绝不按 `cargo-<序号>` 恢复初始位置**；编队定义仍按定义顺序入列、入舱 `splice` 移除后**其余前移**；**`CARGO_LIMITS.maxTotal = 40` 作用域收窄为「仅编队定义阶段」**（只在 `normalizeSectorCargos` 截断并记 `cargoOverflow`；`setupView` 仍用它封顶添加）⇒ **运行时列表长度不限**、外来货物可无限追加；**id 口径**＝定义阶段 `cargoInstanceId(位置)`、运行时 `nextCargoId()` 自增计数器（种子＝现有最大序号、单调递增、**绝不回收**、无随机/时间）；**「优先队列 queueIndex」与「列表顺序」两概念明确区分、互不改写**（点击入队不动列表顺序，装载/返还不动队列成员，队列移除后序号连续）；**UI**：星区栏**每帧按引擎列表顺序 `append` 重排 + 按需补建芯片**（此前外来货物无芯片、顺序恒为初始位置 ⇒ 已修）、隐藏保留 DOM；详情货物栏**每帧按引擎口径重排**（入舱顺序 + 在装项）⇒ 前出后入界面真实可见；**i18n**：新增 1 键、2 条模板去 `+`（键名不变）；**无新样式/新配色** | ✔ 已确认；建议 git 备注：`M2.5 已强化徽标※ + 加成增量百分比修正 + 星区货物队列式排列/容量不限` |
| 2025（日期待补） | 卸载缺陷 / 取消封顶 | **①修复「已装载货物点击芯片无法卸载回星区」（确诊 + 修复）**：逐项否证后确认**引擎与 UI 绑定本身都是好的**（`unloadCargo`/`returnCargoToSector`/`appendCargoToSector` 路径正确、id/shipId 取自活体对象、`operable()` 正常、详情栏**未**逐帧重建芯片）；**真因＝卸载后立刻被同一条装载器重新装回去**——`pickLoadableCargo` 把**未入队的星区货物**也算作可装载对象，而装载光束**无自身冷却**（忙/闲只判是否在装）⇒ 玩家在两 tick 之间卸载、**下一 tick 即被重新认领**，且该件已永久 fast-load（20t、`need≈18t`）⇒ x1 约 0.9s、x10 约 0.09s 就回到货舱，肉眼即"点了没反应"（也解释了"入队正常、只有卸载像失效"的不对称）；**修复**＝新增 **`cargo.manualUnloaded`**（`unloadCargo` 置位、**阵亡返还不置**；`pickLoadableCargo` 跳过；`toggleCargoQueue` 入队即清除；实例起步 `false`、随实体走、UI 只读）＋ **UI 加固**（详情栏与星区栏点击**统一改容器级事件委托**、新增 `arrangeChips`「顺序未变⇒零 DOM 操作」、`chipFor` 复用即补回节点）；三种边界已确认（连续多击按点击顺序排队尾／再点幂等零副作用／在装芯片仍由引擎拒绝、置灰虚线不可点）；★ **新口径待用户确认**：「玩家手动卸载的货物，装载器不再自动装回去，直到玩家把它加入优先队列」（回退路径极简：去掉该判断 + `manualUnloaded` 参数，其余加固保留）；装载对象选取顺序最终表述＝**优先队列队首 → 其余按星区列表顺序，逐件排除：已锁定 / 本 tick 已认领 / 本舰装不下 / 玩家手动卸载**；②**取消封顶（编队界面也不封顶）**：`normalizeSectorCargos` **删除总数截断与 `cargoOverflow` 告警**，`setupView` **去掉添加按钮与批量循环的总数判据**，`CARGO_LIMITS` **删除 `maxTotal`**，`cargoOverflow` 的**告警码/分支/zh-en 文案一并彻底移除**（不留备用）；**字段级校验与钳制全保留**；**`maxCountPerEntry`(20) 按用户口径保留**（单次输入便捷上限、非总量限制）；③**核对未受影响**：星区矿石储量（另一套口径）、单位货舱容量（`cargoLoadOf`/`cargoRoomOf`，`ship.js` 零改动）——货物列表不限长度只涉星区列表长度与编队条目数，`hull.cargo` 仍只在步骤 5 由唯一写入者按 `cargoRoomOf` 门控增减；`manualUnloaded` 只影响选取、不改容量/结算/`__pending`；④**i18n**：新增 `battle.sector.cargoHoldHint`、删除 `battle.drill.warn.cargoOverflow` | ✔ 已确认；建议 git 备注：`M2.5 卸载失效修复（manualUnloaded + 容器级事件委托）+ 货物总件数取消封顶` |
| 2025（日期待补） | 卸载标记/战报/传输顺延 | **三项**：①**手动卸载标记改为「分阵营 + 带时限」**（用户拍板；解决“其它船与敌方都不自动装载”）：字段＝**`manualUnloadedSide`（卸载者阵营，复用 `side` 的 `'ally'`/`'enemy'`）**＋**`manualUnloadedUntil`（绝对到期 tick）**，常量 **`CARGO_MANUAL_UNLOAD_TICKS = 600`（30s）**导出在 `js/data/cargo.js`（不硬编码）；**判据唯一实现** `cargoManualUnloadActive(cargo, side)`＝**阵营相同 且 未到期**（**绝对到期模型**、无逐 tick 递减 ⇒ 不抖动、确定可复现、镜像对等、到期自动失效）；**只排除同阵营装载器的“自动选取”** ⇒ **敌方照常自动装载**；**入队即双清**（立即恢复自动装载）、**阵亡返还仍不记标记**；星区快照新增**派生字段** `manualUnloaded`/`manualUnloadedTicks`（原始字段与 `_loadBy` 一样**不外泄**）；芯片悬停四态优先级＝**装载中 > 已入队 > 已手动卸载 > 可入队**，卸载态复用 `battle.sector.cargoHoldHint` 并附 `{s}` 剩余秒数（走 `formatTickSeconds`）；②**新增两条低频战报**：`battle.log.cargoLoad`＝`{owner}的{module}：装载 {cargo} 完成`（唯一入舱点 `landCargoOn`、仅真正入舱 1 条、与数值同批）、`battle.log.cargoUnload`＝`{owner}：把 {cargo} 卸载回星区`（`unloadCargo` 成功后**即时成句**、**不写 `__pending`**、失败不记、重复点击幂等、**不额外限流**；`cargo` 纯文本口径与芯片一致）；**阵亡返还仍不播报**；③**货物传输容量不足顺延**：`pickCargoOf` 新增第 4 参 `maxTons`＝目标剩余货舱，`fifo` 模式**按货舱列表顺序遍历取第一件装得下的**、**全部装不下 ⇒ 返回 null ⇒ 不激活不耗能不进冷却**；**门控与记账同函数同快照**（不会“门控通过但选不到”）、**Pass1 冻结实体引用**、结算 **3d-3 防御性复核整件不转/幂等**；仍**只搬一件**、不改货物字段；**维修（'min' 不传 maxTons）与强化（fifo 不传 maxTons）零影响已核对**；④i18n：新增 2 键、改写 1 键（`cargoHoldHint` 加 `{s}`） | ✔ 已确认；建议 git 备注：`M2.5 卸载标记分阵营带时限 + 装载/卸载战报 + 货物传输容量顺延` |
| 2025（日期待补） | 无人机类 | **无人机类 5 个召唤模块 + 新增「维修光束」**（权威规格＝`无人机模块设定.md`；体例同 `drone/laserDroneSpawn.js`，**引擎与 UI/CSS 零改动**，全部走既有 `summon` 路径）：①**模块清单（`category:'drone'` 棕）**：`repairDroneSpawn` 维修无人机（携带维修光束）、`bulwarkDroneSpawn` 壁垒无人机（**固若金汤 + 硬化护盾**；**文档硬数值 hp 50 / energyRegen 50 / energyCap 2000**）、`rocketDroneSpawn` 火箭无人机（**火箭发射器**；**energyRegen 50 / energyCap 1000**）、`laserTurretSpawn` 激光炮塔（**`laser` ×2 两个独立实例 + 再生护盾**；**energyRegen 30 / energyCap 1000 / lifespan 2400**）、`sentryTurretSpawn` 哨戒炮塔（**`cannon` + 同盟护盾**；同上硬数值）；**既有模块映射照用、未新建重复模块**（hardShield/regenShield/allianceShield/rocketLauncher/cannon/laser/impregnable）；**未给数值一律预填占位**（三围/存在时间/在场上限/冷却/耗能；三个无人机 `lifespan 1200t` 占位、炮塔按文档 2400t），**文档硬数值各级保持不变**（逐级表只放大占位项）；②**召唤机制核对**（引擎零改动）：携模块等级＝召唤模块等级（`doSummon` 的 `spec.level ? … : inst.level`）、`temp:true` ⇒ `lifespan_ticks` 到期自动死亡并移出场景、`maxSummoned` 按 `u.summonMod === inst.moduleId` **按模块计数互不挤占**、`modules` 两条同 id ⇒ **两条独立实例（`force=true` 不受槽限）**、统一 `type:['summon','cool_first']`（**部署即进入冷却、开场不白送**，与既有召唤模块同体例）、`target:{}` 无命中目标、召唤单位显示名与图标随模块（缺素材走既有降级）；③**维修光束 `function/repairBeam`**：**`picker:false` 仅无人机专属**（`setupView` 可选清单过滤 `picker` ⇒ 不出现为可安装项；战斗 UI 读 `ship.modules` 实例 ⇒ 无人机模块筹码正常显示）；单体**仅友方**（`ally` 桶天然排除自身）、耗能+冷却；回血＝`hp_target` + **`exact_amount`**（**词条原值、1:1 不乘类别系数**）经 `applyHpTo`（按 `hpMax` 截断、不受受伤减免）；门控＝能量不足／目标满血不激活；**结算沿用步骤 4c、无新分支**；**暂不产生战报**（未擅自新增键）；④**图标**：两个炮塔＝**实心正六边形 SVG**（`assets/img/激光炮塔.svg`、`哨戒炮塔.svg`；主填充 `#b07a4a` 无人机类棕、外框 polygon **逐字符一致**、内部纹样区分 —— 美术交付并已核实存在）；维修/壁垒/火箭**暂不设 `icon`**（名称首字降级，待美术补）；⑤**i18n**：新增 6 对键（5 个模块名 + 维修光束名），召唤单位显示名复用模块名称键 | ✔ 已确认；建议 git 备注：`M2.5 无人机类（维修/壁垒/火箭无人机 + 激光/哨戒炮塔）+ 专属维修光束 + 六边形炮塔图标` |
| 2025（日期待补） | 无人机（纳米） | **新增「纳米无人机」召唤模块**（规格＝`无人机模块设定.md` 纳米无人机条目）：`drone/nanoDroneSpawn.js`（`category:'drone'`、`target:{}`、`type:['summon','cool_first']`）＝携带**火炮 ×1 ＋ 本模块自身 ×1**（**可链式/递归召唤**）；**文档硬数值**：`hp 50`、**无护盾 `shieldCap:0`**、`energyRegen 20`、`energyCap 500`、**`lifespan_ticks 400`＝冷却 200×2**（按用户口径**字面预写**、注释标注"调整冷却需同步改本值"）；占位＝冷却 200／耗能 50／`maxSummoned 2/3/4/5`（逐级表只放大 `maxSummoned`）；无 `icon`（首字「纳」降级）。★ **链式召唤的引擎结论**：子体经 `installModule(force=true)` **每次独立实例**、等级＝父实例等级；上限判据按 `u.summonMod === inst.moduleId` ⇒ **父子同 id ⇒ 共用同一计数 ⇒ 每阵营总存活 ≤ `maxSummoned`（不会无限增长）**，链式召唤实际作用＝**种群上限内回补**；另有 `cool_first`（子体安装即冷却 200t）、每 200t 至多 1 架、400t 寿命、**Pass1 行动名单为 tick 起始快照**（新召者下 tick 才行动）四重自然约束 ⇒ **未加任何额外防递归限制**；★ **`shieldCap:0` 引擎正确接受**（`Object.assign` 逐字保留 0、池 cap 0 无报错、不吸伤；与既有 `missileLauncher`/`rocketLauncher` 召唤物同配置）；**UI 无"0 盾隐藏盾条"规则 ⇒ 仍显示空条「0/1」——用户裁决「保持现状」，本轮未改 UI**；引擎/UI/CSS 零改动，i18n 新增 1 对键 | ✔ 已确认；建议 git 备注：`M2.5 纳米无人机（火炮+自身模块链式召唤，共用上限不加限制）` |
| 2025（日期待补） | 星域（阶段 0/A/B/C · 收尾） | **战场大地图（星域）从设计到实现并完成收尾**（权威设计＝`战场大地图(星域)说明.md`）：①**S0-1 界面分离**：原「编队配置（单星区模拟）」**移除界面入口**，改由控制台 **`LS.drill()`** 打开（功能零改动）；原入口位置改为**星域配置界面**；②**A 数据与配置层**：`js/data/sectorTypes/`（恒星/星球/矿物/空区/星门，`kind`+`placement(center/ring/edges)`+`count`+`content`+`npcListIds`+`fill`+`texture`+`mapColor`/`marker`）、`js/data/npcLists/`、`js/data/starfields/{h1,h2,h3}`（半径 2/3/4、持续时间 3600/5400/7200、类型开关与数量区间、`sideRules`、`playerUnits`）、`js/data/starfieldData.js`（`validateStarfieldConfig`/`selfCheck`/`list`）、**生成器 `js/data/starfield.js`**（纯函数：配置＋种子 ⇒ 初始状态；方网格「欧氏距离 ≤ R」近似圆、**center→edges→ring 三段分配**、数量区间取值、**未占用格位补为 `fill:true` 类型 ⇒ 星区总数恒＝`layout.cells`**、多候选 NPC 确定性抽取、`fork('count:'+typeId|'layout'|'sector:'+index)` 分区子流、告警 `sectorCountTruncated`/`sectorCellsFilled`）、`js/core/rng.js`（确定性 PRNG；`randomSeed()` 为**唯一非确定性入口**）；③**B 引擎容器层**：`battle.js` 补 `opts.starfield` 星域星区模式（不订阅全局 ticker/不发全局事件/战报只进实例/不执行全灭结束，`checkEnd` 保留）＋删除唯一模块级可变状态（`activeAllies/activeEnemies` 改显式传参）＋实例只读 `log`/`logTotal`/`step()`；`js/systems/starfield.js` 容器（**每星区一个独立 battle 实例**、固定索引序逐区 `step()`、**星域持续时间**倒计时、`crossSectorPhase` 跨区阶段占位、`settle()` 结算入口预留、`fleetPolicy` 跨星区唯一来源＋`setFleetPolicy` 下发、`playerUnits` 入场注入＋`playerEntryIndex`、逐区只读摘要含 `cargoInit`/`oreInit`）；④**C UI 层**：`starfieldMapView.js`（方网格、类型色/标记/**贴图预留**、**缩放 0.5×~2.6× 与不设限自由平移**、格内**存活单位图标 + `+n` 省略 + 分类主题色进度条（矿物紫 `--cat-mining`/货物亮黄 `--cat-transport`，比例＝当前/初始、超初始满条、值为 0 隐藏）**、图例栏 `nowrap` 且**降到自然宽 2/3 即整栏隐藏**（滞回 0.7×）、侧栏**默认半屏**可拖拽并会话记忆、顶栏倒计时与星域状态）、**侧栏＝完整星区战斗场景**（`mountSectorScene` 快照换绑复用战斗屏同一渲染链与全部指令，**不显示战报**、星区栏注入 `#编号 类型名（坐标）`、可操作指挥、切换/关闭无残留）、`starfieldConfigView.js`（难度/玩法选择、**种子唯一输入处**、半径/持续时间、类型增减与数量区间、**多 NPC 列表**、**玩家单位列表**、预览、**导出下载/文件·剪贴板导入且界面不显示 JSON 文本**、校验走同一入口）、`starfieldSession.js`（当前星域唯一持有者）；⑤**C-3b 内嵌 NPC 列表编辑器**：配置根层 `npcLists`（**与内置 `units[]` 同构**）、内嵌 id **必须 `custom:` 前缀**且禁与内置冲突、解析顺序**内置→内嵌→报错**、可新建/增删条目/重命名（引用同步）/删除（**被引用则拦截 + 一键解引用**）；⑥**tick 驱动权（已裁定）**：**星域由地图视图驱动**（打开地图时按全局 ticker 推进 `step(1)`，**离开地图即停止**，回图续跑）；暂停/调速/逐 tick **复用全局 ticker 的既有战斗控件**；容器不持有驱动时钟（将来改后台运行＝地图 `onTick` 一处 + 容器订阅）；⑦**本轮修正**：`radius` 字符串误报修复（校验前数字归一）、星区摘要移除并改为星区名注入、侧栏拖拽上限可压到"仅剩一列星区"、全队主要目标星域级统一、图例 2/3 阈值隐藏、进度条分母改 `oreInit`/`cargoInit`（超初始满条、0 隐藏）、**拖拽范围不设限**（原 40px 夹取已删）；⑧**自检入口**：`LS.starfieldData.selfCheck()`（12 项）/`LS.starfieldGen.selfCheck()`（15 项，含内嵌 vs 内置等价对拍）/`LS.battle.selfCheck()`（7 项）/`LS.rng.selfTest()`（9 项）；**未实现（后续轮次）**：星区间移动、星区特殊效果、最终结算内容、跨区相互作用、星域存档扩展、敌 AI 跨区行为 | ✔ 已确认；建议 git 备注：`M2.6 星域（配置体系+确定性生成器+多星区容器+大地图与侧栏战斗场景+配置界面+内嵌NPC列表编辑器）` |

| 2025（日期待补） | 星域 · 单位跨区移动（阶段 1+2 + 修正） | **单位在星区间移动（航行引擎）已实现并验收**：①**航行系数**并入单位 **`coefficients.nav`**（与 attack/shield **同族同链**；模块词条 `nav_coeff_add` 纯加性叠加、默认 1；删除旧的 `baseNavCoeff` 冻结链路与特判 ⇒ 单一口径）；②**充能时长与每 tick 能耗写入单位配置文件**：船型顶层 **`navCdTicks`（默认 200t＝10s）** 与 **`navEnergyPerTick`**（逐级可覆写、**以文件数值为准**；缺失回落 `ship.js` 唯一默认常量；唯一读口径 `navCdTicksOf`/`navEnergyPerTickOf`），**星域侧原硬编码常量已删除**；③**充能公式** `cd = max(1, round(navCdTicks ÷ 航行系数 × (1 + 单位时间系数)))`，**迁移落地时冻结**（`navReadyUntil` 绝对到期 tick；结算只判到期不重算）；④★**「始终充能」**：每 tick 凡冷却未走完的**可指挥在场单位**（`isCharging` 唯一判据）都推进 1 tick 并**扣能耗**（扣不起 ⇒ 不扣/不推进/`navStalled`；**充能完成零耗能、保持就绪**），**与有无指令无关**；就绪即可移动；⑤**移动规则**：一次一格、仅上下左右；路径＝**最短曼哈顿步数的「均匀阶梯」（Bresenham 交替；`|Δq|==|Δr|` 定为先 q）**（自检含判别性算例）；**就绪即走**（不必等充能满），落地后重新充能；⑥**跨区时机＝即时到区**（无"航行中"态）；**迁移＝同一单位实例整体搬迁**（`takeUnit`/`adoptUnit`，id 与对象同一性不变；**外部施加的临时状态按来源清除、自身来源保留**；保留血量/护盾/能量/模块冷却/已装货物/携带矿物/策略与目标选择；**在装未入舱货物与星区侧冷却不随行**）；**静默离区**（清除源区指向该单位的引用，不产生回落战报）；⑦**召唤物不随行**（留源区）＋**召唤上限改为跨星区统计**（容器注入只读钩子 `summonCountOf`，`battle.js` 单点分派）⇒ **迁走不会在新星区重复召唤**，名额在召唤物阵亡/到期后自然归还；⑧**落地口径＝容器跨星区阶段 `crossSectorPhase()`**（全部星区 `step()` 之后 ⇒ 本 tick 迁移下一 tick 生效、与星区顺序无关；同 tick 多单位按 `sectors[]` 序 × 区内序稳定排序；**Pass1 零数值变化**）；⑨**操作与语义**：侧栏**拖动单位卡到地图目标星区**（目标格描边高亮）⇒ 唯一写入口 `moveUnitTo(unitId, targetIndex)`；**拖回自身所在星区＝取消移动**；**冷却中可排队**（覆盖目标、保留 `navReadyUntil`）；⑩**UI**：单位卡与详情**图标右侧绿色竖直充能条**（就绪＝满格且**隐藏但占位**；能量不足 ⇒ 停住变黄 + 「能量不足」；**默认无文本，仅移动/充能中显示**）、**`⇥#n` 排队徽标**、**地图任何缩放层级都显示存活单位图标（`+n` 省略；进度条仍仅放大显示）**、**选中单位后地图显示剩余路径浅色描边（逐格消减）**、**该单位移动时侧栏自动跟随切区并保持详情**、移除地图下方操作说明；⑪**自检**：`LS.starfield.moveSelfCheck()`（**15 项**）；**零回归**：`LS.drill()` 与战斗屏不受影响 | ✔ 已确认；建议 git 备注：`M2.6 单位跨区移动（航行引擎/始终充能/阶梯路径/同实例搬迁/召唤物留原地 + 地图图标与路径描边）` |

*本文件随开发迭代更新；里程碑完成后由开发者（用户）确认勾选。*
