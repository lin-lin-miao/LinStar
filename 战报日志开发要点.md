# 战报 / 日志开发要点（Skill 提示）

> 用途：凡改动涉及「战报 / 战斗日志」的任务，先读本文件。
> 本文档「可更新」：改动战报/日志机制后，请同步修订本文件，只增删关键要素与修改位置。

## 1. 成句模型（命中 = 表头 + 逐吸收段）
- 表头：`{actor}的{weapon}<动词>{target}`，动词按命中类型：
  - 主命中(普通) = `开火命中`
  - 爆炸主目标 = `爆炸命中`
  - 波及(splash) = `溅射到`
- 逐吸收段，按**引擎实际吸收顺序**用逗号拼接：
  `，对{承接者}造成{amount}点{dtype}伤害`
  - 承接者：护盾**模块名**(绿)、`舰载护盾`(基地池)、`共享同盟护盾`、`共享防爆护盾`、`舰体`(hull)
  - 吸收 0 的段省略；引擎实际顺序：爆炸先友方防爆拦截 → 目标自身时长盾池(激活序) → 舰载护盾 → 共享同盟/防爆 → 舰体
- 反射：`{owner}的{module}反射{amount}点伤害给{attacker}` + 返程落地逐吸收段

## 2. 伤害类型（dtype）
- 取「伤害来源模块」`fx.type` 里的**伤害类型标签**；无命中标签 → `普通`。
- 当前标签集在 `js/systems/battle.js` 顶部 `DTYPE_TAGS = ['projectile','beam','explosive']`；中文映射在 i18n `battle.dmgType.*`（projectile=动能、beam=能量、explosive=爆炸、normal=普通、reflect=反射）。
- **新增一种伤害类型 = 两处**：① `DTYPE_TAGS` 加标签；② zh-CN.js 与 en.js 的 `battle.dmgType.<tag>` 加映射；③ 给对应武器/弹头模块 `effects.type` 数组加该标签。

## 3. 主要方法与修改位置
| 关注点 | 位置 |
|---|---|
| 命中吸收明细 | `battle.js` `applyHit`，返回 `seg[]`（`{k:'mod'\|'base'\|'alliance'\|'blastproof'\|'hull', inst?, amount}`）+ `killed` |
| 成句拼装 | `battle.js` 模块级 `emitHitLog(headKey, headParams, seg, dtypeTag)`、`landDamageApp`、`damageTypeTag`、`modTok`、`segAbs` |
| 伤害类型标签表 | `battle.js` 顶部 `DTYPE_TAGS` |
| 反射 | `battle.js`：触发入 `reflectQueue`；`step()` Phase B2 在武器/爆炸全部结算后统一 `applyHit(noReflect)` 落地成句 |
| EMP 瘫痪战报 / 成长清零 | `battle.js`：Pass1 `maybeActivate` 上限词条分支（`capKeys`）只**记账**——当 `fx.energy_cap_target < 0`（“把能量上限压到 0”）时把 `paralyze:true` 记进施放方 `__pending.capOps`；真正的战报与清零在 **结算步骤 2** `applyCapOps(rec)` 里与上限应用**同处同批**执行：对 `rec.targets` **每个存活单位各记一条** `battle.log.empParalyze`（`{actor}的{module}使{target}瘫痪`，`module` 用 `modTok` 绿字，colorKeys `['actor','target']`），并**一次性**清零其 `ramp_per_hit` 模块的 `_ramp`。每次激活每单位一条，持续期内不重复 |
| 强制目标战报 | `battle.js`：Pass1 `maybeActivate` 的 `force_target_self`（**`type` 标签**，非词条）分支只**记账**（施放方 `__pending.forceOps`）；**结算步骤 2b** `applyForceOp(rec)` 压栈时记一条 `battle.log.forceTarget`（`{actor} 的 {module} 生效：{n} 个目标被强制攻击它`，`module` 用 `modTok` 绿字，colorKeys `['actor']`，`n`=实际入栈单位数）。**每次激活一条**（不逐目标刷屏），锁定单位/已死者不计入 `n` |
| 强制来源回落/解除战报 | 撤销路径（到期/停用/阵亡/移出场景）统一走 `releaseForced(inst)`：**按“本次撤销事件”聚合**，且只统计**来源栈栈顶发生变化**的单位 —— 仍有下一来源时记 `battle.log.forceFallback`（`{owner}的{module}强制效果结束：{n}个单位的集火对象回落至其它仍生效的来源`）；栈空时记 `battle.log.forceRelease`（`{owner}的{module}强制效果结束：{n}个单位恢复按正常优先级选择目标`）。**每来源每次撤销至多各一条**，避免按单位刷屏 |
| **效果结束类战报的“拥有者”铁律** | 凡“**效果结束/失效/解除**”类战报，都必须**带模块拥有者**，成句形如「`{owner}的{module}{效果}结束：…`」：`owner`＝**持该模块的单位**（红/蓝，colorKeys 含 `owner`，由 `ownerOf(inst)` 取得），`module`＝模块名（恒绿，`modTok`）。已按此规范改为带 owner 的键：`battle.log.forceFallback`、`battle.log.forceRelease`（原为「{module} 的强制来源结束…」，缺 owner）。**其余“结束/失效”相关键已核查**：`battle.log.shieldBreak`（`{ship} 的 {module} 护盾被击破`）与 `battle.log.moduleOff`（`{ship} 停用了模块：{module}`）本就带 owner；`battle.log.tempExpired`（召唤物到期消失）/`battle.log.destroyed`（被击毁）/`battle.log.selfDestruct`（引爆自毁）属**单位生命周期**事件（不是模块效果结束），沿用原来的单位名写法，不另加模块 owner |
| **时间加速战报（低频聚合）** | `battle.js`：**只在模块级“加速生效状态”翻转时各记一条**，绝不逐次激活播报。触发点两处：①**开始**＝`applyHastenOp(rec)`（结算步骤 2 落地）末尾，判据 `!wasActive && inst._hastenActive` → `battle.log.hastenStart`（`{owner}的{module}开始加速：{n}个单位`，`n`＝本次**真正写入** `hastenMods` 的单位数，即结算时复核存活后的数量）；②**结束**＝`releaseHasten(inst, silent)`（到期 `expiries`/停用/阵亡/移出场景/重新激活前统一走它），判据 `inst._hastenActive && n>0 && !silent` → `battle.log.hastenEnd`（`{owner}的{module}加速结束：{n}个单位`，`n`＝被撤销的 `_hastenRefs` 单位数）。两键均 `module` 用 `modTok` 绿字、`owner`＝**模块拥有者**（`ownerOf(inst)` + `uTok`，colorKeys `['owner']`，遵循“效果结束类必须带拥有者”铁律）。状态标记 `inst._hastenActive`（模块级布尔，非数值）随每次落地更新 |
| **时间加速战报的聚合边界** | ①**从无→有**才记“开始”，**从有→无**才记“结束”——同一模块的加速被同一单位反复续期（持续时间未断）**不重复记**；②**同 tick 到期后又重新激活**（`expiries` 记录带 `cancelled`）＝撤销与重建同 tick 完成：`releaseHasten(inst, silent=true)` **既不记“结束”也不清 `_hastenActive`**，紧随其后的 `applyHastenOp` 因 `wasActive=true` **不再记“开始”** ⇒ **完全静默、不产生成对刷屏**（与 `releaseForced(inst, silent)` 同一套做法）；③**多来源**（两个加速模块/两个施放方）**各按自己模块**记各自的开始/结束（单位侧仍按 `max` 取用，不因此多记）；④作用集合内单位**全部**在本 tick 判死/离场 → `applied.length === 0` → 不记“开始”（`n` 恒为“真正生效的单位数”）；⑤战斗在加速生效中结束时走收尾清场、不经 `releaseHasten`，故不会补记“结束” |
| **时间放缓战报（低频聚合）** | 与时间加速**完全对称**：①**开始**＝`battle.js applySlowOp(rec)`（结算步骤 2，与 `hastenOps`/`capOps` 同批）末尾，判据 `!wasActive && inst._slowActive` → `battle.log.slowStart`（`{owner}的{module}开始减速：{n}个单位`，`n`＝真正写入 `slowMods` 的单位数）；②**结束**＝`releaseSlow(inst, silent)`（到期 `expiries`/停用/阵亡/移出场景/重新激活前统一走它），判据 `inst._slowActive && n>0 && !silent` → `battle.log.slowEnd`（`{owner}的{module}减速结束：{n}个单位`）。两键均 `module` 用 `modTok` 绿字、`owner`＝模块拥有者（`ownerOf(inst)` + `uTok`，colorKeys `['owner']`，遵循“效果结束类必须带拥有者”铁律）；**同 tick 到期并重新激活整体静默**（`releaseSlow(e.inst, !!e.cancelled)`） |
| **潜行战报（低频聚合）** | `battle.log.stealthStart`（`{owner}的{module}生效：{n}个单位进入潜行`）/ `battle.log.stealthEnd`（`{owner}的{module}潜行结束：{n}个单位`）：**仅状态翻转各 1 条**（`applyStealthOp` 末尾判“从无→有”、`releaseStealth` 判“从有→无”且非 `silent`），不逐次激活、不逐单位播报；`module`＝`modTok` 绿字、`owner`＝模块拥有者（`ownerOf`+`uTok`，colorKeys `['owner']`）；同 tick 到期并重新激活（`cancelled`）整体静默 |
| i18n 键 | `js/i18n/zh-CN.js`、`en.js`：`battle.log.hit.fire/blast/splash/absorb/reflect`、`battle.log.empParalyze`、`battle.log.forceTarget`、`battle.log.forceFallback`、`battle.log.forceRelease`、`battle.log.hastenStart`、`battle.log.hastenEnd`、`battle.log.slowStart`、`battle.log.slowEnd`、`battle.log.stealthStart`、`battle.log.stealthEnd`、`battle.abs.*`、`battle.dmgType.*` |
| 彩色 token | `js/core/log.js` `formatRich`：`{label, side}`(红/蓝阵营) 与 `{label, mod:true}`(恒绿模块名) |
| 日志渲染 | `js/ui/battleView.js`（识别 `seg.mod` → `<span class="log-module-name">`）；`css/battle.css` `.log-module-name{color:#7ee29a}` |

## 4. 显示顺序铁律
- **命中句先于「被击毁」**：`applyHit` 击毁只置 `killed`（不即时记 destroyed）；由 `landDamageApp`（主命中/波及）与反射 Phase B2 在各自命中句后补记 `battle.log.destroyed`。
- **反射延迟**：反射返程不在触发时即时打回，统一排到本 tick 所有武器/爆炸命中结算完（`reflectQueue`，Phase B2），保证对等且不抢反射池；返程 `noReflect` 不二次反射。
- **0 伤害 / 无敌**：不产出命中句（仍累计统计）。
- **结算内固定顺序：先计时 → 上限/系数 → 强制目标 → 能量 → 护盾/血量 → 伤害**：Pass1 只“决策 + 读 tick 起始快照 + 记账”（零数值变化）；Phase A 一遍单位 for 只**收集**意图成记录数组；随后按固定全局顺序落地：
  1) 计时推进（模块私有计时器，就地；**递减量＝`1 + 时间加速`**，见 §5「时间加速」条；其唯一的跨单位影响“到期撤销/恢复”已记为 `__pending.expiries` 意图）；
  2) **上限与系数/受伤减免/时间加速**：`expiries`（系数/受伤减免/时间加速修饰撤销 → `clearAllSourceMods` + `releaseHasten` + 强制来源出栈 + `dropSourceMods`+`recalcDerived`）、`capOps`（`dropSourceMods`+`setOverlay`；`paralyze` 记录同步记 `empParalyze` 与清零 `_ramp`）、`coeffOps`（**类别系数**：**自身词条** `attack_coeff_add` 加性 → `coeffMods`；**目标级词条** `attack_coeff_add_target`（逐目标一条、`rec.ship`＝被作用单位）；**受伤减免**：`damage_coeff_mul`（自身）与 `damage_coeff_mul_target`（逐目标）→ 各被作用单位的 `damageTakeMulMods`（`ship.damageTakeMul = Π` 各来源，`0.95` = 只承受 95% 伤害）——仅状态/数值变化时写入）与 `timeOps`（**时间加速/放缓**：`time_coeff` → 各作用单位 `timeCoeffMods`（负＝加速、正＝放缓，**正负各取绝对值最大者再求和**）；**不改伤害、只改“需求量”**（`需求量 = max(0, round(基础量 × (1 + 系数)))`，每 tick 恒推进 1）；**低频战报**：仅“从无→有”记 `hastenStart`、“从有→无”记 `hastenEnd`，同 tick 重激活则整体静默）一并落地；**先于伤害结算**；
  2b) **强制目标**：`forceOps`（`force_target_self` 标签 → 把各目标**压入其强制来源栈**（栈顶＝最后激活者优先被集火）+ 一条 `forceTarget` 战报）。只改**目标指向/优先级**（结构性，**不改写 `targetId`**），从**下一 tick 的目标解析**起生效；
  3) **能量**：回充（`energyRegen`）→ 模块消耗（`energySpends`）→ `energy_target` 量值；
  4) **护盾/模块池填充/血量/自毁/临时寿命**（4a 池填充 → 4b 补/汲取盾 → 4c 血量 → 4d 自毁 → 4e 临时寿命判死）。
  之后才 `settleHits`（Phase B）→ 反射 Phase B2 → Phase C。**不要**把上述任一步骤挪回 Pass1 即时执行：那会让同 tick 双方因队列位置不同而吃到不同的上限/能量，产生不对等。
- **系数只有一个口径**：任何读“船类系数”的地方都必须走 `ship.js` 的 `coeff(ship, category)`（`(基础 + Σ运行期加性) × Π运行期乘性`）。**禁止**再写 `SHIPS[ship.typeId].coefficients[...]`（旧 UI 写法）——那会漏掉运行期增益，造成“战斗与显示两套口径”。
- **受伤减免只有一个结算点**：`damage_coeff_mul` 系列（`damage_coeff_mul` / `damage_coeff_mul_target`，`0.95` = 只承受 95% 伤害）是**受击向**词条，改的是 `ship.damageTakeMul`（`ship.js` 的 `damageTakeMul(u)` 为唯一读口径，缺省 1）。**唯一结算点＝`applyHit(target, amount, …)` 入口**（在任何吸收之前乘**受击方**的系数），故主目标命中/爆炸波及/反射返程**所有来源**自动一并减免；此外负值 `hp_target` 扣血与负值 `shield_gain_target` 削盾在各自落地处按同一快照相乘。**不得在出伤侧（`maybeActivate`/波及/反射记账）乘它**（会重复减免）。**豁免**：自毁 `self_destruct_damage`、能量削减（`energy_target` 负值）、上限类 `*_cap_target`。同一 tick 内按 Pass1 起始快照 `u._takeMulTick` 取值 → 本 tick 落地的减免**下一 tick** 才体现。
- **时间系数只有一个读口径**：`time_coeff` 词条 → 单位表 `ship.timeCoeffMods`（**正、负两向各取一个绝对值最大的来源，再把两者相加**——同向不叠加、异向相抵；切换点＝`ship.js refreshTimeCoeff()`），派生 `ship.timeCoeff`，唯一读函数＝`ship.js` 的 `timeCoeffOf(u)`；战斗内统一读 **Pass1 起始快照** `u._timeCoeffTick`（新召单位在生成时写入），故**本 tick 落地的时间系数/撤销从下一 tick 才体现**。**需求量整数化唯一口径**＝`timeScaled(need, coeff) = max(0, round(need × (1 + coeff)))`；**每 tick 恒推进 1**，三类计时器按“需求 − 已推进 tick 数”现算剩余（`inst.durElapsed`/`inst.cdElapsed`/`u.tempLifeElapsed`）：模块持续时间、模块冷却（钳到 0）、临时单位存在时间（结算步骤 4e）。闪标类倒数（`_dmgFlash` 等）**不受时间系数影响**。落地/撤销都在结算阶段（`timeOps` / `releaseTime`），跨单位顺序一致、镜像对等。

## 5. 规则记忆
- 舰载护盾=基地池；舰体=hp/装甲；装甲不再写作“装甲”。
- 模块名一律绿字；单位名按阵营红/蓝。
- 命中句与击毁句的成对顺序以「表头命中段 → 被击毁」为准，改日志时不要把它们拆回 applyHit 内部即时输出。
- **Pass1 零数值变化**：Pass1 只允许写“模块私有计时器 / 闪标 / 结构引用 / 记账”；能量回充与消耗、护盾池创建填满、上限修改与恢复、系数修饰（自身加性 + 目标级加性）与**受伤减免**修饰、**时间加速**修饰、强制目标入栈/出栈、临时单位寿命递减一律只记意图。能量门控用**单位内运行计数** `ctx.avail`（tick 起始能量 + 回充 − 已记账消耗），保证同单位多模块依次门控结果不变；该计数是单位局部，不跨单位。计时器递减就地进行（模块私有、无跨单位影响），但其**到期撤销/恢复**必须记为 `expiries` 意图（同时撤销该模块的自身系数/受伤减免修饰、各被作用单位上的目标级修饰、时间加速与强制来源）。
- **“作用集合”只有一个实现**：`blast_range` 波及 + `include_self`（**`type` 标签**，由词条改标签）自身的扩展由 `battle.js` 的 **`effectSetOf(ship, targets, fx)`** 统一计算（目标 ∪ 各目标**所在队列**前后各 N 个存活单位 ∪ `include_self` 标签补入的自身，按 id 去重、激活瞬间冻结；**“自身是否溅射”看它是怎么进集合的**：`include_self` 标签补入的自身**不产生溅射**，选择器正常解析出的自身（`prefer_self` 默认取到 / 手动选中自己）**照常有溅射** —— 溅射循环只遍历 `targets`，无 `primary === ship` 特判）；上限类词条与 `time_coeff` 等非伤害词条组共用。新增“按集合成批落地”的词条时**复用该 helper**，不要重写扩展逻辑。
- **目标优先级链只有一个口径**：`激活锁定(lock_target_on_activate·持续期内) > 强制目标 > 模块手动目标 > 优先自己(prefer_self) > 船 targetId > 自动粘性 > 全队策略`，由 `moduleTargetList` 打桶排序实现；`shipEffectiveTarget`（船级“主要攻击目标”显示）同源，且**只返回敌方**（支援类模块选中友军/自己不得改写该显示）。模块手动目标的范围由该模块 `kinds` 决定、**可为自身/友军**，故存活校验（`clearShipDeadRefs`）必须用**全场**查找（`aliveUnitId`），只有船级 `targetId` 用敌方队列校验 —— 曾因此把“选中自己/友军”的手动目标误判为阵亡并清成 `follow`（表现为**手动目标改不动**）。
- **上限重算不碰护盾池值**：`recomputeCap` 只写本体池 `capExtra`，再由 `recalcDerived` 以**正确 `permanentBonus`** 统一重算 cap 与汇总。禁止把 `baseShieldPoolOf`（内部 `ensureBasePool(ship, undefined)`，会把常驻护盾如再生护盾的并入量当作 0）当作“取本体池”的 getter 使用——它会把 cap 算小，`ensureBasePool` 的 `value=min(value,cap)` 随即钳掉真实池值（EMP 清空再生护盾的根因）。
