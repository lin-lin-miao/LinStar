# 调试与特殊命令（仅测试用）

> 本文件集中登记**仅测试使用**的控制台命令：这些入口**已从主菜单/UI 中移除**（主菜单现仅保留「主基地」），只能在此查阅并通过浏览器控制台调用。
> 由 AI 维护；**口径确定后即时更新本文件**（不需再确认）。

## 1. 界面入口（UI 中已无按钮）

| 命令 | 作用 |
|---|---|
| `LS.goto('menu')` | 主菜单（**仅「主基地」一个入口**） |
| `LS.goto('base')` | **主基地** |
| `LS.drill()` | **编队/演练编队屏**（仅测试） |
| `LS.ui.openStarfieldConfig()` | **星域配置界面**（种子/数量/内嵌 NPC 列表/导出导入，仅测试） |
| `LS.ui.openStarfieldMap()` | 打开星域大地图（须已有当前星域） |
| `LS.goto('battle')` / `LS.goto('starfield')` | 直接切到对应路由 |

## 2. 基地（`LS.base`）

| 命令 | 作用 |
|---|---|
| `LS.base.state` | 基地状态本体 |
| `LS.base.snapshot()` | 只读快照（资源/上限/建筑/列表项/**fleet（配置表 + 容量 + 在外）**/研究容器） |
| `LS.base.list()` | 注册表摘要（资源/建筑/列表项） |
| `LS.base.selfCheck()` | 基地自检（当前 **28 项**） |
| `LS.base.debug.gain({energy:100})` | 加资源（**按容量上限截断**，返回 `{ok,gained,overflow}`） |
| `LS.base.debug.spend(o)` / `canAfford(o)` / `reasonOf(o)` | 扣资源（不足零改动+原因）/ 可支付 / 缺什么 |
| `LS.base.debug.caps()` / `remaining(key)` | 资源上限 / 剩余空间 |
| `LS.base.debug.buildings()` / `upgrade(id)` / `reset()` | 建筑等级 / 升级入口（未开放阶段返回失败码）/ 重置基地 |

## 3. 舰队与船坞（`LS.base.fleet.*` · M3b）

| 命令 | 作用 |
|---|---|
| `LS.base.fleet.stats()` / `configs()` | 容量·总数·在外·余量 / 配置列表（**快照顺序＝显示顺序**） |
| `LS.base.fleet.capacity()` / `total()` / `out()` / `remaining()` | 舰队总容量（船坞等级）/ 已建 / 在外 / 余量 |
| `LS.base.fleet.preview({shipId,level,modules})` | 干跑：槽位、造价、非法原因（**不改状态**） |
| `LS.base.fleet.modulePicker()` | 模块二级弹窗的数据源（分类序 + 可装配模块清单） |
| `LS.base.fleet.moduleCost(id, level)` | 某模块某等级的装配/拆下费用（与表格造价同源） |
| `LS.base.fleet.slotOp(modules, {action, moduleId, level, slots})` | 草稿级槽位操作纯函数（`set`/`add`/`remove`，失败零改动） |
| `LS.base.fleet.costOf(id)` / `refundOf(id,n)` / `canBuild(id,n)` | 单艘造价 / 拆解返还预览 / 可建造判定 |
| `LS.base.fleet.create({name,shipId,level,modules})` | 新建配置（**同名自动 `#N`**；与既有配置相同 ⇒ **合并**） |
| `LS.base.fleet.clone(id)` / `rename(id,name)` / `remove(id)` | 编辑＝**另存为新配置** / 改名（**以新名为准**）/ 删除（**仅数量与在外均为 0**） |
| `LS.base.fleet.build(id,n)` / `scrap(id,n)` | 建造（校验容量与资源）/ 拆解（返还＝`floor(花费×n×船坞比例)`，只能拆未出征部分） |
| `LS.base.fleet.setOut(id,n)` | 登记在外数量（M3d 出征接线用） |
| `LS.base.fleet.move(id,±1)` / `reorder(id,toIndex)` | 上/下移 / 拖到指定下标（越界 ⇒ `badOrder`/`edgeMove`，零改动） |

## 4. 既有自检与调试（回归时常用）

| 命令 | 作用 |
|---|---|
| `LS.starfieldData.selfCheck()` | 星域数据与配置（12 项） |
| `LS.starfieldGen.selfCheck()` | 星域生成器（15 项） |
| `LS.battle.selfCheck()` | 战斗实例隔离（7 项） |
| `LS.starfield.moveSelfCheck()` | 单位跨区移动（15 项） |
| `LS.rng.selfTest()` | 随机数确定性与分布（9 项） |
| `LS.starfield.create('h1','demo')` / `LS.starfield.summary()` | 创建星域 / 摘要 |
| `LS.starfield.unitNav(id)` / `LS.starfield.moveQueue` | 单位航行状态 / 移动队列 |
| `LS.log.*` / `LS.tick.*` / `LS.save.*` | 战报、时钟、存档门面 |

## 5. 批量配置修改脚本（`tools/`，不参与应用加载）

| 命令 | 作用 |
|---|---|
| `node tools/patch-base-config-fields.mjs [--dry-run]` | 货物配置字段修订（已执行） |
| `node tools/patch-ship-unlock-fields.mjs [--dry-run]` | 船型 `unlockByLevel` 字段位（已执行） |
| `node tools/patch-m3b-costs.mjs [--dry-run]` | 战斗舰建造/升级费用 + 火炮模块逐级费用（已执行） |

> 约定：**数量多的配置（模块/单位/货物等）一律用脚本统一批量处理**，只读取 1 个代表性配置文件确认字段形状，**不逐文件人工编辑**；脚本必须**幂等**并支持 `--dry-run`。
