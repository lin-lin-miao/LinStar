# 星域开发要点

> 本文档为**星域系统的开发索引**：汇总**关键文件**、**关键节点**（生命周期与调用链）、**关键接口与铁律**、**扩展指南**、**自检与调试入口**、**已知占位与坑位**。
> 面向"后续继续拓展星域"的场景；**由 AI 随开发维护**。设计与口径的**权威出处**仍是 `战场大地图(星域)说明.md`（设计稿）与 `开发步骤与开发清单.md`（进度清单）；本文档**只做索引与速查**，不重复完整设计。

---

## 0. 一句话现状（截至本次收尾）

星域已完成：**配置体系（含内嵌 NPC 列表编辑器）→ 确定性生成器 → 多星区容器（每星区一个独立战斗实例）→ 星域大地图 → 侧栏完整战斗场景（可指挥、不显示战报）→ 星域配置界面 → 单位跨区移动（航行引擎 / 始终充能 / 阶梯路径 / 同实例搬迁 / 召唤物留原地）**。

**尚未实现**：星区间移动之外的跨区交互（增援/跨区目标/资源输送）、星区特殊效果、最终结算内容、星域存档扩展、敌 AI 跨区行为、`tick` 后台自驱（现为"地图打开时才推进"）。

---

## 1. 关键文件（按层）

### 1.1 数据层（纯数据 + 纯函数，注册表驱动）

| 文件 | 作用 | 要点 |
|---|---|---|
| `js/data/sectorTypes/{star,planet,mineral,empty,stargate}.js` + `index.js` | **星区类型**（一类型一文件） | 字段：`id`/`nameKey`/`kind`/`placement`/`count`/`content{ore,cargos,structures}`/`npcListIds`/`fill`/`texture`/`mapColor`（**只存 CSS 变量名**）/`marker`。★ `placement.mode ∈ center/ring/edges` 是**生成位置的唯一驱动**；★ `fill:true` 者（空区）是**剩余格位填充类型**（恰一个） |
| `js/data/npcLists/{none,patrolLight,patrolHeavy}.js` + `index.js` | **内置 NPC 列表** | `units[] = { shipId, count\|countRange, level?, modules?[{moduleId,level?}] }`；★ **不写阵营**（`side` 占位 `null`，敌我由星域配置 `sideRules` 决定） |
| `js/data/starfields/{h1,h2,h3}.js` + `index.js` | **星域难度档**（一档一文件） | `id`/`nameKey`/`radius`/`durationTicks`/`seed`/`sectorTypes{<id>:{enabled,count,npcListIds}}`/`sideRules`/`playerUnits`/`specialEffects`；**纯数据、可 JSON 往返** |
| `js/data/starfieldData.js` | **配置校验与聚合门面** | `validateStarfieldConfig(cfg)`（**唯一校验入口**，C-3 导入亦用它）、`selfCheck()`（12 项）、`list()`、`resolveNpcList(id,cfg)`（**内置→配置根层 `npcLists`→报错**）、`normalizeNpcListIds/readNpcListIds`、`CUSTOM_NPC_LIST_PREFIX = 'custom:'`、`unitsProblems`（单位/模块校验的**唯一实现**） |
| `js/data/starfield.js` | **星域生成器（纯函数）** | `generateStarfield(configOrId, seed)`、`previewStarfield`、`starfieldGenSelfCheck()`（15 项）、`resolvePlayerEntryIndex(sectors,cfg)`、`expandUnitSpecs`、`pickNpcListId`；**缺种子即抛错、绝不回落随机** |
| `js/data/ships/*.js` | 船型（含星域相关字段） | `coefficients.nav`（**航行系数**，默认 1，逐级可覆写）、`navCdTicks`（**充能时长**，默认 200t）、`navEnergyPerTick`（**每 tick 充能耗能**，默认 2） |
| `js/data/modules/**` | 模块词条 | 星域专用：`nav_coeff_add`（航行推进器 `transport/navThruster.js`）、召唤（`drone/*Spawn.js`）等 |

### 1.2 生成与容器层

| 文件 | 作用 | 要点 |
|---|---|---|
| `js/systems/starfield.js` | **星域容器（核心）** | `createStarfield(configOrId, seed)`；每星区一个 **独立 battle 实例**；`step(n)`/`stop()`/`settle()`/`summary()`/`battleOf(index)`/`sectors`（只读摘要）；**`crossSectorPhase()`＝唯一跨区落地阶段**；`fleetPolicy`（**跨星区唯一来源**）+ `setFleetPolicy`；`playerUnits` 入场注入 + `playerEntryIndex`；单位移动全套（`moveUnitTo`/`unitNav`/`moveQueue`/`navPathOf`/`navCooldownOf`/`navPayEnergy`/`isCharging`/`countSummonsAcross`）；`starfieldMoveSelfCheck()`（15 项） |
| `js/systems/battle.js` | **战斗引擎（星域接入点）** | `createBattle(preset, opts)`；**`opts.starfield=true` ⇒ 星域星区模式**（不订阅全局 ticker / 不发全局事件 / 战报只进实例 / **不执行"一方全灭即结束"**）；实例只读 `log`/`logTotal`/`step()`；**星域专用最小接口** `takeUnit(id)`/`adoptUnit(unit, side)`（同实例搬迁）、`opts.summonCountOf`（**召唤上限跨星区统计的唯一分派点**）；`starfieldMode` 判据（UI 据此隐藏战报等） |

### 1.3 UI 层

| 文件 | 作用 | 要点 |
|---|---|---|
| `js/ui/starfieldMapView.js` | **星域大地图** | 方网格（读 `generation.layout`）、类型色/标记/**贴图预留**、**缩放 0.5×~2.6× + 不设限自由平移**、格内**存活单位图标 + `+n` + 分类色进度条**、图例 `nowrap` 且降到自然宽 2/3 **整栏隐藏**、侧栏**默认半屏**可拖拽记忆；**拖动单位卡到格子 ⇒ `moveUnitTo`**、目标格 `.drop-target` 高亮、**选中单位剩余路径浅色描边**、**跟随被选单位切区** |
| `js/ui/battleView.js` | **战斗屏 / 侧栏战斗场景** | **`mountSectorScene(container, battle, opts)` + `destroySectorScene()`**（快照换绑复用同一渲染链与全部指令，`zoneName` 注入星区栏、`fleetPolicy`/`nav` 适配器注入）；单位卡与详情的**图标右侧绿色竖直充能条**、`⇥#n` 排队徽标；`applyLogPanelVisibility()`（按 `battle.starfieldMode` 隐藏战报） |
| `js/ui/starfieldConfigView.js` | **星域配置界面（正式主入口）** | 难度/玩法、**种子唯一输入处**、半径/持续时间、类型增减与数量区间、**多 NPC 列表**、**内嵌 NPC 列表编辑器**（新建/重命名/删除/条目增删，`custom:` 前缀）、**玩家单位列表**、预览、**导出下载 / 文件·剪贴板导入（界面不显示 JSON 文本）** |
| `js/ui/starfieldSession.js` | **"当前星域"唯一持有者** | `getStarfield/setStarfield/ensureStarfield`；控制台与地图共用同一实例；镜像 `window.__starfield` |
| `js/ui/unitIcon.js` | 单位图标唯一口径 | 战斗屏与地图共用（召唤模块图标 → 船型 `icon` → 约定路径三级优先、失败降级 ▲） |
| `js/ui/hud.js` | 全局 HUD | 暂停/调速/逐 tick 三键在 **`combatActive || mapOpen`** 时显示（星域与战斗屏**共用**） |

### 1.4 样式与文案

- `css/base.css :root`：分类主题色（`--cat-attack`/`--cat-shield`/`--cat-function`/`--cat-mining`/`--cat-transport`/`--cat-drone`）、资源语义色（`--ore`/`--cargo`）、星区类型色 `--sector-*`（**占位**）、`--ok`/`--warn`/`--accent` 等。
- `css/screens.css`：星域地图与配置界面（`.screen-starfield`/`.sf-*`/`.starfield-sidebar*`/`.cfg-*`）、侧栏与把手、图例、格子、路径描边 `.sf-cell.nav-path`/`.nav-here`、落点 `.drop-target`。
- `css/battle.css`：单位卡与详情（含 `.nav-cool*` 充能条、`.unit-card.nav-queued/.nav-stalled`）、`.battle-log.hidden`。
- `js/i18n/zh-CN.js` + `en.js`：键前缀 `sectorType.*`/`npcList.*`/`starfield.*`（含 `starfield.cfg.*` 配置界面、`starfield.move.*` 移动反馈、`unit.nav*`、`battle.coeff.nav`）。

---

## 2. 关键节点（生命周期与调用链）

### 2.1 从配置到开战

```
星域配置界面（种子在此唯一输入）
  → validateStarfieldConfig(cfg)           # 同一校验入口
  → setStarfield(createStarfield(cfg, seed))
      → generateStarfield(cfg, seed)       # 纯函数：布局/类型/内容/单位/告警（含补位）
      → 逐星区 startBattle(..., {starfield:true})   # 每区一个独立实例
      → playerUnits 注入 playerEntryIndex 星区（星门 → 回退 edges → 回退 #1）
  → router.show('starfieldMap')            # 大地图
```

### 2.2 单 tick（星域层）

```
地图视图 onTick（全局 ticker，20tps；暂停/调速/逐帧复用同一 ticker）
  → starfield.step(1)
      ① runTicks += 1（星域持续时间倒计时）
      ② 按 sectors[] 索引序逐区 battle.step()      # 各星区内部仍是既有 Pass1→A→A2→B→B2→C 全序
      ③ crossSectorPhase()                        # ★ 唯一跨区落地阶段
           · 逐单位：始终充能（扣 navEnergyPerTick 推进冷却；扣不起 ⇒ 停/顺延）
           · 到期 ⇒ 取阶梯首格 ⇒ takeUnit → adoptUnit 完成一步迁移 ⇒ 冻结下一步冷却
           · 抵达目标 ⇒ 清队列；未到 ⇒ 保留自动续走
      ④ runTicks >= durationTicks ⇒ 停止全部星区 + settle()（结算入口预留）
```

**为什么迁移放在 ③**：它在**全部星区 `step()` 之后**，故本 tick 的迁移在**下一 tick** 才被各区读到 ⇒ **与星区处理顺序无关**、不会"谁先 step 谁得利"；也**完全不在 Pass1/结算遍历内**（Pass1 零数值变化）。

### 2.3 单位移动链路（UI → 引擎）

```
侧栏单位卡 dragstart（先读只读 canCommand；不可指挥则不起拖）
  → 地图格 dragover/drop → .sf-cell.drop-target 高亮
  → starfield.moveUnitTo(unitId, targetIndex)      # ★ 唯一写入口，只记账/排队
       就绪 ⇒ navReadyUntil = runTicks（立即到期）＋ navCdTicks = 0   # 就绪即走
       冷却中 ⇒ 覆盖目标、保留 navReadyUntil（排队）
       目标＝自身所在星区 ⇒ 取消指令（{cancelled:true}）
  → （下一次 step 的 ③）crossSectorPhase 执行一步迁移 → 冻结下一步冷却
```

### 2.4 侧栏场景生命周期

```
点星区 → mountSceneFor(index)
   → mountSectorScene(sbStageEl, starfield.battleOf(index), { zoneName, fleetPolicy, nav })
        · 快照 14 项既有场景引用 → 换绑 → 复用既有 renderRunning() 建舞台
        · 星域模式：除战报外与战斗屏**同一套渲染与指令**（无第二套逻辑）
   → 切换星区 ⇒ destroy() 旧的（严格还原快照）+ mount 新的
   → 关闭/离开路由 ⇒ destroySectorScene()
   → 被选单位跨区移动 ⇒ followSelectedUnit 自动切区并 selectSceneUnit 保持详情
```

---

## 3. 关键接口与只读口径

### 3.1 星域容器（`starfield`）

| 接口 | 说明 |
|---|---|
| `step(n=1)` / `stop()` / `settle()` | 驱动、手动停止（不结算）、结算入口（时间耗尽自动调用一次） |
| `sectors[]` | 逐区只读：`index/q/r/typeId/isStar/placement/alive{ally,enemy}/ore/oreInit/cargoCount/cargoInit/cd/hasEvent/logLines/phase` |
| `battleOf(index)` | 取该区 **battle 实例**（侧栏场景用） |
| `summary()` / `generation` / `warnings` | 星域级摘要、A-5 原始生成结果、告警（`sectorCountTruncated`/`sectorCellsFilled`） |
| `runTicks` / `remainingTicks` / `durationTicks` / `finished` / `stopped` / `settled` | 时钟与状态 |
| `fleetPolicy`（读）/ `setFleetPolicy(kind)`（写） | **全队主要目标跨星区唯一来源**（逐实例下发） |
| `moveUnitTo(unitId, targetIndex)` | **移动的唯一写入口**：`{ok, queued?, cancelled?, cleared?, reason?, navReadyUntil?}`；`reason ∈ none/dead/owner/far/invalid/finished` |
| `unitNav(unitId)` | 只读：`sectorIndex/navCoeff/navReadyUntil/navRemainTicks/navCdTicks/moveQueueTargetIndex/navEnergy/navEnergyPerTick/navStalled/navPath/canCommand` |
| `moveQueue` | 排队快照（每次新数组） |
| `playerEntryIndex` / `playerUnitCount` | 玩家单位入场星区（显示编号＝下表+1）与数量 |

### 3.2 战斗实例（`battle`，星域相关补充）

| 接口 | 说明 |
|---|---|
| `starfieldMode`（只读布尔） | ★ **唯一判据**：是否由星域容器驱动（UI 据此隐藏战报、启用移动相关 UI） |
| `takeUnit(id)` / `adoptUnit(unit, side)` | **同实例搬迁**的最小接口（仅容器使用；保全自身状态、清除外部来源修饰、静默、离场≠阵亡） |
| `injectUnit/removeUnit` | 已由 `takeUnit/adoptUnit` **替换**（迁移改为整体搬迁） |
| `step()` / `log` / `logTotal` | 手动推进一 tick；实例私有战报（星域各区互不污染，也不写全局通道） |
| `opts.summonCountOf(side, moduleId)` | 召唤上限计数注入点（有 ⇒ 跨星区统计；无 ⇒ 原本实例计数） |

### 3.3 关键只读字段（单位侧）

`ship.coefficients.nav`（航行系数，走既有 `coeff()` 链）、`ship.navReadyUntil`、`ship.navCdTicks`（本步冻结值，**仅展示**）、`ship.baseNavCdTicks`/`baseNavEnergyPerTick`（配置解析基准）。
唯一读函数：`navCoeffOf(ship)`（＝`coeff(ship,'nav')`）、`navCdTicksOf(ship)`、`navEnergyPerTickOf(ship)`。

---

## 4. 铁律（改动时不可违反）

1. **Pass1 零数值变化**：`step()` 的首遍遍历不得产生任何数值变化；跨区落地一律放 `crossSectorPhase()`。
2. **单一落地阶段**：所有星区间影响（当前＝移动/充能/召唤计数）**只在跨星区阶段**处理，且该阶段在**全部星区 `step()` 之后**。
3. **唯一口径**：校验（`validateStarfieldConfig`）、单位/模块校验（`unitsProblems`）、随机（`core/rng.js`，且**只有界面可用 `randomSeed()`**）、时长（`formatTickSeconds`）、图标（`unitIcon`）、进度条比例（`ratioOf`）……**都只允许一套实现**；不新造第二套。
4. **确定性可复现**：同配置＋同种子（＋同操作序列）⇒ 完全一致。随机一律走 `createRng(seed)` 与**分区子流** `fork('count:'+typeId)` / `fork('layout')` / `fork('sector:'+index)` / `fork('playerUnits')`；**不消耗随机的分支不得偷偷消耗**（如"单元素候选 ≡ 原单值"）。
5. **不硬编码类型 id / 不写死色值**：类型能力一律由注册表字段派生（`placement`/`fill`/`kind`/`isStar`）；颜色只写 CSS 变量名。
6. **UI 只读引擎口径**：UI 只渲染 `sectors`/`unitNav`/`summary` 等只读结果，**不自算**规则；呈现层换算（坐标→行列、秒数、缩放、路径渲染）除外。
7. **零回归**：单星区玩法（`LS.drill()`）、战斗屏、既有货物/模块/编队体系不得因星域改动而变化；星域能力一律用"**仅有容器时才启用**"的注入方式接入（`starfieldMode`/`summonCountOf`/`fleetPolicy` 钩子）。
8. **注释与文件安全**：块注释内**禁止出现 `**/` 序列**（会被解析为注释结束 ⇒ 整个模块语法错误、游戏白屏）。

---

## 5. 扩展指南（常见改动该动哪里）

| 想做的事 | 动哪里 | 注意 |
|---|---|---|
| 新增**星区类型** | 新建 `data/sectorTypes/<id>.js` + 注册 `index.js`；加 `nameKey` 词条与 `--sector-*` 变量（若需） | 由 `placement` 决定位置；若要让它在无其他类型占位时被填充，需 `fill:true`（**全仓恰一个**） |
| 新增**难度档/玩法** | 新建 `data/starfields/<id>.js` + 注册 `index.js` | 纯数据；界面下拉自动出现 |
| 新增**NPC 列表**（内置） | 新建 `data/npcLists/<id>.js` + 注册 | 或直接在配置界面用**内嵌** `custom:` 列表（导出随同一份 JSON） |
| 改**生成规则**（布局/数量/内容） | `data/starfield.js` | 保持 `fork` 分区语义与告警码；**不得静默**（截断/补位要有告警或明确字段） |
| 新增**特殊星区效果** | 数据 `specialEffect` 占位 → 在生成器透传 → **在跨星区阶段或对应星区实例内**落地 | 别放进 Pass1；注意确定性 |
| 新增**跨区交互**（增援/输送/跨区目标） | **`crossSectorPhase()`** 内新增子阶段 | 用固定顺序（`sectors[]` 序 × 区内序）；只读口径同步暴露给 UI |
| 改**移动/充能数值** | 船型 `navCdTicks` / `navEnergyPerTick` / `coefficients.nav` | **以配置文件数值为准**；改后自检锚点自适应 |
| 改**移动 UI** | `ui/battleView.js`（卡/详情）、`ui/starfieldMapView.js`（格/侧栏/拖拽） | 只读 `unitNav()`；不自算剩余时间 |
| 改**结算内容** | 容器 `settle()` 与 `settleResult` | 目前为占位出口（`{todo:true,...}`） |
| 让星域**后台自驱** | 地图视图 `onTick` 一处 + 容器订阅 | 现行裁定是"地图打开时才推进"（见 `战场大地图(星域)说明.md` 顶部） |

---

## 6. 自检与调试入口（控制台速查）

| 入口 | 内容 |
|---|---|
| `LS.rng.selfTest()` | 确定性 PRNG（9 项） |
| `LS.starfieldData.selfCheck()` | 数据层与配置（12 项：注册表/结构/引用/JSON 往返/星门四方位/中心唯一/i18n 成对/`fill` 唯一/`npcLists` 前缀与冲突…） |
| `LS.starfieldGen.selfCheck()` | 生成器（15 项：同种子全等/异种子不同/不改入参/数量区间/截断与补位告警/星门外缘/中心唯一/fork 分区/JSON 往返/单元素等价/内嵌 vs 内置对拍…） |
| `LS.battle.selfCheck()` | 实例隔离（7 项：并行互不影响/战报隔离/同编队逐 tick 全等/全灭不结束/空编队/不写全局战报…） |
| `LS.starfield.moveSelfCheck()` | 单位移动（15 项：零回归/阶梯路径/充能公式与冻结/同 tick 顺序/目标变更与取消/整体搬迁保状态/外部状态清除/能量门控/异常清理/召唤物不随行与守恒/模块行为…） |
| `LS.starfieldData.list()` | 类型 / NPC 列表 / 星域档 / 内嵌列表摘要 |
| `LS.starfield.create('h1','demo')` → `LS.starfield.summary()` | 创建星域并看摘要（`step(n)` 返回含 `ms` 计时） |
| `LS.starfield.unitNav(id)` / `moveQueue` / `battleOf(i).units()` | 单位移动与星区实例速查 |
| `LS.ui.openStarfieldConfig()` / `LS.ui.openStarfieldMap()` / `LS.drill()` | 配置界面 / 大地图 / **单星区编队测试（原编队入口，已从 UI 移除）** |

---

## 7. 已知占位与后续待办

**占位数值（【占位预填 · 待用户调校】）**
- 星区类型数量区间、矿物储量、货物件数与吨位/等级、NPC 列表单位构成与等级（`data/sectorTypes/*`、`data/npcLists/*`、`data/starfields/*`）。
- `navCdTicks` 默认 **200t**；`navEnergyPerTick` 默认 **2**（★ 注意：2/tick 大于巡航回能 ≈1.2/tick ⇒ 空闲单位充能会周期性停滞、一次充能约 1.67×cd，调小即可顺畅）。
- `nav_coeff_add`（航行推进器）Lv1 +0.1 / Lv2 +0.2 / Lv3 +0.35 / Lv4 +0.5。
- 星区类型色 `--sector-*` 与贴图字段 `texture`（全 `null`，有待美术提供后填路径即可，无需改代码）。

**后续轮次候选**
1. 星区**特殊效果**（数据 `specialEffect` 已占位透传）。
2. **跨区相互作用**：增援、跨区目标选择、资源/货物输送（`crossSectorPhase` 已预留子阶段位置）。
3. **最终结算**内容与形式（`settle()`/`settleResult` 现为占位）。
4. **星域存档扩展**（当前存档体系未覆盖星域）。
5. **敌 AI 跨区行为**（现敌方不移动）。
6. 星域**后台自驱**（若改口径，改动面＝地图 `onTick` 一处 + 容器订阅）。

---

## 8. 历史坑位（复现成本高，改前先看）

1. **注释内的 `**/`** ⇒ 注释提前闭合 ⇒ 整个模块 `SyntaxError`、游戏白屏（已在 `systems/starfield.js` 发生过一次）。写注释后务必自查 `\*\*/` 与行内 `*/`。
2. **`<input type="number">` 读出的是字符串** ⇒ 交给引擎校验（`Number.isInteger`）会误报"应为整数"；**表单数值必须先数字归一**再校验（`normalizedCfg()`）。
3. **进度条"只有灰条"**：`.bar-fill{height:100%}` 在战斗屏生效是因为父元素是 `display:grid`（被块级化）；放进普通 `<span>` 容器后必须**显式 `display:block`** 才有高度/宽度。
4. **货物芯片"卸载无效"**：无冷却的装载光束会立刻把货物装回 ⇒ 用 `manualUnloaded` 标记（后改分阵营带时限）。
5. **星域格内进度条比例**：分母必须是 `oreInit`/`cargoInit`（初始值），**超出初始值一律满条**、值为 0 隐藏（曾误用展示常量导致抖动）。
6. **召唤上限按"本星区实例"计数** ⇒ 单位迁区后会**重复召唤**（表现为"召唤物跟随并多出一只"）；修复＝`summonCountOf` **跨星区统计**。
7. **路径"先 q 后 r"是直角**；用户要求**均匀阶梯**（Bresenham 交替，`|Δq|==|Δr|` 定为先 q）；★ 改步进顺序会**改变个别目标的可达性**（如 `(0,−2)→(1,0)` 由不可达变可达）。
8. **非确定项**：`entities/ship.js uid()` 使用 `Date.now()+随机` ⇒ **单位 id 不可用于确定性对拍**（自检一律用对象同一性/出场序号/区间断言）。
9. **`radius` 误报**（`h1`）与 **`navThruster` 非单调**（把"两个候选值"误当 Lv1/Lv5）均属"口径未说清"类问题；**改动前先确认口径**。
