# 主基地框架说明

> 本文档为**主基地（M3）的确定性设计与口径文档**（由 AI 随开发维护，先定口径再开发）。
> 定位：`游戏实现大纲.txt`（总纲）→ 本文件（主基地框架细则）→ `开发步骤与开发清单.md`（步骤与勾选）。
> 相邻文档：`星域开发要点.md`（星域索引，出征的目标场景）、`模块字段说明.md`（模块词条）、`战报日志开发要点.md`（战报口径）。
>
> **状态：口径已定稿（本文档即开发依据）；M3a~M3f 逐步实现中。**

---

## 1. 目标与范围

### 1.1 一句话目标
做出**主基地**：一个**左侧建筑列表 + 右侧面板**的基地屏，含 **船坞 / 舰队 / 指挥中心 / 星门 / 研究站 / 星球** 六个入口，串起「基地造船 → 星门出征（星域）→ 返回基地 → 研究货物解锁模块 → 再造更强的船」的**闭环**。

### 1.2 M3 验收闭环（人工测试 #3）
基地可进入 → 船坞造 2 艘船（可改装、可升级）→ 指挥中心决定出战上限 → 星门选战区出征（进入星域）→ 战斗中把基地船开到**星门星区**并**主动返回**（或结算时仍在星门星区存活）⇒ 这些船**返回舰队并自动修复**；带回的**货物**进入研究站 ⇒ 研究产出**模块蓝图** ⇒ 船坞据此**解锁/升级模块** ⇒ 再造更强的船。

### 1.3 明确不做（M3 之外）
- **星球产业**（E5）：入口**占位不可用**（提示"本阶段未开放"），产业建造与产出属 M4。
- **资源产出链**（F1/F2/F3/F4 的完整玩法）：M3 只做**资源账户与消耗校验**，产出只接**星域带回**（矿物/货物）。
- **科技树**（F5 完整形态）：M3 只做**模块蓝图解锁/升级**这一条链路。
- **基地存档**：M3 **不存档**（内存态，刷新即重置）；M3f 一次性补档。

---

## 2. 资源体系（M3 启用五种）

| 资源 | 键 | 定位 | M3 用途 |
|---|---|---|---|
| **能量币** | `energy` | ★**一切活动的基础消耗** | 建造、改装、升级、研究、出征**均消耗**（占位数值见表） |
| **矿物** | `ore` | 原料 | 建造高阶船只/部件的**副消耗**；由星域**采矿入库**带回 |
| **合金** | `alloy` | 建造材料 | 船体与建造的**主材料** |
| **稀土** | `rare` | 建造材料（稀有） | 高等级建造/升级消耗（占位：等级 ≥3 才需要） |
| **科研点** | `science` | 解锁科技 | 研究站**加速/替代**部分研究消耗（占位：可缩短研究耗时） |

**口径**
1. **资源是基地级账户**（`baseState.resources`），与战斗内的"矿物(ore)/货物(cargo)"**不是同一层**：战斗内资源属于**单位/星区**，只有经过"返回"才转换为基地资源。
2. **转换规则（M3）**：
   - 单位从星门返回时，其**携带矿物 `hull.ore`** ⇒ 累加进基地 **矿物**；
   - 其**已装货物 `cargos`** ⇒ 进入**研究站的货物库**（不折算资源，供研究消耗）；
   - 未返回（被击毁/未回星门）的单位，其携带资源**一并损失**。
3. **初始额度**【占位预填 · 待用户调校】：能量币 `2000`、矿物 `300`、合金 `500`、稀土 `60`、科研点 `0`。
4. **M3 唯一额外来源**：调试台（控制台）可加资源，用于测试；**不做**基地自动产出。
5. **显示与容量上限**：基地屏顶部资源栏常驻显示五种资源；**每种资源有容量上限**（数值＝`initialResourceCaps` ＋ Σ各建筑该等级 `effect.resourceCap`，**上限不进状态、随配置变化**），**悬浮显示"当前 / 上限"**，达上限加轻微标识；`gain` **按上限截断**（被拒部分返回 `overflow`）。
6. **资源配色语义（全局统一）**：能量币＝黄、**矿物＝紫**、**合金＝银**、**稀土＝绿**、科研点＝强调青；**货物＝橙**（`--cargo`，不被任何资源借用）。列表中的造价/返还**只显示资源图标 + 数量**，不显示资源名文本。
7. **不足时的表现**：按钮**禁用（灰）**并给出**简短**原因（悬浮 `title` 短语，如"资源不足/槽位已满/容量已满"），**不铺行内说明文字、不弹窗、不报错**。

---

## 3. 界面结构

### 3.1 路由与形态
- 新增路由 **`base`**（`ui/baseView.js`），主菜单入口"主基地"。
- **左列表 + 右面板**：左侧为**建筑列表**（**5 项：星门 / 指挥中心 / 研究站 / 船坞 / 星球**，`order` 连续且唯一），右侧为**当前建筑面板**（默认选中「星门」）。★ **"舰队"不再是独立列表项**：舰队与出征状态**并入「船坞」**面板（见 4.1）。
- 顶部：**基地资源栏**（五种资源）+ 返回主菜单按钮。**不做**基地名称/玩家名等元数据（M3 不需要）。
- 切换建筑**不重建整屏**：左列表选中态 + 右面板按需重建（沿用既有"只改变化项"体例）。

### 3.2 建筑与面板职责
| 左列表项 | 右面板内容 | M3 状态 |
|---|---|---|
| **星门** | 战区（难度档）选择 + 本次"**星域编号**"展示 + 出征编队装载 + 出征按钮 | 实装 |
| **指挥中心** | 等级、出战上限、增益（占位）、升级按钮与消耗 | 实装（数值占位） |
| **研究站** | 返回货物库（按类型堆叠）+ 研究队列（进行中/已完成）+ 蓝图列表 | **实装** |
| **船坞** | **单位配置表**（配置名称 / 单位图标 / 单位类型 / 等级 / 模块图标（含等级，`module-chip`）/ 空占位列 / 造价 / 返还 / 数量 ＋ 换行后的操作区）＋ **配置编辑弹窗**（内含**模块二级选择弹窗**）；**舰队与出征状态并入此面板** | 实装（M3b） |
| **星球** | 占位提示"本阶段未开放"（并展示各资源种类说明） | 占位 |

> 说明：**舰队＝船坞内的"单位配置 + 数量"表**（不再单列）；出征编队的选择放在**星门**面板内完成（避免两处都能改编队）。

---

## 4. 各建筑细则

### 4.1 船坞（E4，实装 · ★ 舰队＝"单位配置 + 数量"）
**模型（用户口径）**：基地舰队**不是逐艘实例**，而是**抽象的单位配置条目 + 数量**：
`fleetConfigs[] = { id(fc1/fc2…单调不回收), name, shipId, level, modules[{moduleId, level}], count(拥有总数), out(出征中数量) }`；可用数＝`count − out`。

1. **配置条目**
   - **相同配置合并**：同一性键＝`shipId + level +` **排序后的模块多重集**（`[A,A] ≠ [A]`、`[A@1,A@2] ≠ [A@1,A@1]`；`[A,B] ≡ [B,A]`——顺序不参与同一性，**但存储保留录入顺序**）。命中既有 ⇒ **合并，不新建**；未命中（改了船型/等级/模块）⇒ **原条目保留、新建一条**。
   - **手动命名**，重名自动追加 `#2`/`#3`…（克隆链不滚长）；**名称留空 ⇒ 取默认名＝该语言的"单位名称"**（i18n `base.fleet.defaultName = '{ship}'`，**不含"编队"字样**，因为这是**单位的配置**）。
   - **已建造的不可修改**：要改只能**"编辑"＝以现有配置为模板另存为新配置**（克隆，`count = out = 0` 起）；**仅改名称**时按同一性合并回原条目并**以新名为准**（数量与在外原样保留）。
   - **删除**：仅当 `count === 0 && out === 0` 时可删（否则灰按钮 + 悬浮原因）。
   - **排序**：顺序**就是 `fleetConfigs[]` 的数组顺序**（不另设 order 字段，快照按同序输出）；**最左把手＝`(↑ ⠿ ↓)` 横向三件套**，**该区域即可拖动**（拖动 ≡ 上/下移，引擎同一入口）。
2. **模块可重复、可设等级**：同一模块**允许重复装配**（唯一上限＝**槽位数**）；**每个模块条目各自设等级**（1..该模块 `maxLevel`）；装配/拆下**按该等级**读模块配置的费用。★ **模块的增/删/改全部在"模块二级选择弹窗"内完成**（配置弹窗的模块列**只有图标（含等级）与空槽位占位**，不放添加/移除按钮）。
3. **两级弹窗**：配置编辑弹窗（名称 / 船型 / 等级 / 模块列 / 造价预览）+ **模块二级弹窗**（**按分类筛选**、**图标 + 名称的独立列表**、**改等级**、**显示该模块该等级的装配/拆下费用**）；底部按钮顺序 **确认、新增、移除、取消**（确认＝替换当前槽位、新增＝追加新槽位、移除＝删当前槽位、取消＝不动草稿）；Esc/遮罩关闭，**关二级弹窗不影响主弹窗草稿**。
4. **建造 / 拆解**
   - **建造**：可选初始等级；**单艘造价 ＝ 船型 `buildCost` ＋ Σ(逐级 `upgradeCost` 中 level ≤ N 的条目) ＋ Σ各模块该等级 `installCost`**（重复模块各计一份）；★ **建造高等级单位的解锁/升级条件写在船型配置里（如消耗资源），不由货物决定**（`unlockByLevel` 预留字段，非空即"尚未实装"⇒ 保守拒绝）。
   - **容量**：`Σ count ≤ 船坞该等级 effect.fleetCapacity`（**船坞等级＝舰队总容量**；**指挥中心等级＝出战上限**，两者独立互不换算）。
   - **拆解**：返还＝**逐资源 `floor(单艘累计花费 × n × 船坞该等级 effect.scrapRefundRatio)`**（先消浮点误差再取整；不足 1 不返还；只能拆**未出征**部分 `count − out`）。
5. **列表呈现**：列表**只显示资源图标 + 数量**（**不显示资源名文本**，完整信息进悬浮提示）；数量文案为 **`N 艘 | M 在外`**；信息列**可换行**（不使用横向滚动），**造价/返还/数量作为一组右对齐**；操作行**左＝编辑·删除，右＝建造·拆解**；不可操作 ⇒ **按钮灰 + 悬浮短语原因**（**不铺行内说明文字**）。
6. **拆除/退役**：见上（拆解返还）；**不做**"退役返还配置本身"等额外玩法。

### 4.2 舰队（并入船坞 · M3b 口径）
1. 舰队＝**船坞面板内的单位配置表**（每条配置的 `count` 与 `out`），**唯一来源**是 `baseState.fleetConfigs`。
2. **出战上限**由**指挥中心等级**决定（`effect.maxFleet`，见 4.3）；**舰队总容量**由**船坞等级**决定（`effect.fleetCapacity`）。两者**独立**：容量是"基地能拥有多少"，上限是"一次能带多少"。
3. 出征时按配置**选取数量**（≤ 出战上限）：**`count` 保留、`out` 增加**（用户口径："数量保留 + 另记在外数"）；返回 ⇒ `out` 减；损毁 ⇒ `count` 与 `out` 同步减。
4. 出征单位由**配置**（`shipId/level/modules`）生成**出征实例**注入星域；**基地配置条目本身不被修改**。

### 4.3 指挥中心（E2）
1. 等级 1~5【占位】，升级消耗随等级递增（能量币 + 合金 + 稀土，数值占位）。
2. 效果：**出战上限**（4.2）＋**占位增益**（M3 仅登记字段与显示，实际增益待 M4 定义，**不得**伪造未实现的战斗加成）。

### 4.4 星门（E1）
1. **战区选择**：列出 `data/starfields/*`（现有 h1/h2/h3 三档，界面显示其名称/半径/持续时间/难度描述）。
2. **星域编号**：每次出征**随机生成**（内部即既有 `randomSeed()`／种子），界面**一律称"星域编号"**，**不出现"种子"字样**、**不提供输入**；编号用于**复现与排障**（控制台可用编号重建同一星域）。★ 该编号**不承诺可由界面输入**。
3. **出征**：校验（选船数 ≥1 且 ≤ 上限）⇒ 用"**基地舰队实例**"替换星域配置中的 `playerUnits`（每艘船按其 `shipId/level/modules` 注入）⇒ 创建星域并进入 `starfieldMap`。
4. ★ **配置界面转为调试通道**：`ui/starfieldConfigView.js` 及其入口**只保留控制台函数** `LS.ui.openStarfieldConfig()`（UI 中不再有按钮/入口）；其内的**玩家单位手填**保留，但标注**调试用**。
5. **返回**：见 §5。

### 4.5 研究站（E3，实装）
1. **货物库**：从星门返回的货物**全部存放于此**，按**类型 + 等级**堆叠入库（`baseState.research.cargoStore`）。★ **货物等级与加成定义在 `js/data/cargos/*` 的货物配置文件里**，由星域中带出的**货物实例**决定其等级。
2. **一次研究一个货物**（研究位固定 1；**未来可升级为多个研究位**，字段预留）。
3. **研究流程**：选定**一个货物（类型 + 等级）** ⇒ 校验数量与资源 ⇒ **消耗该货物** + **消耗能量币**（两者数值写**货物配置文件**）⇒ **耗时 T ticks**（★ **研究耗时写入各货物配置文件**，逐级可覆写；界面按秒显示，走既有 `formatTickSeconds`）⇒ 完成后**给予一定数量的「对应种类蓝图」**，数量由**货物等级 + 加成**决定（取值链与参数均在货物配置文件中；加成来源 M3 预留字段，先按"无加成"计算）。
4. **蓝图＝门槛（不消耗）**：`baseState.research.blueprints[moduleId] = 数量`。★ **解锁/升级条件＝该模块的蓝图数量达到定值**（定值写在**该模块配置文件**里、按等级递增）；**达标即满足，蓝图不扣除**。
5. **科研点＝消耗**：解锁/升级时按**模块配置文件**登记的科研点消耗扣除（不达标 ⇒ 不可执行）。科研点 **M3 无产出**（初始额度 + 调试发放）；**未来由玩法生产**（M4）。
6. **数值出处（唯一）**：货物侧（需求数量/能量币/耗时/蓝图产出规则 与加成）⇒ **货物配置文件**；模块侧（蓝图门槛/科研点消耗，逐级）⇒ **模块配置文件**；船型侧（建造/升级/槽位）⇒ **船型配置文件**；基地建筑侧（等级/升级消耗/效果）⇒ **基地建筑配置文件**。★ **base.js 与 UI 不得硬编码任何数值**。
7. **推进**：研究耗时**按基地屏打开时的全局 ticker 推进**（与星域"地图打开才推进"同口径）；**离开基地不推进**。
8. **M3 不做**：物品掉落（F4）、科技树分支（F5）、蓝图消耗/交易、多研究位。

### 4.6 星球（E5）
占位不可用：面板显示"**本阶段未开放**"，并**列出各资源种类及其用途**（§2 表格的精简版）。

---

## 5. 出征与返回闭环（★ 关键口径）

### 5.1 状态机
```
基地（dock） --出征--> 出征中（out，位于星域） --返回--> 修复并回 dock
                                   └--未返回（击毁/结算时不在星门星区）--> lost（需重建）
```

### 5.2 返回判据（★ 用户口径）
1. **返回点＝星门星区**（星域内 `typeId` 为**入口类型**的星区，即玩家入场星区；**不硬编码 id**，取容器 `playerEntryIndex`）。
2. **主动返回**：单位**位于星门星区**时，在侧栏**点"返回基地"按钮**（仅当该单位在星门星区时可用）⇒ 立即返回基地。
3. **结算自动返回**：星域**结束时**，**仍在星门星区存活**的基地船**自动返回**。
4. **不返回**：被击毁的、以及结束时**不在星门星区**（含仍在航行途中）的船 ⇒ `lost`，**需重建**（不带回任何状态与携带物）。
5. ★ **不存在"战斗结束即自动返回"**：星域内其他星区存活**不算**返回。

### 5.3 返回后的处理
- **回基地即修复**：**满血满盾满能量**、模块冷却归零、**模块配置与等级完整保留**（保留单位模板）；
- **携带物转换**：携带矿物 ⇒ 基地**矿物**；已装货物 ⇒ 研究站**货物库**；
- 该船 `status` 回到 `'dock'`，回到舰队，可再次出征。

### 5.4 出征装载口径（按 M3b 新模型修订）
- 出征时**按单位配置**（`shipId/level/modules`）生成**出征实例**注入星域（星域侧按同一规格展开），**基地的配置条目与 `count` 保持不变**，只把 **`out` 增加**相应数量；
- **返回时**：`out` 减、该数量的单位回到基地并**自动修复**（同 5.3）；M3 中出征期间**不可改装**（配置不可改），故"配置与实战一致"天然成立；
- **损毁/未返回** ⇒ 该数量**从 `count` 与 `out` 中一并扣除**（资源**不返还**，即需重新建造）。

---

## 6. 数据与文件（新增/改动清单）

**新增**
| 文件 | 作用 |
|---|---|
| `js/systems/base.js` | **基地状态与逻辑唯一来源**：`createBaseState()`、资源校验/扣减、船坞（建造/改装/升级）、舰队与出战上限、指挥中心升级、星门出征装配、研究站推进 |
| `js/ui/baseView.js` | 基地屏（左列表 + 右面板）与六个面板 |
| `js/data/blueprints.js` | **货物类型 ⇒ 模块**的研究映射表（占位）， `js\data\cargos`中已有货物种类|
| `js/data/baseConfig.js` | 基地数值表（初始资源、建造/升级/研究消耗、耗时、上限公式的**唯一出处**，全部标注占位） |

**改动**
| 文件 | 改动 |
|---|---|
| `js/ui/router.js` / `js/main.js` | 新路由 `base`；主菜单加入口；`LS.base` 门面（含调试函数） |
| `js/ui/starfieldConfigView.js` | 入口**只留控制台函数**（UI 按钮移除）；玩家单位手填标注"调试" |
| `js/systems/starfield.js` | 出征装配：接受**外部玩家单位规格**（由基地提供）；**返回判定**钩子（星门星区／返回动作）与"结束自动返回"结算口径 |
| `js/ui/starfieldMapView.js` / `battleView.js` | 侧栏「**返回基地**」按钮（仅星门星区内的基地船可用）；返回后短提示 |
| `js/i18n/zh-CN.js` / `en.js` | 基地相关词条（`base.*`、`res.*`、`blueprint.*`）成对补齐 |
| `css/screens.css` | 基地屏样式（`.base-*`），**复用既有变量**，不新增色值 |

---

## 7. 单一来源与接口

- **基地状态唯一来源**：`systems/base.js` 的内存对象 `baseState`（M3 不持久化）；UI **只读**其快照，**不自算**资源与上限。
- **数值唯一出处**：`data/baseConfig.js`（所有消耗/耗时/上限公式）。**禁止**在 UI 或引擎里硬编码数值。
- **船型可建造性**：数据字段 `buildable`（**非全部船型可造**；NPC 专用船型不可造）。
- **蓝图与解锁**：`baseState.blueprints`（`{moduleId: maxLevel}`）是**唯一解锁判据**；船坞安装模块必须查它。
- **控制台门面（调试）**：`LS.base.debug`（加资源、加货物、发蓝图、清空基地、`openStarfieldConfig()` 等）—— **仅供调试**，UI 不暴露。
- **不硬编码**：星门星区一律取自容器 `playerEntryIndex`；资源/建筑/模块一律走注册表。

---

## 8. 铁律与零回归

1. **零回归**：既有战斗/星域/演练编队屏行为不变；基地只通过**注入玩家单位规格**与**新增返回钩子**接入星域。
2. **确定性**：基地侧 id 用**自增序号**；随机只用于"星域编号"（`randomSeed()`，UI 唯一随机源）；研究产出按**表顺序取第一个未解锁**，不消耗额外随机。
3. **UI 只读引擎**：基地面板不自行计算消耗/上限/耗时，一律读 `base.js` 快照。
4. **不存档（M3）**：刷新即重置；M3f 统一补档（`schemaVersion` 预留）。
5. **不做未实现的加成**：指挥中心增益等若不实装，**只显示"待开放"**，不得伪造战斗数值。
6. **文档同步**：本文件与 `开发步骤与开发清单.md` 的 M3 行**同步维护**。

---

## 9. 开发拆分（M3a~M3f）

| 步骤 | 内容 | 验收 |
|:---:|---|---|
| **M3a** | 路由 `base` + `ui/baseView.js` 骨架（左列表 + 右面板 + 资源栏）+ `systems/base.js` 状态与**资源账户/校验** | 主菜单可进基地；五种资源显示；控制台可加/扣资源 |
| **M3b** | **船坞**：建造 / 改装（槽位与蓝图校验）/ 升级（等级→属性与槽位） | 造 2 艘船、换模块、升 1 级且槽位 +1（占位规则） |
| **M3c** | **舰队 + 指挥中心**：舰队列表（dock/out）、出战上限公式、指挥中心升级 | 指挥中心升级后出战上限提升 |
| **M3d** | **星门**：战区选择 + **星域编号**（随机、不称种子）+ 出征装配（基地舰队 ⇒ `playerUnits`）+ 进入星域；**返回闭环**（侧栏「返回基地」＋结束自动返回＋修复＋携带物转换＋`lost`） | 出征能带基地船进星域；在星门星区返回后回舰队且满状态；未返回则需重建 |
| **M3e** | **研究站实装**：货物入库、研究（消耗+耗时+科研点加速）、蓝图产出与在船坞生效 | 研究一条货物类型 ⇒ 解锁/升级一个模块，船坞可装 |
| **M3f** | **导航闭环 + 存档**：任意屏 ↔ 基地 ↔ 菜单、战斗/星域结束回基地、基地/舰队/研究/蓝图全部入档与恢复 | 关页重开状态一致（含进行中的研究） |

**每步结束**：AI 自检（`node --check` + 该步自检入口）→ 给出《测试期望》+《测试方案》→ 待用户确认后进入下一步。

---

## 10. 数值落位（★ 一切数值写各自配置文件，由配置文件决定）

**原则**：`systems/base.js` 只实现**规则与校验**，**不持有任何数值**；UI 只读快照、不算数值。所有消耗/耗时/门槛/产出规则都落在**对应实体的配置文件**里，逐级（按等级）可覆写。

| 数值 | 落在哪个配置文件 | 需要的字段（占位命名） |
|---|---|---|
| **单位建造/升级消耗** | ★ **单位（船型）配置文件** `data/ships/<id>.js` | `buildable`、`buildCost{能量币,合金,矿物?,稀土?}`、`upgradeCost`（逐级条目）、`slots`（基础槽位）＋ `slotGrowth`（成长规则，**仅用于校验，不叠加到 `slots`**）、`unlockByLevel`（高等级解锁条件，**不由货物决定**） |
| **舰队容量 / 拆解返还比例** | ★ **船坞配置文件** `data/baseBuildings/shipyard.js` | `levels[].effect.fleetCapacity`、`levels[].effect.scrapRefundRatio` |
| **出战上限** | ★ **指挥中心配置文件** `data/baseBuildings/commandCenter.js` | `levels[].effect.maxFleet`（**与船坞容量独立**） |
| **改装消耗**（装卸模块） | ★ **各模块配置文件** `data/modules/**/*.js` | `installCost{…}` / `removeCost{…}`（缺失则视为免费） |
| **模块升级消耗** | ★ **各模块配置文件** | `upgradeCost`（逐级条目） |
| **模块蓝图门槛** | ★ **各模块配置文件** | `blueprint`（逐级所需**蓝图数量**，**门槛、不消耗**） |
| **科研点消耗** | ★ **各模块配置文件** | `scienceCost`（逐级；**消耗**） |
| **基地建筑**（星门/指挥中心/研究站/船坞/星球） | ★ **基地建筑配置文件**（新增 `data/baseBuildings/<id>.js` + `index.js`，一建筑一文件） | `id/nameKey/maxLevel/levels[{cost, effect}]`（消耗与效果都写在配置里） |
| **研究耗时** | ★ **各货物配置文件** `data/cargos/<id>.js` | `researchTicks`（逐级可覆写） |
| **研究消耗** | ★ **货物配置文件** | `researchCost{energy}`（★ **无"件数"**：一次研究一个货物，**一个即有产出**） |
| **货物等级 / 加成** | ★ **货物配置文件** | 等级定义（`levels[]`，逐级可覆写）与**加成字段 `bonus`**（**没有"加成模式"**，加成**只由 `bonus` 决定**；M3 先按 0 加成） |
| **蓝图产出数量** | ★ **货物配置文件** | `blueprintOutput{ baseByLevel[{level,count}], bonus, targetModuleId }`（产出＝基础数量经 `bonus` 加成） |
| **初始资源额度 / 资源容量上限** | 基地配置文件 `data/baseConfig.js`（**唯一**保留在此的数值：基地初始状态） | `initialResources{…}`、`initialResourceCaps{…}`（容量上限**暂定**由此给出，**未来由星球建筑 `effect.resourceCap` 提升**） |
| **研究位数量** | 基地建筑配置文件（研究站 `levels[].effect`） | M3 固定 1，字段预留 |

> 所有占位数值一律标注【占位预填 · 待用户调校】，且**只在上述配置文件里出现一次**（禁止同值两处定义）。
> 出战上限、指挥中心增益等**派生值**一律由配置字段（等级→效果）计算，**不额外硬编码公式常量**。

---

## 11. 未定项（开发中遇到再定，需用户裁决时先问）

1. **星门"战区"与星域档位的对应**：M3 直接列 h1/h2/h3（现为三个难度档），是否要在其上再加"战区 1/2/3"的叙事层（M5 区域等级 G2）。
2. **指挥中心增益**的具体内容（M4 定义）。
3. **货物类型清单**与模块映射表的正式版本（M3e 需先给一版占位）。
4. **多研究位/研究站等级**是否在 M3 之后开放。
5. **基地存档**的键名与迁移策略（M3f）。
