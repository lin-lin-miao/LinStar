/* ===== data/modules/mining/oreEnrichment.js —— 矿藏富集（采矿 · 星区储量乘法增幅） =====
 * 功能：【主动】按**乘法**提升**星区矿物储量**（词条 `sector_ore_mul`），**无目标**（不进目标选择链）。
 *   - 词条 `sector_ore_mul`：**星区储量的增量比例**（0.1 ⇒ 当前剩余储量 ×1.1）。
 *     **作用于「当前剩余储量」**（不是初始储量）；结果**取整**（`Math.round`，沿用储量恒为非负整数的口径）。
 *     ★ **纯比例、不乘任何系数**（与创世纪不同：加法才乘采矿系数，乘法的系数口径用户未要求）。
 *     ⚠ 作用对象＝**星区**（战场全局），既非「自身词条」也非「目标词条」；归属按模块拥有者 `ownerOf(inst)`。
 *   - **混合冷却口径（★ 用户确认）**：与创世纪完全同体例——必须**同时**满足
 *       ① 模块实例自身冷却已就绪（`cooldown_ticks`）；② 星区侧**本模块**的冷却已结束
 *       （参与判据＝独立词条 **`sector_cd_ticks`**（按词条识别、不硬编码模块 id）、时长＝**该词条值**）。
 *     两把冷却互相独立、各自计时；与创世纪的星区冷却**互不影响**（key ＝ 模块 id）。
 *     门控落在 Pass1 **唯一门控出口** `canImpact`：不满足 → **不激活、不耗能、不进冷却**。
 *     ⚠ 附加门控（与采矿激光“剩余容量为 0 → 不激活”同体例）：**星区剩余储量为 0 时乘法无任何效果** ⇒
 *       同样不激活（不白耗能、不进冷却）；该判据只有加法（＋固定值）不受影响。
 *   - **同一 tick 多个单位携带同一模块**：按固定结算顺序（我方 → 敌方）**只接受第一个**，其余不激活。
 *   - **结算统一落地**：Pass1 只记意图（`__pending.sectorOps`，**零数值变化**）；
 *     **结算步骤 3c**：**先加法、后乘法** —— 加法（全部求和）落地后，本词条按固定结算顺序**逐条**作用在
 *     「当前剩余储量」上、**每条各取整一次**（多条乘法＝连乘，各次取整）；星区储量**不封顶**；
 *     星区冷却在同一步写入（`sectorCdUntil`）。
 *   - 分类＝**采矿**；**战报**＝每模块每次激活至多 1 条 `battle.log.sectorOreMul`
 *     (`{owner}的{module}：星区矿物 ×{mul}（+{n}）`，`n`＝该条落地前后差值；低频关键事件)。
 *   - 无 `icon`：暂无对应 SVG 素材 → 走既有降级（显示名称首字「矿」）；素材就位后补 `icon` 即可。
 * 数值说明：【占位预填】×1.1（＝`0.1` 增量比例）为 **Lv1 基准**、逐级递增占位，由用户逐级人工调校。
 *   ⚠ 乘法为**复利式**增长（每次激活都乘在当前剩余上），等级越高差异越大，调参时请注意。
 */
export default {
  id: 'oreEnrichment',
  nameKey: 'module.oreEnrichment', // i18n -> 矿藏富集 / Ore Enrichment
  name: '矿藏富集',
  category: 'mining',
  // ★ 无目标：`kinds` 缺省 ⇒ 候选池为空、不进入目标选择链（星区词条，作用于星区储量）
  target: {},
  effects: {
    // 无特殊钩子标签（`type` 省略）：既不是 weapon（无 damage），也不走召唤/控制/时长类分支
    sector_ore_mul: 0.1, // Lv1 占位：星区剩余矿物储量 ×1.1（增量比例 0.1；**纯比例不乘系数**；结果取整、无上限）
    cooldown_ticks: 60,  // Lv1 占位：**实例自身**冷却（各级沿用 Lv1）
    sector_cd_ticks: 60, // Lv1 占位：**星区侧**冷却（独立词条；带它即参与星区冷却；各级沿用 Lv1）
    energy_cost: 25,     // Lv1 占位：每次激活耗能
  },
  maxLevel: 16,
  levels: [
    { level: 5, effects: { sector_ore_mul: 0.15 } },
    { level: 10, effects: { sector_ore_mul: 0.2 } },
    { level: 16, effects: { sector_ore_mul: 0.3 } },
  ],
};
