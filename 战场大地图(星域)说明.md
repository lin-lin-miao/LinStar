# 星域
星域是由一个或多个星区(战斗场景)的集合体，在星域中各个星区均持续正常运行(同时走tick)。
一般的星域是由一个恒星星区并围绕(星域半径)多个星区组成(类似恒星系)
星区在星域中以方形网格状呈现，按照半径围绕为一个近似的圆。
各船只(单位)可在星区间移动(待实现)
星区中不再进行胜负判断结束战斗/停止走tick。(仅不执行，需保留判断逻辑不删)

# 实现原理

> 本节为**实现设计稿**（随开发更新，由 AI 维护）。**当前状态：阶段 0 / A / B / C（含 C-3b 内嵌 NPC 列表编辑器）均已实现，正在收尾（C-4 + 阶段 D）；开发步骤见 §10。**
>
> ★ **已实现要点（现行）**：配置数据层（`sectorTypes`/`npcLists`/`starfields`，含**内嵌自定义 NPC 列表** `custom:` 前缀与"内置→内嵌→报错"解析顺序）；**带种子的确定性生成器**（方网格近似圆、`center/ring/edges` 三种 `placement`、类型数量区间、**未占用格位补为 `fill:true` 类型（格子恒满）**、NPC 多候选随机抽取、`fork` 分区子流；截断告警 `sectorCountTruncated`、补位信息 `sectorCellsFilled`）；**星域容器**（每星区一个独立 battle 实例、固定索引序逐区 `step()`、星域持续时间、`settle()` 结算入口预留、`fleetPolicy` 跨星区唯一来源、`playerUnits` 入场注入）；**星域大地图**（方网格 + 类型色/标记/贴图预留、**缩放 0.5×~2.6× 与不设限自由平移**、格内**单位图标 + `+n` 省略 + 分类色进度条**、图例栏 `nowrap` 且降到自然宽 2/3 即整栏隐藏、侧栏**默认半屏**可拖拽记忆）；**侧栏＝完整星区战斗场景**（复用战斗屏同一渲染与指令链，**不显示战报**、`#编号 类型名（坐标）` 注入星区栏、可操作指挥）；**星域配置界面**（难度/玩法、**种子唯一输入处**、半径/持续时间、类型增减与数量区间、多 NPC 列表、玩家单位列表、预览、**导出/导入同格式 JSON 且界面不显示 JSON 文本**）。
>
> ★ **tick 驱动权（已明确裁定 · 现行口径）**：**星域由「星域地图视图」驱动**——打开地图时按**全局 ticker** 推进 `starfield.step(1)`；**离开地图即停止推进**，回到地图从停止位置继续。⇒ 文首“各个星区均持续正常运行(同时走tick)”的**准确含义＝「在星域地图打开期间」**；星域**不在后台自行推进**。**暂停／调速／逐 tick 复用全局 ticker 的既有战斗控件**（语义与战斗屏一致：暂停即不 emit tick ⇒ 星域不推进；逐帧＝补发 1 tick ⇒ 恰好推进 1 个星域 tick）。**星域容器本身不持有驱动时钟**（只提供 `step()/settle()/stop()` 与只读口径）；将来若改为“后台持续运行”，改动面＝**地图视图 `onTick` 一处** ＋ 容器订阅。

## 0. 已确认决策

| 项 | 决策 |
|---|---|
| 引擎架构 | **多 battle 实例**：星域为容器，**每个星区一个独立战斗实例**（状态完全隔离）；`battle.js` 改为**可实例化**，对外只读口径形状不变 |
| 第一阶段范围 | **基础架构优先**：**配置体系 + 星域容器 + 多星区同时走 tick + 星域大地图 + 侧栏星区场景（可操作） + 关闭胜负结束**；**不含星区间移动**（跨区阶段接口预留）；**特定玩法后续待定** |
| 结束方式 | ★ 星域靠**自身持续时间**：时间耗尽 ⇒ **所有星区结束并进入最终结算**（取代手动结束与自动判胜负） |
| 难度/模式 | 星域**持续时间、半径、星区构成等均由难度/模式配置决定**；**一种难度或玩法＝一个星域配置文件** |
| 命名规则 | **难度＝`H1`～`Hn`**（一文件一档，如 `h1.js` ⇒ `id:'h1'`）；**特定玩法＝以玩法名命名**（**后续待定**） |
| 星域半径 | **编码上不设上限**，半径大小**由配置指定** |
| 随机种子 | ★ **仅在「星域配置界面」可输入**（其它地方不可输入）；**默认随机生成**；**生成/进入星域后可显示**（**显示位置待定**） |
| 星区类型 | **星球星区、矿物区、空区、特殊区**（**星门星区属于特殊星区类型**）等；类型决定生成内容与表现 |
| 星域初始化 | 依据配置（**带可预测的随机性**）生成各星区内容：**矿物、星球(货物)、单位、特殊结构**等 |
| 地图交互 | **支持缩放/平移**；显示星区状态（星球(货物)、矿物、**在星区中的单位示例**）；**点击星区 ⇒ 以「侧栏」形式显示/关闭该星区场景** |
| 侧栏场景 | ＝**完整的该星区战斗场景**，且**可操作指挥**（在侧栏内即可下达指令），非整屏切换 |
| 编辑界面 | 星域场景编辑/配置界面：**配置各类星区的数量与各项配置、各类星区的单位随机列表**；**非直接进入星域中编辑**；★ **支持导出/导入配置，导出文件与内置星域配置文件格式相同** |
| 敌我分布 | **按玩法决定**；**默认（难度阶梯模式）有固定的「星门星区」用于玩家单位进出**，**敌方(NPC) 依配置分布** |
| 其它 | **恒星区可参战**；**某些星区有特殊效果（待开发）**；**允许空星区** |
| 最终结算 | **内容与形式待定 ⇒ 本轮只预留接口** |
| ★ **界面入口调整** | **原「编队配置」（单星区模拟）从界面中分离——界面不再有入口，改为通过控制台指令打开（开发测试用）**；**原入口位置改为「星域配置界面」** |

## 1. 总体结构

```
星域（Starfield）
 ├── 星域级状态：配置文件来源、半径、持续时间/已用时间、随机种子、生成结果
 ├── 星区列表 sectors[]（每个＝一个独立战斗实例）
 │     ├── coord(q,r) / index / 显示名 / **type（星区类型）** / isStar（恒星星区，可参战）/ 特殊效果（待开发）
 │     ├── 战斗状态：单位、模块实例、护盾/血量/能量、星区储量与冷却、货物与队列
 │     ├── 初始内容：矿物、星球(货物)、单位、特殊结构（由配置 + 种子生成）
 │     └── 结束判定逻辑（★ 保留代码，但**不执行**）
 └── 跨星区阶段（预留）：移动推进 / 抵达入场 / 跨区资源汇总
```

- **单 tick 流程（确定性）**：① 星域层推进持续时间 → ② 按**固定星区顺序**（`sectors[]` 索引序）依次 `step()` 每个星区（各区内部结算全序不变：`3 → 3b → 3c → 3d-1…3d-5 → 4 → Phase B/B2 → 5 → C`）→ ③ **跨星区阶段**（本轮空实现、仅预留接口）→ ④ 时间耗尽 ⇒ 所有星区结束 + 进入最终结算（接口）。
- **铁律沿用**：Pass1 零数值变化、结算统一落地、唯一口径、镜像对等、**与遍历顺序无关**；本轮星区之间**无相互影响**（天然确定可复现）。

## 2. 星域配置文件（一种难度/玩法一个文件）

- **位置与体例**：`js/data/starfields/`（**一文件一难度/玩法** + `index.js` 注册表，体例对齐 `data/ships/`、`data/cargos/`）。
- **命名**：难度＝**`H1`…`Hn`**；特定玩法＝**玩法名**（后续待定）。
- **配置结构（★ 用户口径 · 草案字段名待定）**：
  1. **星域基础**：`id` / `nameKey` / **`radius`（星域半径）** / **`durationTicks`（星域自身持续时间）** / **`seed`（可选：默认随机生成）**；
  2. **星区类型开关**：**可「增加/减少」出现的星区类型**（启用哪些类型）；
  3. **各类型数量范围**：**每个类型的最小值/最大值**（生成时在该区间内取数，取值由种子决定的确定性随机给出）；
  4. **各类型星区的默认 NPC 列表**：引用 NPC 列表 id（见下）；
  5. **NPC 列表定义**：**单位种类与数量**（**单位配置**：种类 id、数量或数量区间、等级、携带模块等）——供各星区类型引用；
  6. **敌我分布规则**（按玩法决定；默认模式含**星门星区**处理）；
  7. **特殊效果字段**（待开发，先留位）；
  8. **难度修饰**（敌方强度等一律走配置，**不写死在代码**）。
- **数值策略**：所有数值均为**占位预填、待用户人工调校**。

## 3. 星区类型与「特殊星区类型」配置文件

- **基础类型**：**星球星区 / 矿物区 / 空区 / 特殊区**（**星门星区＝特殊类型之一**）；**恒星星区**＝中心特殊星区（**可参战**）。
- ★ **特殊类型星区也需要独立配置文件：一种一个**（如星门星区、特殊区各自一份）；建议位置 `js/data/sectorTypes/`（一类型一文件 + `index.js` 注册表，体例同上）。
  - **特殊类型配置内容（草案）**：`id` / `nameKey` / **生成位置规则**（如星门＝**随机生成在星域的「最上、最下、最左、最右」四个方位**）/ 数量范围 / 初始内容（矿物、星球(货物)、特殊结构）/ 默认 NPC 列表引用 / 特殊效果（待开发）/ 地图标记与底色。
- **星门星区（用户口径）**：属于**特殊星区类型**，**随机生成在星域的最上、最下、最左、最右四个方位**；默认（难度阶梯模式）用于**玩家单位进出**。

## 4. 星域初始化（配置 + 可预测随机性）

- **种子的使用（★ 用户口径）**：**仅在「星域配置界面」可输入**；**默认随机生成**；**生成/进入星域后可显示**（**显示位置待定**）。核心生成**用带种子的确定性伪随机** ⇒ **同一配置 + 同一种子 ⇒ 完全相同的星域**；生成逻辑**不使用 `Math.random()`/时间戳**（随机源只用于产生初始种子那一步）。
- **生成内容（逐星区）**：① **矿物**（星区储量）；② **星球(货物)**；③ **单位**（依该类型引用的 NPC 列表与敌我分布规则）；④ **特殊结构**（类型待定，配置先留字段）。
- **数量生成**：各类型星区数量在**配置的最小/最大值区间**内由种子确定性取值；**允许空星区**。
- **约束**：生成是**纯函数式**过程（输入＝配置＋种子 ⇒ 输出＝星域初始状态），**不依赖 tick/UI**；**镜像对等**（敌我对称规则一致）。

## 5. 布局生成（方形网格 · 近似圆）

- **网格**：方形网格 `(q,r)`；筛选规则＝**到中心的欧氏距离 ≤ 星域半径 R** ⇒ **近似圆形**分布；**中心 `(0,0)`＝恒星星区**（**可参战**）。
- **半径**：由配置给出，**编码上不设上限**（大半径 ⇒ 星区数量随面积增长）。
- **确定性**：纯函数、无随机（随机只来自种子）；`index` 按固定扫描顺序（先 `r` 后 `q`）编号 ⇒ 可复现、便于存档与调试。
- **特殊类型定位**：如**星门星区随机分布于「最上/最下/最左/最右」四个方位**（由配置的位置规则决定）。
- **特殊效果**：某些星区可带特殊效果（**待开发**，配置与 UI 先留位置）。

## 6. 地图交互与侧栏（星域大地图）

- **视图**：方形网格地图，**支持缩放与平移**；星区按**类型**用不同标记/底色呈现（恒星星区、星球星区、矿物区、空区、特殊区、星门星区）。
- **星区状态显示**：**星球(货物)**、**矿物**（储量）、**在星区中的单位示例**（少量代表单位图标/数量，不全量绘制）；建议**按缩放层级显示不同细节**。
- **点击星区 ⇒ 侧栏**：★ **侧栏内呈现的是“完整的该星区战斗场景”，且可操作指挥**（在侧栏内即可下达指令）；可开关、可在不同星区之间切换；**不是整屏切换**。
- **口径**：地图与侧栏**共用同一只读口径**（UI 不自算）；**同一时刻只绑定“当前侧栏星区”的完整细节**（避免多区同时全量重绘）。

## 7. 界面规划

| 界面 | 定位 | 入口 |
|---|---|---|
| **编队界面（现有）** | **保留 · 仅开发测试用** | ★ **界面中不再提供入口**；**通过控制台指令打开**（既有调试控制台 A6，指令名随实现定稿） |
| **星域配置界面（新建）** | 正式主入口 | ★ **占用原「编队配置」的入口位置**；选难度（`H1`…`Hn`）/玩法、**种子仅在此输入**（默认随机、可显示）、配置半径/类型增减/数量区间/NPC 列表、预览、**导出/导入（与内置配置文件同格式）** |
| **星域大地图** | 战斗主体 | 由星域配置界面确认后进入；缩放平移、查看各星区状态与类型、点区开侧栏看**完整星区战斗场景（可操作指挥）** |
| **最终结算界面** | **待定 · 本轮只预留接口** | 星域持续时间耗尽后进入（内容后续完善） |

## 8. 结束与最终结算

- **星域持续时间**（`durationTicks`，由配置按难度/玩法决定）：随 tick 递减，**独立于任何星区的战斗进程**；**归零 ⇒ 所有星区停止运行（不再 tick）并进入最终结算**。
- **保留但不执行**：原有“一方全灭 ⇒ 结束战斗”的判定逻辑**保留在代码中**（供未来模式使用），本轮**不执行**；星区无战斗单位时**静默空转、仍走 tick**。
- **最终结算**：★ **内容与形式待定** ⇒ 本轮**只预留接口**（星域层提供“结算入口 + 汇总数据出口”）。

## 9. 本轮非目标（后续轮次）

1. **星区间移动**（说明中标记“待实现”）：航行中状态（**不参与任何星区战斗、不可被选中**）+ 抵达入场 + 地图上可见；跨区阶段接口已预留。
2. **特定玩法**（除难度阶梯外的玩法内容，待定）。
3. **星区特殊效果**的具体设计（配置与 UI 先留字段）。
4. **最终结算**的具体内容与形式。
5. **跨星区相互作用**（增援、跨区目标、跨区资源输送等）。
6. **存档扩展**：星域级状态（多星区 + 时间 + 种子 + 生成结果）的序列化与迁移。
7. **敌 AI 跨区行为**（扩张/巡逻等）。

## 10. 开发步骤（按序推进 · 一步一确认）

> 约定：每步完成后由开发方给出《测试期望》+《测试方案》，用户人工测试确认「符合测试期望」后才进入下一步；**数值一律占位预填、待用户调校**；**文档随开发同步（由 AI 维护）**。

### 阶段 0 · 界面分离与入口调整（先做）
- **S0-1 编队配置分离**：现有「编队配置」（单星区模拟）**从界面移除入口**，改为**通过控制台指令打开**（既有调试控制台 A6；指令名随实现定稿，如 `drill`）；原入口位置**改为「星域配置界面」的占位入口**；编队界面本身**功能不变**（继续作为逐模块测试环境）。
- **验收**：界面无编队入口、控制台指令可正常打开并使用编队/开战；原入口位置可进入星域配置界面（此时可为占位页）。

### 阶段 A · 数据与配置层（纯数据 + 纯函数，无 UI 依赖）
- **A-1 种子随机工具**：`js/core/rng.js`——字符串/数字种子 ⇒ **确定性 PRNG**（同种子 ⇒ 同序列）；**禁止 `Math.random()`/时间戳**用于生成内容。
- **A-2 特殊星区类型配置**：`js/data/sectorTypes/`（**一种一个文件** + `index.js` 注册表）——星门星区、特殊区等；含**生成位置规则**（星门＝**最上/最下/最左/最右四方位随机**）、数量范围、初始内容、默认 NPC 引用、地图标记与底色、特殊效果占位。
- **A-3 NPC 列表与单位配置结构**：单位种类 id、数量（或区间）、等级、携带模块等；供各星区类型引用。
- **A-4 星域配置文件**：`js/data/starfields/`（**一难度/玩法一文件** + `index.js`）——首批 **`H1`…`Hn`**；字段＝`radius`、`durationTicks`、类型增减开关、各类型数量最小/最大值、各类型默认 NPC 列表、敌我分布、特殊效果占位。
- **A-5 星域生成器**：`js/data/starfield.js`（**纯函数**：配置＋种子 ⇒ 星域初始状态）——方网格「欧氏距离 ≤ R」近似圆布局、类型数量区间取值、NPC 展开、特殊类型定位（星门四方位）、允许空星区；**同配置＋同种子结果完全一致**。
- **验收**：可在临时入口/控制台生成并打印星域摘要；**同种子两次生成完全一致**、不同种子不同；空区与各类型数量落在配置区间内。

### 阶段 B · 引擎容器层
- **B-1 `battle.js` 可实例化**：`createBattle(config) → battle 实例`，状态不共享（含日志、星区、单位、模块实例等）；**对外只读口径形状不变** ⇒ 既有 UI/测试零回归。
- **B-2 星域容器** `js/systems/starfield.js`：多星区实例管理、**固定顺序依次 `step()`**、**星域自身持续时间**倒计时、**跨星区阶段占位接口**、时间耗尽 ⇒ **停止全部 tick** 并进入**结算入口（本轮仅预留接口）**。
- **验收**：多星区同时运行且互不干扰；同种子同结果（可复现）；星区数较大时性能可接受；时间耗尽后全部停止。

### 阶段 C · UI 层
- **C-1 星域大地图**：方网格渲染、**缩放/平移**、按类型标记/底色、状态摘要（星球(货物)／矿物／单位示例，**按缩放层级显示不同细节**）、点击星区。
- **C-2 侧栏星区场景**：**完整战斗场景**呈现在侧栏内、**可操作指挥**、可开关与切换星区；地图与侧栏**共用同一只读口径**、同一时刻只绑定当前侧栏星区的细节。
- **C-3 星域配置界面**：选难度/玩法、**种子仅此处可输入**（默认随机、生成后显示）、半径、类型增减、各类型数量区间、各类型默认 NPC 列表与 NPC 列表编辑、**预览生成结果**、**导出/导入（与内置配置文件同格式）**；确认后进入星域大地图。
- **C-4 入口收口**：原「编队配置」入口位置＝星域配置界面（S0-1 的占位页在此正式实装）。
- **验收**：从主界面经星域配置界面开战；地图可缩放平移、点区开侧栏并指挥；导出文件可被导入还原为同一星域（同种子）。

### 阶段 D · 收尾
- **D-1 i18n**：全部新界面/新键 zh/en 成对。
- **D-2 文档同步**：本文件（说明文档）、`模块字段说明.md`、`开发步骤与开发清单.md`（§6/§10）随开发更新。
- **D-3 性能与回归**：大半径星区数下的每 tick 开销、既有单星区玩法（控制台入口）与全部模块功能的回归核对。

## 11. 待确认清单（开发前需定）

1. **配置字段名与结构定稿**（沿 §2/§3 草案：类型开关、数量区间、NPC 列表与单位配置、特殊类型配置、特殊效果字段）。
2. **NPC 列表与「单位配置」的字段粒度**（种类 id / 数量或区间 / 等级 / 携带模块 / 是否允许多条同类）。
3. **种子的显示位置**（生成/进入星域后显示于何处：地图顶栏 / 星域信息栏 / 结算）。
4. **星区类型完整清单与各自生成规则**（星球星区/矿物区/空区/特殊区之外是否还有；各类型生成什么）。
5. **星门星区的细节**：四方位各一个还是可多个、是否随难度变化、玩家单位「进出」的具体机制（为下一轮移动/入场预留）。
6. **侧栏**布局：完整战斗场景在侧栏内的排布（宽度、缩放、是否可折叠）、操作指挥的入口与既有指令是否全部可用。
7. **导出/导入格式细节**：文件扩展名、是否含种子与生成结果、与内置配置文件的字段一致性校验。
8. **控制台指令名**（打开编队测试界面的指令定稿）。
9. **最终结算**的内容与形式（哪些数据上屏、是否评分）。
