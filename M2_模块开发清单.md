# M2 模块开发清单（活文档 · 临时顺序文件）

> 权威来源：`开发步骤与开发清单.md` §6.2（模块总表）。本文件是 M2 阶段逐个开发模块的**顺序与规格记录**，随开发更新。
> 铁律：**所有模块的功能与数值一律由用户人工设定，AI 不得擅自决定**；数值进 `data/` 配置，禁止硬编码；每个模块经用户人工测试确认后才进入下一个。
>
> 数值策略（用户定稿，2025 会话）：**一般模块默认满级 = 16 级**；开发时由 AI **预填占位数值**（逐级绝对表），用户后期人工逐级调整。

## 1. 模块升级 / 等级模型（已定稿 · 待开始实现时落实）

- 每模块配置带 **maxLevel**（一般默认 16）与 **levels[]**：`levels` 是**逐级绝对表**（连续等级 1..maxLevel）。
- **允许随级变的字段**：数值类词条 + **type 钩子 / target 目标 kinds 等机制类字段**均可随级定义。
- **跨级继承/回退规则**：某等级**未填的字段，回退取“上一级”的值**（递归向上，最终以 Lv1 / base 为兜底）。因此高阶等级只需写“与上一级的差异”，不必重复全部字段。
- **实例默认等级 = 1**；模块实例带 `level`，引擎按实例等级结算。
- **“升级(耗资源)”操作留到 M3/M4 船坞**；M2 只落地：数据级模型 + 等级生效 + 演练开战时每舰每模块可选等级（1..maxLevel）逐级验证。
- 数值给法：**逐级绝对数值表**（每级各字段逐级填写；留空 = 继承上一级）。

### 文件组织（待你最终确认 A/B）
- 倾向 **A（推荐）**：每模块一个文件 `data/modules/<id>.js`（内分 `base` 共享段 与 `levels[]` 逐级段）+ `data/modules/index.js` 汇总 `MODULES` 注册表；沿用 .js（不引入 JSON，避免 file:// / 旧浏览器 import-assert 兼容问题）。
- B：维持单一 `data/modules.js`，levels 内嵌到各模块对象（简单但文件会随模块增多而膨胀）。

## 2. 每级规格表模板（每个模块填一份）

模块：`<id>`　分类：`<category>`　名称词条：`<nameKey>`

**base（Lv1/共享字段，可被 levels 覆盖）**
| 项 | 值（用户填） |
|---|---|
| glyph 槽位图标 |  |
| category 分类 |  |
| target 目标 kinds / countMode / maxCount |  |
| type（特殊钩子列表） |  |

**levels[]（逐级）——只填“该级与上一级不同”的字段，其余自动继承上一级**
| 等级 | 覆盖的字段（数值词条 / target / type 均可） | 值 | 说明 |
|:---:|------|:---:|------|
| Lv1 | （= base，无需重复） |  |  |
| Lv2 |  |  |  |
| … |  |  |  |
| LvMax= |  |  |  |

词条词汇表（沿用引擎下划线词条）：`damage / max_damage / ramp_per_hit / shield_gain / shield_cap_bonus / shield_gain_target / shield_cap_target / hp_target / hp_cap_target / energy_target / energy_cap_target / duration_ticks / cooldown_ticks / energy_cost`（用到的列；未用留空继承）。

## 3. 开发顺序（逐个进行，当前：火炮重做）

> 你指定：**原有模块都要重做，先从火炮(C01)开始**。每行完成状态人工更新。
>
> **步骤 1（等级化数据模型 + 文件拆分 + 火炮单体重做）**：代码已实现——
> 每模块一文件 `data/modules/<id>.js`，`data/modules.js` 作 index；`module.js` 加入
> `resolveModuleCfg(def,level)`/`moduleMaxLevel()`（base=Lv1，levels[] 逐级差异，
> 未填字段回退上一级）；实例带 level 按级生效；火炮 target 改 single×1。
> ✔ **步骤 1 已通过人工测试**。
>
> **步骤 2（火炮 16 级占位 + 等级 UI + 对战等级显示）**：代码已实现——
> cannon `maxLevel:16` 并预填 Lv1…Lv16 damage 占位递增表(cooldown/energy 沿用 Lv1)；
> 演练装配每模块在 maxLevel>1 时显示 **Lv 下拉(1..16)**；开战/再战按所选等级生成实例；
> 对战单位模块 chip 右下角与详情模块行显示 `L{n}`。
> 数值为**占位预填待用户调校**。状态：**待用户人工测试确认**（期望/方案已给出）。

| 顺序 | 模块 ID | 名称 | 分类 | 状态 | 备注 |
|:---:|------|------|------|:---:|------|
| 1 | cannon | 火炮 | 攻击 | ✍ 代码完成待测试 | **目标 single×1**；maxLevel 16；Lv1=damage14/cd20/能6；Lv2..16 为占位预填待调 |
| 2 | regenShield | 再生护盾 | 护盾 | ☐ 待重做 | 原 shield_cap_bonus500/shield_gain30/cd5/能10 |
| 3 | rampCannon | rampCannon | 攻击(测试) | ☐ 待重做 | 测试逐步伤害模块 |
| 4+ | C02–C07 | 其余攻击模块 | 攻击 | ☐ | 大型火炮/激光/双向激光/密集弹幕/火箭发射器(Q5)/脉冲发生器 |
| 后续 | C08–C14 | 护盾模块 | 护盾 | ☐ | 阿尔法/脉冲/欧米茄/防爆/反射/同盟 |
| 后续 | C38–C42 | 无人机模块 | 无人机 | ☐ | 护盾/维修/火箭/激光/火炮无人机 |
| 后续 | C15–C24 | 功能模块 | 功能 | ☐ | 含主动技能(技能UI行)/条件被动；归属逐个定义(Q7) |

## 4. 每个模块的开发流程（M2 通用）

1. AI 依据本文件给该模块《规格表草稿》+《测试期望/方案》；
2. **用户填写/确认**该模块 Lv 表（含 LvMax、各级差异字段与数值、是否机制随级变）与机制归属；
3. AI 实现：模块数据文件(levels) + 引擎(如需新钩子/新词条) + i18n + UI 呈现 + 等级可选；
4. 用户人工测试确认 → 清单勾 ✔ → 进入下一个。

## 5. M2 里程碑任务（先模块后其余；模块开发结束后再处理）

| 任务 | 说明 | 状态 |
|------|------|:---:|
| 模块逐个开发 + 等级模型 | 本文件 §3 顺序 | ☐ |
| M2.5 后勤行最小规则 | 运输/采矿船入场(combat 之外的船型放开)、在场增益/产出占位 | ☐ 后做 |
| M2.6 敌人场景 data/enemies.js | 多船多行敌方场景选择 | ☐ 后做 |
| M2.7 战斗日志细节 | 关键事件日志输出到战斗下方 | ☐ 后做 |
| M2.8 结算面板 | 耗时/战损/奖励占位 | ☐ 后做 |
| M2 验收 | 见清单 §8.3；完成后收尾 M2 | ☐ |
